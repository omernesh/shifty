---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - app/pnpm-workspace.yaml
  - app/plugins/shifty-audit-writer/package.json
  - app/plugins/shifty-audit-writer/.gitkeep
  - app/plugins/shifty-auth/.gitkeep
  - app/connections/.gitkeep
  - app/pages/.gitkeep
  - playwright.config.ts
  - package.json
  - tests/e2e/_fixtures/seed-tenants.ts
  - tests/e2e/_fixtures/teardown.ts
  - tests/e2e/.gitkeep
  - tools/check-queries.mjs
  - tools/fixtures/kibbutz.sql
  - tools/test/invite-code.test.mjs
  - db/migrations/0002_tenancy_and_org.sql
autonomous: false
requirements:
  - D-01
  - OPS-01
  - OPS-02
  - OPS-10
  - SEC-08
  - TEN-01
  - TEN-02
  - TEN-03
  - TEN-04
  - TEN-05
  - I18N-07
  - PERF-04
  - AUTH-04
user_setup:
  - service: hpg5-env
    why: "Provision RESEND_API_KEY and RESEND_FROM_EMAIL in C:\\shifts-manager\\.env on hpg5 (blocks AUTH-01 in Plan 03, but added here so .env.example template stays in sync)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend dashboard -> API Keys -> Create -> copy `re_...` token"
      - name: RESEND_FROM_EMAIL
        source: "Set to `shifty@nesher.co` (or alternate verified sender once Resend domain is provisioned)"

must_haves:
  truths:
    - "Lowdefy runtime smoke test on hpg5 returns HTTP 200 at /employees with zero ERR_MODULE_NOT_FOUND across 10 page loads"
    - "`docker compose run --rm migrate up` exits 0; second invocation also exits 0 (idempotent)"
    - "`docker compose config` lists services `lowdefy`, `postgres`, `migrate` (plus commented stubs for `solver`, `cron`, `waha`)"
    - "After migration 0002 applies, `\\d tenant`, `\\d org_unit`, `\\d app_user`, `\\d soldier`, `\\d membership`, `\\d users`, `\\d accounts`, `\\d sessions`, `\\d verification_tokens` all exist with tenant_id NOT NULL (except `tenant`/`users` which use id as tenant key) and Hebrew display columns COLLATE \"he-x-icu\""
    - "`node tools/check-queries.mjs` exits 0 on the current YAML tree (no queries yet so nothing fails)"
    - "`node --test tools/test/invite-code.test.mjs` passes (Crockford regex unit tests)"
    - "`tools/fixtures/kibbutz.sql` exists with 12 soldiers including one whose display_name contains U+2019"
    - "playwright.config.ts + tests/e2e/_fixtures/seed-tenants.ts + teardown.ts exist; `npx playwright test --list` succeeds (lists 0 specs is OK)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "migrate one-shot service definition + new RESEND_* env passthrough on lowdefy service + commented stubs for solver/cron/waha"
      contains: "image: migrate/migrate:v4.18.3"
    - path: ".env.example"
      provides: "Template documenting every env var Phase 1 requires (POSTGRES_*, NEXTAUTH_*, RESEND_*)"
      contains: "RESEND_API_KEY"
    - path: "app/pnpm-workspace.yaml"
      provides: "Workspace declaration so plugins/* resolve via workspace:* in app/package.json"
      contains: "plugins/*"
    - path: "playwright.config.ts"
      provides: "Playwright base config with baseURL and sequence-mode setting"
      contains: "fullyParallel: false"
    - path: "package.json"
      provides: "Root-level dev dependencies (@playwright/test, pg) for running Wave 0 tests outside of the Lowdefy container"
      contains: "@playwright/test"
    - path: "tests/e2e/_fixtures/seed-tenants.ts"
      provides: "seedTwoTenants() + signInAs() + getTenantBIds() helpers"
      exports: ["seedTwoTenants", "signInAs", "getTenantBIds"]
    - path: "tests/e2e/_fixtures/teardown.ts"
      provides: "teardownTestData() TRUNCATE-in-reverse-FK-order helper"
      exports: ["teardownTestData"]
    - path: "tools/check-queries.mjs"
      provides: "CI grep gate scaffold (functional but with limited rule set; Plan 04 hardens it)"
      contains: "ALLOWLIST_MARKER"
    - path: "tools/fixtures/kibbutz.sql"
      provides: "12-soldier seed (one with U+2019) + 64-day planning_window per RESEARCH Pattern 15"
      contains: "11111111-1111-1111-1111-111111111111"
    - path: "tools/test/invite-code.test.mjs"
      provides: "Crockford base32 regex unit tests (AUTH-04 validation)"
      contains: "0123456789ABCDEFGHJKMNPQRSTVWXYZ"
    - path: "db/migrations/0002_tenancy_and_org.sql"
      provides: "tenant + org_unit + app_user + soldier + membership + NextAuth KnexAdapter tables (users/accounts/sessions/verification_tokens), CITEXT extension, he-x-icu collation, composite indexes"
      contains: "CREATE COLLATION IF NOT EXISTS \"he-x-icu\""
    - path: "app/plugins/shifty-audit-writer/package.json"
      provides: "Plugin manifest scaffold; full src/ implementation added in Plan 02"
      contains: "\"name\": \"shifty-audit-writer\""
  key_links:
    - from: "docker-compose.yml `migrate` service"
      to: "db/migrations/*.sql"
      via: "volume mount `./db/migrations:/migrations:ro` + command `-path=/migrations up`"
      pattern: "image: migrate/migrate:v4\\.18\\.3"
    - from: "app/pnpm-workspace.yaml"
      to: "app/plugins/shifty-audit-writer/"
      via: "packages: ['plugins/*'] resolves workspace:* references in app/package.json"
      pattern: "plugins/\\*"
    - from: "0002_tenancy_and_org.sql `app_user` table"
      to: "0002 NextAuth `users` table"
      via: "Documented in file header — `app_user` is the Shifty domain user; `users` is the Auth.js adapter table; KnexAdapter writes to `users`, session callback joins via email"
      pattern: "app_user|users"
---

<objective>
Establish the Phase 1 foundation: prove the Lowdefy runtime fix from commit b8afba1 is live on hpg5, scaffold every Wave-0 file that later plans' `<automated>` verify hooks depend on, add the golang-migrate compose service, and ship migration 0002 (tenancy + NextAuth adapter schema + ICU collation + composite indexes).

Purpose: Without Wave 0 (test infra, kibbutz fixture, workspace config, check-queries scaffold) every later plan's verify command fails with file-not-found. Without 0002 the auth plan (Plan 03) has no `app_user` / `membership` / `users` tables to write to. Without the smoke test, we'd build on a possibly-broken runtime.

Output: A buildable repo whose `docker compose` stack on hpg5 has `lowdefy`+`postgres`+`migrate`, with migration 0002 applied (15 new tables, 4 of them Auth.js KnexAdapter tables, Hebrew collation on 4 display columns, composite `(tenant_id, ...)` indexes per PERF-04), every Wave 0 test scaffold file in place, and Playwright + Node test runner usable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md
@.planning/phases/01-foundations/01-VALIDATION.md
@CLAUDE.md
@docs/PRD.md
@.claude/skills/lowdefy/SKILL.md
@.claude/skills/lowdefy/reference/06-operators.md
@.claude/skills/lowdefy/reference/09-plugins.md
@.claude/skills/lowdefy/reference/10-deployment.md
@db/migrations/0001_init.sql
@app/lowdefy.yaml
@app/Dockerfile
@app/package.json
@docker-compose.yml

<interfaces>
<!-- Existing identifiers the executor must align with -->

From `db/migrations/0001_init.sql` (lines 1-101 — already in context):
- Extension: `pgcrypto` (already enabled; provides `gen_random_uuid()`)
- Pattern: `BEGIN; ... COMMIT;` envelope on every migration
- Pattern: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()` on every domain table
- Pattern: `created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` + `set_updated_at()` trigger function (already defined; reuse it; do NOT redeclare)
- Existing tables: `employees`, `shifts`, `assignments`, `availability`, `time_clock_entries` — DO NOT TOUCH in Phase 1 (Phase 2 drops them via 0008)

From `docker-compose.yml` (already in context):
- Existing service names: `lowdefy` (build: ./app, port 8080:3000), `postgres` (postgres:16, no host port, healthcheck pg_isready)
- Existing env var pattern: `${VAR:-default}` for defaultable, `${VAR:?missing}` for required
- Existing healthcheck shape on lowdefy: node -e HTTP probe

From `app/package.json` (already in context):
- name: shifty, version: 0.1.0, private: true
- existing deps: lowdefy@5.3.0, @lowdefy/connection-knex@5.3.0, @lowdefy/blocks-aggrid@5.3.0
- scripts: build/dev/start via `lowdefy <subcommand>`

From `app/lowdefy.yaml` (already in context):
- Existing plugins: `@lowdefy/connection-knex@5.3.0`, `@lowdefy/blocks-aggrid@5.3.0`
- Existing connection id: `shifts_db` (type Knex, uses POSTGRES_CONNECTION_STRING secret)
- Existing pages: `home`, `employees` (smoke-test surface — must keep working through Phase 1 per D-06)

NextAuth KnexAdapter table contract (Auth.js convention — exact names required):
- `users` (id UUID PK, name TEXT, email TEXT UNIQUE, "emailVerified" TIMESTAMPTZ, image TEXT)
- `accounts` (id UUID PK, "userId" UUID FK, type TEXT, provider TEXT, "providerAccountId" TEXT, refresh_token TEXT, access_token TEXT, expires_at INTEGER, token_type TEXT, scope TEXT, id_token TEXT, session_state TEXT, UNIQUE(provider, "providerAccountId"))
- `sessions` (id UUID PK, "sessionToken" TEXT UNIQUE NOT NULL, "userId" UUID FK, expires TIMESTAMPTZ NOT NULL)
- `verification_tokens` (identifier TEXT, token TEXT UNIQUE NOT NULL, expires TIMESTAMPTZ NOT NULL, PRIMARY KEY (identifier, token))

Shifty domain table contract (from PRD §10 + RESEARCH Pattern 3 — minimum columns for Phase 1):
- `tenant` (id UUID PK, name TEXT COLLATE "he-x-icu" NOT NULL, org_depth SMALLINT NOT NULL CHECK (org_depth BETWEEN 1 AND 3), created_at, updated_at)
- `org_unit` (id UUID PK, tenant_id UUID FK→tenant ON DELETE CASCADE, parent_id UUID FK→org_unit nullable, level SMALLINT NOT NULL, name TEXT COLLATE "he-x-icu" NOT NULL, created_at, updated_at, UNIQUE(tenant_id, parent_id, name))
- `app_user` (id UUID PK, tenant_id UUID FK→tenant ON DELETE CASCADE, email CITEXT NOT NULL, display_name TEXT COLLATE "he-x-icu", locale TEXT NOT NULL DEFAULT 'he' CHECK (locale IN ('he','en')), user_id UUID nullable FK→users(id) ON DELETE SET NULL — links domain user to Auth.js user, created_at, updated_at, UNIQUE(tenant_id, email))
- `soldier` (id UUID PK, tenant_id UUID FK→tenant ON DELETE CASCADE, user_id UUID nullable FK→app_user(id) ON DELETE SET NULL, display_name TEXT COLLATE "he-x-icu" NOT NULL, color TEXT NOT NULL DEFAULT '#3B82F6', seniority SMALLINT NOT NULL DEFAULT 0 CHECK (seniority BETWEEN 0 AND 10), role_tags TEXT[] NOT NULL DEFAULT '{}', phone_e164 TEXT, status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active','archived')), notes TEXT, created_at, updated_at)
- `membership` (id UUID PK, tenant_id UUID FK→tenant ON DELETE CASCADE, soldier_id UUID FK→soldier ON DELETE CASCADE, org_unit_id UUID FK→org_unit ON DELETE CASCADE, role TEXT NOT NULL CHECK (role IN ('unit_admin','team_manager','member','viewer')), created_at, UNIQUE(soldier_id, org_unit_id))

Note: `tenant.id` IS the tenant key — no `tenant_id` column on `tenant` itself. RLS policy in Plan 03 handles this special case (USING (id = current_setting('app.current_tenant')::uuid)).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Smoke-test Lowdefy runtime on hpg5 (D-01 verification)</name>
  <read_first>
    - CLAUDE.md §SSH access (plink command), §Common ops on hpg5
    - .planning/phases/01-foundations/01-RESEARCH.md §Pattern 1 (Smoke Test — Exact Command Sequence)
    - .planning/phases/01-foundations/01-CONTEXT.md §D-01
    - app/Dockerfile (read entire 38-line file; understand the symlink-preservation rationale in lines 1-15)
  </read_first>
  <what-built>
    The Lowdefy runtime fix at commit b8afba1 is already deployed on hpg5 (per RESEARCH SUMMARY — live verification 2026-05-12 confirmed HTTP 200 + zero ERR_MODULE_NOT_FOUND). This task RE-VERIFIES the smoke test before any new code lands so we have a clean baseline.
  </what-built>
  <action>
    Run the exact 4-command sequence from RESEARCH Pattern 1 against hpg5. Do not modify any code in this task — this is a verify-and-record step per D-01.

    Commands (run each in sequence; abort and surface the failure if any command's expected output is not seen):

    1. Container health check:
       `plink -ssh -l claude -pw "Onclaude2103" -batch -hostkey "SHA256:tPg5mYQbJO/9ccGmNGeyJeQQSPXq+C6SL3EHJcbRZMQ" hpg5 "docker ps --format \"{{.Names}} {{.Status}}\""`
       Expected: lines containing `shifty-lowdefy ... (healthy)` AND `shifts-postgres ... (healthy)`.

    2. HTTP smoke probe:
       `plink ... hpg5 "powershell -c \"(Invoke-WebRequest -Uri http://localhost:8080/employees -UseBasicParsing).StatusCode\""`
       Expected: `200`.

    3. 10 page loads (cmd loop syntax inside plink; PowerShell `for` would need escaping — use cmd):
       `plink ... hpg5 "for /l %i in (1,1,10) do @curl -s -o NUL http://localhost:8080/employees"`
       Expected: command exits 0 (no body output is fine; `-s -o NUL` discards both).

    4. ERR_MODULE_NOT_FOUND log scan:
       `plink ... hpg5 "docker logs shifty-lowdefy --since 2m 2>&1 | findstr ERR_MODULE_NOT_FOUND"`
       Expected: no output (findstr exits 1 when no match — that is the PASS signal).

    PASS criteria: HTTP 200 from step 2 AND step 4 produces zero matches.
    FAIL criteria: any non-200 OR any ERR_MODULE_NOT_FOUND in logs. On FAIL, halt Plan 01 immediately and activate the 5-day escape-hatch timebox per CONTEXT.md D-01 (switch to npm, or escalate Lowdefy lock re-open). Do not proceed to Task 2 on FAIL.
  </action>
  <how-to-verify>
    1. Open a terminal and run each command above. Confirm step-by-step pass.
    2. The session log of all four commands MUST be appended to PLAN summary at the end of this task as evidence.
    3. Sign-off requires both authors agreeing: "smoke passed; b8afba1 fix is live on hpg5".
  </how-to-verify>
  <verify>
    <automated>plink ... hpg5 "powershell -c \"(Invoke-WebRequest -Uri http://localhost:8080/employees -UseBasicParsing).StatusCode\"" returns 200; plink ... hpg5 "docker logs shifty-lowdefy --since 2m 2>&1 | findstr ERR_MODULE_NOT_FOUND" returns no output (exit code 1)</automated>
  </verify>
  <acceptance_criteria>
    - Curl returns HTTP 200 against http://hpg5:8080/employees
    - `docker logs shifty-lowdefy --since 2m | findstr ERR_MODULE_NOT_FOUND` produces zero matches
    - Both containers (shifty-lowdefy, shifts-postgres) reported (healthy) in `docker ps`
    - User explicitly approves: "smoke test passed; proceed to Wave 0 scaffolding"
  </acceptance_criteria>
  <resume-signal>Type "smoke passed" to proceed; type "smoke failed: &lt;details&gt;" to activate 5-day escape hatch and halt Plan 01.</resume-signal>
  <done>
    Smoke test PASS confirmed live on hpg5 against the b8afba1 fix; user has approved proceeding; no code changes were made during this task.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wave 0 scaffolding — workspace, test infra, fixtures, env template</name>
  <read_first>
    - .planning/phases/01-foundations/01-PATTERNS.md (full file — pattern map for every Wave 0 file)
    - .planning/phases/01-foundations/01-VALIDATION.md §Wave 0 Requirements
    - .planning/phases/01-foundations/01-RESEARCH.md §Pattern 9 (check-queries.mjs full implementation, lines 858-936), §Pattern 10 (Playwright fixture shape), §Pattern 15 (kibbutz fixture full SQL, lines 1149-1186), §Pitfall 3 (pnpm-workspace.yaml)
    - app/package.json (existing deps)
    - docker-compose.yml (existing services)
    - CLAUDE.md §SSH access (need to know `.env` lives on hpg5, not in repo)
  </read_first>
  <files>
    app/pnpm-workspace.yaml
    .env.example
    package.json
    playwright.config.ts
    tests/e2e/_fixtures/seed-tenants.ts
    tests/e2e/_fixtures/teardown.ts
    tests/e2e/.gitkeep
    tools/check-queries.mjs
    tools/fixtures/kibbutz.sql
    tools/test/invite-code.test.mjs
    app/plugins/shifty-audit-writer/package.json
    app/plugins/shifty-audit-writer/.gitkeep
    app/plugins/shifty-auth/.gitkeep
    app/connections/.gitkeep
    app/pages/.gitkeep
  </files>
  <action>
    Create every Wave 0 file below exactly as specified. Use the Write tool — do not use heredoc shell scripts. All paths are relative to repo root C:\Projects\shifts manager\.

    1. **`app/pnpm-workspace.yaml`** (per RESEARCH Pitfall 3, lines 1247-1256) — exact content:
       ```yaml
       packages:
         - '.'
         - 'plugins/*'
       ```

    2. **`.env.example`** — top-level template (repo root). Document every env var Phase 1 requires. Sections: `# Postgres` (POSTGRES_USER=shifts, POSTGRES_PASSWORD=changeme, POSTGRES_DB=shifts, POSTGRES_CONNECTION_STRING template comment), `# NextAuth` (NEXTAUTH_SECRET=&lt;openssl rand -base64 32&gt;, NEXTAUTH_URL=https://apps.nesher.co), `# Resend (Phase 1 magic-link email — required by Plan 03)` (RESEND_API_KEY=re_..., RESEND_FROM_EMAIL=shifty@nesher.co). Each var on its own line with a comment above describing its source. Note in a header comment: "This template is committed; the real `.env` lives ONLY on hpg5 at C:\\shifts-manager\\.env per CLAUDE.md."

    3. **`package.json`** (repo root — separate from `app/package.json`) — minimal dev-only package for test tooling. Content:
       ```json
       {
         "name": "shifts-manager-dev",
         "version": "0.1.0",
         "private": true,
         "description": "Repo-root dev tooling (Playwright, pg). Application code lives in app/.",
         "scripts": {
           "test:e2e": "playwright test",
           "test:check-queries": "node tools/check-queries.mjs",
           "test:invite-code": "node --test tools/test/invite-code.test.mjs"
         },
         "devDependencies": {
           "@playwright/test": "^1.49.0",
           "pg": "^8.13.1"
         }
       }
       ```
       Pin versions explicitly; do not use `*` or `latest`.

    4. **`playwright.config.ts`** — minimal config per VALIDATION §Test Infrastructure. Use `baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:8080'` so CI can override to hpg5 later. Set `fullyParallel: false` (shared DB state). Set `testDir: './tests/e2e'`. Export default `defineConfig({...})`.

    5. **`tests/e2e/.gitkeep`** — empty file (so the empty dir commits cleanly).

    6. **`tests/e2e/_fixtures/seed-tenants.ts`** — TypeScript fixture per RESEARCH Pattern 10. Use the `pg` package (NOT Lowdefy) to connect directly to localhost:5432. Export three functions:
       - `seedTwoTenants()`: inserts two `tenant` rows (UUIDs `11111111-...` and `22222222-...`), one `org_unit` per tenant, one `app_user` per tenant (`admin-a@example.test` / `admin-b@example.test`), one `soldier` per tenant linked to that app_user, one `membership` per tenant with role=`unit_admin`. Returns `{ tenantA: { tenantId, adminEmail, orgUnitId, soldierId }, tenantB: {...} }`.
       - `signInAs(email: string)`: stub for now — return `{ cookies: [], userId: '' }`. Plan 03 wires the real sign-in. Mark with a TODO comment: "Plan 03 will implement; until then, tests using this should be skipped."
       - `getTenantBIds(tenantB)`: returns `{ soldiers: [tenantB.soldierId], windows: [], assignments: [] }` — Phase 1 only seeds tenants + soldiers; later phases extend.

       The file uses `import { Client } from 'pg';` and reads connection from `process.env.PG_TEST_URL ?? 'postgres://shifts:changeme@localhost:5432/shifts'`. DDL never; only INSERT.

    7. **`tests/e2e/_fixtures/teardown.ts`** — TRUNCATE in reverse FK order. Export `teardownTestData()`. List (in TRUNCATE order, reverse-FK): `membership`, `soldier`, `app_user`, `org_unit`, `tenant`, then the NextAuth tables `sessions`, `accounts`, `verification_tokens`, `users`. Use `TRUNCATE TABLE ... RESTART IDENTITY CASCADE`. Wrap in try/finally so connection always closes.

    8. **`tools/check-queries.mjs`** — Plan-01 SCAFFOLD version (full hardening lives in Plan 04). Copy the exact 60-line implementation from RESEARCH Pattern 9 (lines 862-920) verbatim, including the `ALLOWLIST_MARKER`, `KNEX_REQUEST_TYPES`, `TENANT_FILTER_PATTERN`, `DML_PATTERN`, `collectYaml`, `extractType` helpers. The scaffold MUST exit 0 when run against the current `app/` (no requests yet that need tenant_id; the bootstrap `employees` query lives in `app/lowdefy.yaml` but is on the legacy single-tenant table — annotate it with `-- @gsd-allow-untenanted: bootstrap smoke-test surface; legacy 0001 table; superseded in Phase 2` only if the script flags it. If the script does NOT flag the existing `employees` query because the query string lacks any of the DML keywords... actually it has `SELECT`. So annotate it). Re-edit `app/lowdefy.yaml` to add the allowlist comment INSIDE the existing query block (insert as the first line of the `query: |` block before `SELECT`).

    9. **`tools/fixtures/kibbutz.sql`** — Copy verbatim from RESEARCH Pattern 15 (lines 1152-1185). EXTEND the truncated soldier list with 9 additional rows so the total is exactly 12 soldiers (the U+2019 soldier is row 12). The 11 normal soldier names (Hebrew) the executor may use: יוסי כהן, דני לוי, מרב גולן, אמיר ברק, רותם דהן, יעל מזרחי, חיים פרץ, אלון שמואל, מאיה אבני, גיא צור, נועה שטרן. The 12th (U+2019) is נועם ג'לאל with U+2019 between ג and ל. UUIDs: aaaa..., bbbb..., cccc..., ddddd..., eeeee..., 1111aaaa..., 2222aaaa..., 3333aaaa..., 4444aaaa..., 5555aaaa..., 6666aaaa..., ffff... (use any pattern of 12 distinct UUIDs; the spec only mandates `ffff...` for the smart-quoted soldier). NOTE: this fixture references tables (`soldier`, `planning_window`) that don't exist after only 0002 (which adds soldier). `planning_window` lands in 0003 (Plan 02). To keep this file valid at end of Plan 01, COMMENT OUT the `INSERT INTO planning_window` block with a comment "-- Uncomment after migration 0003 applies (Plan 02)". The `tenant`/`org_unit`/`soldier` inserts run after Plan 01 lands 0002. **Plan 02 Task 1 owns uncommenting this block** (Blocker 4 fix tracked in checker): once 0003 lands, the commented INSERT block is restored to live SQL and the fixture re-loaded.

    10. **`tools/test/invite-code.test.mjs`** — Node built-in test runner per PATTERNS §`tools/test/invite-code.test.mjs`. Exact alphabet: `0123456789ABCDEFGHJKMNPQRSTVWXYZ` (no I, L, O, U per AUTH-04). Regex: `/^[0-9A-HJKMNPQRSTVWXYZ]{8}$/`. Tests: (a) valid 8-char string `0123ABCD` matches; (b) lowercase `abcdefgh` rejected; (c) ambiguous chars I/L/O/U each rejected when in position 8 (`1234567I`, `1234567L`, `1234567O`, `1234567U`); (d) 7-char string rejected; (e) 9-char string rejected. Run with `node --test`.

    11. **`app/plugins/shifty-audit-writer/package.json`** — manifest scaffold per PATTERNS §`app/plugins/shifty-audit-writer/package.json`. Plan 02 adds the `src/*.js` files; this task only creates the manifest + `.gitkeep` so the workspace resolves. Exact content:
        ```json
        {
          "name": "shifty-audit-writer",
          "version": "1.0.0",
          "type": "module",
          "exports": {
            "./connections": "./src/connections.js",
            "./types": "./src/types.js"
          }
        }
        ```

    12. **`.gitkeep` files**: `app/plugins/shifty-audit-writer/.gitkeep`, `app/plugins/shifty-auth/.gitkeep`, `app/connections/.gitkeep`, `app/pages/.gitkeep`. Empty files so the directory structure commits.

    Per D-08 plugin pattern (PATTERNS §Plugin Dual Declaration): the lowdefy.yaml registration of `shifty-audit-writer` happens in Plan 02 (when src/*.js exists) — do NOT add it to `lowdefy.yaml` in this task or the Lowdefy build will fail with "module not found".
  </action>
  <verify>
    <automated>node tools/check-queries.mjs (exit 0); node --test tools/test/invite-code.test.mjs (all 5 tests pass); test -f app/pnpm-workspace.yaml && test -f playwright.config.ts && test -f tests/e2e/_fixtures/seed-tenants.ts && test -f tests/e2e/_fixtures/teardown.ts && test -f tools/fixtures/kibbutz.sql && test -f app/plugins/shifty-audit-writer/package.json && test -f .env.example && test -f package.json; SEC-08 gitignore assertion: test "$(git ls-files | grep -E "^.env$" | wc -l)" = "0" AND grep -q "^.env$" .gitignore (both must succeed — proves .env is not tracked and is listed in .gitignore)</automated>
  </verify>
  <acceptance_criteria>
    - All 15 files created at the exact paths listed in `<files>`
    - `node tools/check-queries.mjs` exits 0 (after the bootstrap employees query is allowlisted)
    - `node --test tools/test/invite-code.test.mjs` reports 5 passing tests
    - `.env.example` contains RESEND_API_KEY and RESEND_FROM_EMAIL with comment sources
    - `app/plugins/shifty-audit-writer/package.json` has `"type": "module"` and the two `./connections` + `./types` exports
    - `tools/fixtures/kibbutz.sql` has exactly 12 INSERT INTO soldier rows; row 12 contains U+2019 (verify with `grep -c $'’' tools/fixtures/kibbutz.sql` returns ≥1)
    - `tests/e2e/_fixtures/seed-tenants.ts` imports from `pg` package (not Lowdefy) and exports three named functions
  </acceptance_criteria>
  <done>Every Wave 0 file required by 01-VALIDATION.md exists and passes its scaffold-level verify; check-queries.mjs is functional (though further hardened in Plan 04); invite-code unit test exercises Crockford regex.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add `migrate` compose service + RESEND env wiring + commented stubs</name>
  <read_first>
    - docker-compose.yml (full file, already in context — current 2-service shape)
    - .planning/phases/01-foundations/01-RESEARCH.md §Pattern 2 (golang-migrate compose service, lines 309-347)
    - .planning/phases/01-foundations/01-PATTERNS.md §`docker-compose.yml` (extensions detail)
    - .planning/phases/01-foundations/01-CONTEXT.md §D-04, §D-09
    - CLAUDE.md §Common ops on hpg5 (PsExec wrapping for `docker compose build`)
  </read_first>
  <files>
    docker-compose.yml
  </files>
  <action>
    Edit `docker-compose.yml` additively. Do not change the existing `lowdefy` or `postgres` service definitions except for adding two new env vars to the `lowdefy` service.

    Changes required (per RESEARCH Pattern 2 + PATTERNS §`docker-compose.yml`):

    a) **Add RESEND env vars to `lowdefy.environment`** (extend the existing block; preserve POSTGRES_CONNECTION_STRING, NEXTAUTH_SECRET, NEXTAUTH_URL):
       ```yaml
       RESEND_API_KEY: ${RESEND_API_KEY:?missing}
       RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL:-shifty@nesher.co}
       ```
       Place these AFTER the NEXTAUTH_URL line. Note: `${RESEND_API_KEY:?missing}` will block `docker compose up` until the user provisions the key on hpg5 — this is intentional. Document in a YAML comment above the block: `# RESEND_API_KEY is required by Plan 03 NextAuth EmailProvider. Provision in hpg5 .env before running Plan 03.`

    b) **Add new `migrate` one-shot service** between `lowdefy` and `postgres` (alphabetical-ish ordering for readability — actual order is irrelevant to compose). Exact YAML (per Pattern 2):
       ```yaml
         migrate:
           image: migrate/migrate:v4.18.3
           container_name: shifty-migrate
           depends_on:
             postgres:
               condition: service_healthy
           volumes:
             - ./db/migrations:/migrations:ro
           command:
             - "-path=/migrations"
             - "-database=postgres://${POSTGRES_USER:-shifts}:${POSTGRES_PASSWORD:?missing}@postgres:5432/${POSTGRES_DB:-shifts}?sslmode=disable"
             - "up"
           restart: "no"
       ```
       NOTE per Pattern 2 (line 345): do NOT add `migrate` to `lowdefy.depends_on`. Migrate is run manually via `docker compose run --rm migrate up` before each deploy. This keeps `docker compose up -d` fast on simple restarts.

    c) **Add commented stubs for `solver`, `cron`, `waha`** at the end of `services:` so OPS-01 verify (`docker compose config | grep -E 'lowdefy|postgres|migrate'` succeeds, plus presence of `# solver`, `# cron`, `# waha` comments documents the slots). Use this exact format:
       ```yaml
         # solver:  # Phase 4 — FastAPI CP-SAT solver service; internal-only port; auth via Bearer SOLVER_SHARED_SECRET
         #   build: ./solver
         #   environment:
         #     SOLVER_SHARED_SECRET: ${SOLVER_SHARED_SECRET:?missing}
         #   restart: unless-stopped

         # cron:    # Phase 6 — notification cron (daily/weekly digests, lock-reminder, window-archiver)
         #   build: ./cron
         #   depends_on:
         #     postgres:
         #       condition: service_healthy
         #   restart: unless-stopped

         # waha:    # Phase 6 — WAHA WhatsApp HTTP API; Tailscale-bound UI port; dedicated SIM required
         #   image: devlikeapro/waha-plus:latest
         #   ports:
         #     - "127.0.0.1:3001:3000"   # bind localhost so Tailscale-only access remains true
         #   restart: unless-stopped
       ```

    Deploy step (after committing the file change on the dev machine, push to git, then on hpg5):
       1. `pscp` the updated `docker-compose.yml` to `C:\shifts-manager\docker-compose.yml` on hpg5.
       2. Add `RESEND_API_KEY=<placeholder>` and `RESEND_FROM_EMAIL=shifty@nesher.co` to hpg5 `C:\shifts-manager\.env` (user setup — see user_setup frontmatter; placeholder is OK until Plan 03 actually exercises Resend). If `RESEND_API_KEY` is genuinely missing, set it to a sentinel `re_placeholder_provision_in_plan_03` to satisfy `${VAR:?missing}` for compose validation. Plan 03 will require the real key.
       3. Run `plink ... hpg5 "powershell -c \"cd C:\shifts-manager; docker compose config | findstr migrate\""` — must include the new `migrate` service.
       4. Validate idempotency: `plink ... hpg5 "powershell -c \"cd C:\shifts-manager; docker compose run --rm migrate up\""` (first run applies 0001; existing schema means it's already at version 1 — migrate exits 0 cleanly).
       5. Run again: same command — `migrate` should exit 0 (idempotent re-run, "no change").

    Failure mode: if `RESEND_API_KEY` is not set on hpg5, `docker compose config` will fail with `"missing"`. Recovery: set the placeholder as described in step 2 above. Surface a checkpoint if this happens.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml config (locally, exit 0 if RESEND_API_KEY is set in shell env or passed as ENV) AND on hpg5: docker compose config | findstr migrate (must show the service) AND docker compose run --rm migrate up returns exit 0 on second invocation (idempotency proof)</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` defines services `lowdefy`, `postgres`, `migrate` (and three commented stubs `# solver`, `# cron`, `# waha`)
    - `migrate` service uses `migrate/migrate:v4.18.3`, mounts `./db/migrations:/migrations:ro`, depends on postgres.service_healthy, has `restart: "no"`
    - `lowdefy.environment` includes `RESEND_API_KEY` and `RESEND_FROM_EMAIL` (the former required, the latter defaulting to shifty@nesher.co)
    - `migrate` is NOT in `lowdefy.depends_on` (intentional per Pattern 2)
    - On hpg5: `docker compose run --rm migrate up` exits 0 on a second consecutive invocation (idempotency)
  </acceptance_criteria>
  <done>Migrate compose service operational on hpg5; idempotent re-runs verified; RESEND env vars wired (placeholder acceptable until Plan 03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author migration 0002 — tenancy + NextAuth adapter tables + Hebrew collation + composite indexes</name>
  <read_first>
    - db/migrations/0001_init.sql (full file — envelope, UUID pattern, trigger function, index pattern)
    - .planning/phases/01-foundations/01-RESEARCH.md §Pattern 3 (Migration 0002, lines 351-420), §Pattern 14 (COLLATE columns, lines 1125-1145), §Pitfall 5 (ICU collation install, lines 1264-1269)
    - .planning/phases/01-foundations/01-PATTERNS.md §`db/migrations/0002_tenancy_and_org.sql` (full pattern + adaptations)
    - .planning/phases/01-foundations/01-CONTEXT.md §D-05 (NextAuth tables placement decision), §D-07 (RLS implications — NOT enabled yet, but tables must support tenant_id)
    - docs/PRD.md §7.1 (tenant + org_unit columns), §7.2 (auth user + membership), §7.3 (soldier columns), §10 (data model — full schema)
    - tests/e2e/_fixtures/seed-tenants.ts (created in Task 2 — verify the table/column names align)
  </read_first>
  <files>
    db/migrations/0002_tenancy_and_org.sql
  </files>
  <action>
    Create `db/migrations/0002_tenancy_and_org.sql` per PATTERNS §`db/migrations/0002_tenancy_and_org.sql` and RESEARCH Pattern 3. Use the `<interfaces>` section in this plan's `<context>` block as the authoritative column list — execute each column EXACTLY as specified (NOT NULL flags, FK ON DELETE, CHECK constraints, COLLATE, default values, UNIQUE constraints).

    File structure (use this exact ordering of statements within a single BEGIN/COMMIT envelope):

    1. **Header comment** (5 lines):
       ```
       -- 0002_tenancy_and_org.sql -- multi-tenant foundations + NextAuth KnexAdapter schema
       -- Introduces tenant scoping; superseded the single-tenant 0001 in Phase 2 via 0008 (deferred).
       -- Includes NextAuth KnexAdapter tables (users/accounts/sessions/verification_tokens) per D-05.
       -- Hebrew text columns use COLLATE "he-x-icu" per I18N-07.
       -- Composite (tenant_id, ...) indexes per PERF-04.
       ```

    2. **`BEGIN;`**

    3. **Extensions** — `CREATE EXTENSION IF NOT EXISTS citext;` (RESEARCH Pattern 3 line 364; needed for case-insensitive email on `app_user.email`). pgcrypto already enabled by 0001.

    4. **Collation** — per RESEARCH Pitfall 5: `CREATE COLLATION IF NOT EXISTS "he-x-icu" (PROVIDER = icu, LOCALE = 'he');`. The pg_collation may already have it via the postgres:16 image (per Pitfall 5 the default uses libc; ICU may or may not be available). The `IF NOT EXISTS` makes the statement idempotent.

    5. **Shifty domain tables (in FK order)**:
       a. `tenant` (id PK, name TEXT COLLATE "he-x-icu" NOT NULL, org_depth SMALLINT NOT NULL CHECK (org_depth BETWEEN 1 AND 3), created_at, updated_at)
       b. `org_unit` (per `<interfaces>` block) — composite index `CREATE INDEX idx_org_unit_tenant ON org_unit(tenant_id);` AND `CREATE INDEX idx_org_unit_tenant_parent ON org_unit(tenant_id, parent_id);`
       c. `users` (NextAuth KnexAdapter table — see `<interfaces>` for exact column names; quoted identifiers like `"emailVerified"`)
       d. `app_user` (per `<interfaces>`; index on `(tenant_id, email)` UNIQUE; index on `user_id` for the Auth.js FK)
       e. `soldier` (per `<interfaces>`; composite indexes `CREATE INDEX idx_soldier_tenant ON soldier(tenant_id);` AND `CREATE INDEX idx_soldier_tenant_status ON soldier(tenant_id, status) WHERE status='active';`)
       f. `membership` (per `<interfaces>`; composite indexes `CREATE INDEX idx_membership_tenant ON membership(tenant_id);` AND `CREATE INDEX idx_membership_soldier ON membership(soldier_id);` AND `CREATE INDEX idx_membership_org_unit ON membership(org_unit_id);`)
       g. **NextAuth KnexAdapter remaining tables** (per `<interfaces>`): `accounts`, `sessions`, `verification_tokens`. The quoted identifiers (`"userId"`, `"sessionToken"`, `"providerAccountId"`, `"emailVerified"`) MUST match Auth.js convention exactly — otherwise the KnexAdapter throws "column does not exist".

    6. **`updated_at` triggers** — Apply the existing `set_updated_at()` function (defined in 0001) to: `tenant`, `org_unit`, `app_user`, `soldier`. Pattern:
       ```sql
       CREATE TRIGGER trg_tenant_updated_at BEFORE UPDATE ON tenant FOR EACH ROW EXECUTE FUNCTION set_updated_at();
       ```
       Do NOT redefine `set_updated_at()` — it already exists from 0001.

    7. **`COMMIT;`**

    Apply via `migrate` service on hpg5:
       - Push the new file to `C:\shifts-manager\db\migrations\0002_tenancy_and_org.sql` via pscp.
       - Run: `plink ... hpg5 "powershell -c \"cd C:\shifts-manager; docker compose run --rm migrate up\""` — expect exit 0 with output `1/u tenancy_and_org`.
       - Verify idempotency: same command again — expect exit 0 with "no change" message.
       - Verify schema: `plink ... hpg5 "powershell -c \"docker exec shifts-postgres psql -U shifts -d shifts -c '\d tenant'\""` — expect tenant table columns visible.

    NOTE on RLS bypass during DML migrations: 0002 is DDL-only — no INSERTs. RLS isn't enabled until 0009 (Plan 03). The kibbutz fixture INSERT runs from `tools/fixtures/kibbutz.sql` AFTER all migrations land (Plan 03 + manually); it sets `SET app.current_tenant = ...` itself if needed. Per RESEARCH Pitfall 2: DDL bypasses RLS inherently, so 0002 needs no preamble. Document this in a comment in the file: `-- No RLS preamble needed — this migration is DDL-only.`

    UNRESOLVED in this task (defer to Plan 03 — DO NOT add now): 
    - Sign-up CTE redemption query (lives in app/pages/auth/signup_with_invite.yaml in Plan 03)
    - app_user → users FK direction note: PATTERNS says "app_user.user_id FK → this table's id is added in a subsequent statement". For Plan 01, define `app_user.user_id UUID NULL REFERENCES users(id) ON DELETE SET NULL` directly inline — no need to defer. Both tables exist within the same transaction.

    Schema validation note: the `seed-tenants.ts` fixture from Task 2 inserts into `tenant`, `org_unit`, `app_user`, `soldier`, `membership`. If the column names in 0002 disagree with what Task 2's fixture inserts, fix the fixture in Task 2 (not 0002) — the migration is the source of truth.
  </action>
  <verify>
    <automated>plink ... hpg5 "powershell -c \"docker compose run --rm migrate up\"" exits 0; second invocation exits 0; psql -c '\d tenant' shows org_depth column with type smallint; psql -c "SELECT collname FROM pg_collation WHERE collname='he-x-icu'" returns 1 row; psql -c '\d app_user' shows email column type citext; psql -c '\d sessions' shows columns "sessionToken", "userId" (quoted/case-sensitive)</automated>
  </verify>
  <acceptance_criteria>
    - File `db/migrations/0002_tenancy_and_org.sql` exists with BEGIN/COMMIT envelope
    - `CREATE EXTENSION IF NOT EXISTS citext` and `CREATE COLLATION IF NOT EXISTS "he-x-icu"` present
    - Tables created: `tenant`, `org_unit`, `users`, `app_user`, `soldier`, `membership`, `accounts`, `sessions`, `verification_tokens`
    - `tenant.name`, `org_unit.name`, `app_user.display_name`, `soldier.display_name` all declared `COLLATE "he-x-icu"`
    - Composite `(tenant_id, ...)` indexes on `org_unit`, `app_user`, `soldier`, `membership` per PERF-04
    - NextAuth tables use quoted identifiers exactly: `"emailVerified"`, `"userId"`, `"sessionToken"`, `"providerAccountId"`
    - On hpg5: `docker compose run --rm migrate up` exits 0; second consecutive run also exits 0 (idempotent per OPS-02)
    - `\d tenant`, `\d org_unit`, `\d app_user`, `\d soldier`, `\d membership`, `\d users`, `\d accounts`, `\d sessions`, `\d verification_tokens` all return column listings
  </acceptance_criteria>
  <done>Migration 0002 applied on hpg5; tenancy + NextAuth schemas in place; Hebrew collation works; idempotency proven; bootstrap `/employees` page still renders (unaffected — legacy tables intact per D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH (developer machine → hpg5) | Untrusted input from dev typing commands; mitigated by SSH keys/passwords + hostkey pinning |
| Docker host → `migrate` container | Migrate runs with DB superuser-equivalent privileges (the `shifts` role); mitigated by the role not being SUPERUSER, and migration files being read-only mount |
| `.env` file on hpg5 | Contains all secrets; SEC-08 requires never committed; mitigated by `.env.example` template separation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | `.env` file commit risk | mitigate | Confirm `.env` is in `.gitignore`; `.env.example` ships as template only (no real keys); SEC-08 verification — `git ls-files | grep -E '\.env$'` must return empty |
| T-01-02 | T (Tampering) | `migrate` service running with mutable migration files | mitigate | Volume mount is `:ro` (read-only); migrations are git-tracked SQL files; CI grep gate (Plan 04) prevents query-level tampering |
| T-01-03 | E (Elevation of Privilege) | `shifts` DB role privileges | mitigate | Confirm role is NOT SUPERUSER (RESEARCH Pitfall 6 / Anti-Pattern) — `SELECT rolsuper FROM pg_roles WHERE rolname='shifts'` must return false; this is required before Plan 03's RLS works |
| T-01-04 | D (Denial of Service) | `migrate` runs at compose-up time on every deploy | accept | Migrations are idempotent + tracked in `schema_migrations`; re-runs are fast; degraded boot acceptable for solo-dev v1 |
| T-01-05 | I (Information Disclosure) | RESEND_API_KEY placeholder leaking to logs | mitigate | Plan 05 ships log-redaction middleware; in Plan 01 the key is a placeholder anyway (`re_placeholder_provision_in_plan_03`) so impact is null |
| T-01-06 | T (Tampering) | Smoke-test page (`/employees`) exposes legacy schema publicly through CF Tunnel until Plan 03 wires auth | accept | No production data exists (legacy tables empty per RESEARCH Runtime State Inventory); risk window closes when Plan 03 lands; SEC-08 still satisfied because secrets are not in the schema |
</threat_model>

<verification>
Run on hpg5 after all 4 tasks complete:

1. `docker compose run --rm migrate up` exits 0 with output indicating versions 1 and 2 applied.
2. Re-run same command — exits 0 with "no change" (idempotency).
3. `curl http://hpg5:8080/employees` still returns HTTP 200 (smoke test still passes — D-06 preserved).
4. `docker exec shifts-postgres psql -U shifts -d shifts -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public'"` — count includes tenant, org_unit, app_user, soldier, membership, users, accounts, sessions, verification_tokens (9 new tables) + the 5 legacy tables from 0001 = 14 total.
5. Locally: `node tools/check-queries.mjs` exits 0; `node --test tools/test/invite-code.test.mjs` reports 5 passing tests.
</verification>

<success_criteria>
- D-01 smoke test PASS recorded; b8afba1 fix verified live on hpg5
- Wave 0 files committed: `app/pnpm-workspace.yaml`, `.env.example`, `package.json` (root), `playwright.config.ts`, `tests/e2e/_fixtures/{seed-tenants,teardown}.ts`, `tools/check-queries.mjs`, `tools/fixtures/kibbutz.sql`, `tools/test/invite-code.test.mjs`, `app/plugins/shifty-audit-writer/package.json`
- `migrate` compose service added with `migrate/migrate:v4.18.3`; idempotent re-run verified on hpg5
- Migration 0002 applied: 9 new tables, Hebrew collation, composite indexes, NextAuth quoted-identifier schema
- RESEND env vars wired in compose; placeholder OK until Plan 03 actually consumes them
- Bootstrap `/employees` smoke page still returns HTTP 200 (D-06 — must remain alive through Phase 1)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-foundations-01-SUMMARY.md` documenting:
- D-01 smoke test result (PASS/FAIL + evidence)
- Wave 0 files created with paths
- Migration 0002 schema snapshot (output of `\dt`)
- `migrate` service idempotency log (two consecutive `docker compose run --rm migrate up` invocations)
- Any deviations from PATTERNS/RESEARCH, with rationale
</output>
</content>
</invoke>
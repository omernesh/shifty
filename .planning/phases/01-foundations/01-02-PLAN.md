---
phase: 01-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN
files_modified:
  - db/migrations/0003_shifts_and_windows.sql
  - db/migrations/0004_availability_rules_swaps.sql
  - db/migrations/0005_auth_and_notifications.sql
  - db/migrations/0006_audit_and_solver_runs.sql
  - db/migrations/0007_imports_and_exports.sql
  - app/plugins/shifty-audit-writer/src/types.js
  - app/plugins/shifty-audit-writer/src/connections.js
  - app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js
  - app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs
  - app/lowdefy.yaml
autonomous: true
requirements:
  - I18N-07
  - PERF-04
  - OPS-02
must_haves:
  truths:
    - "Migrations 0003-0007 apply via `docker compose run --rm migrate up`; re-runs idempotent"
    - "All domain tables across 0003-0007 have `tenant_id UUID NOT NULL REFERENCES tenant(id) ON DELETE CASCADE`"
    - "All Hebrew text columns (`shift_slot.name`) use COLLATE \"he-x-icu\""
    - "Composite `(tenant_id, ...)` indexes present on every tenant-scoped table per PERF-04"
    - "Tables for Plan 03 RLS (0009) and audit REVOKEs (0010) exist: `schedule_audit`, `roster_import_log`, `notification_log`, `solver_run`, `invite_code`, `invite_code_redemption`, `notification_pref`, `push_subscription`, `report_recipient`, `ical_subscription_token`, `shift_slot`, `planning_window`, `shift_instance`, `assignment`, `availability`, `rule`, `rule_override`, `swap_request`"
    - "Audit-writer plugin loads (Lowdefy build accepts the workspace:* reference and registers AuditWrite as a request type)"
    - "Bootstrap /employees page still returns HTTP 200 after these migrations apply (D-06 preserved)"
  artifacts:
    - path: "db/migrations/0003_shifts_and_windows.sql"
      provides: "shift_slot + planning_window + shift_instance tables with tenant scoping, Hebrew collation on shift_slot.name, midnight-spanning time check on shift_slot"
      contains: "shift_slot"
    - path: "db/migrations/0004_availability_rules_swaps.sql"
      provides: "availability + assignment + rule + rule_override + swap_request tables with tenant scoping"
      contains: "availability"
    - path: "db/migrations/0005_auth_and_notifications.sql"
      provides: "invite_code + invite_code_redemption + notification_pref + push_subscription + report_recipient + ical_subscription_token (used in Phase 7) tables"
      contains: "invite_code"
    - path: "db/migrations/0006_audit_and_solver_runs.sql"
      provides: "schedule_audit + solver_run + notification_log tables (target tables for audit-writer plugin and 0010 REVOKEs)"
      contains: "schedule_audit"
    - path: "db/migrations/0007_imports_and_exports.sql"
      provides: "roster_import_log table (Phase 2 consumer)"
      contains: "roster_import_log"
    - path: "app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js"
      provides: "Request handler that INSERTs into schedule_audit with actor_user_id from session (NEVER from input)"
      exports: ["default AuditWrite"]
    - path: "app/plugins/shifty-audit-writer/src/types.js"
      provides: "Plugin type declarations — exports { requests: ['AuditWrite'] }"
      exports: ["default { requests }"]
    - path: "app/plugins/shifty-audit-writer/src/connections.js"
      provides: "Aggregator — imports AuditWrite and exports { AuditWrite }"
      exports: ["default { AuditWrite }"]
    - path: "app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs"
      provides: "Unit test asserting AuditWrite hard-fails when actor_user_id is missing from request.user"
      contains: "throw"
    - path: "app/lowdefy.yaml"
      provides: "Adds shifty-audit-writer plugin to the plugins: list (workspace:*)"
      contains: "shifty-audit-writer"
  key_links:
    - from: "0004 availability table"
      to: "0003 shift_instance table"
      via: "availability.shift_instance_id UUID FK ON DELETE CASCADE"
      pattern: "REFERENCES shift_instance"
    - from: "0004 swap_request table"
      to: "0004 assignment table"
      via: "swap_request.from_assignment_id + swap_request.to_assignment_id FKs"
      pattern: "REFERENCES assignment"
    - from: "audit-writer plugin AuditWrite.js"
      to: "0006 schedule_audit table"
      via: "knex('schedule_audit').insert({...}); actor_user_id from request.user, never from request.properties"
      pattern: "schedule_audit"
    - from: "app/lowdefy.yaml plugins: list"
      to: "app/plugins/shifty-audit-writer/package.json"
      via: "workspace:* reference resolved by app/pnpm-workspace.yaml (Plan 01)"
      pattern: "shifty-audit-writer"
---

<objective>
Build the rest of the Phase 1 schema (migrations 0003 through 0007) and ship the first custom Lowdefy request plugin (`shifty-audit-writer`) end-to-end so Plan 03's RLS migration (0009) and audit REVOKEs (0010) have tables to act on, and so Plan 03 can wire the plugin into a smoke-test page.

Purpose: D-08 designates the audit-writer as the canary that proves Lowdefy's plugin system can host a Knex-writing request handler. Validating it now de-risks the dispatcher (Phase 6), webhook receivers (Phase 6), and signed-URL endpoints (Phase 7). The five migrations 0003-0007 deliver every Phase 1 domain table the auth flow, RLS layer, and audit append-only enforcement depend on.

Output: Five new SQL migrations applied (each idempotent); audit-writer plugin loadable in the Lowdefy build (workspace:* reference resolves); plugin declared in `lowdefy.yaml` so Plan 03 can attach an `AuditWrite` request to its admin smoke-test page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md
@.planning/phases/01-foundations/01-VALIDATION.md
@docs/PRD.md
@.claude/skills/lowdefy/SKILL.md
@.claude/skills/lowdefy/reference/09-plugins.md
@.claude/skills/lowdefy/reference/06-operators.md
@db/migrations/0001_init.sql
@db/migrations/0002_tenancy_and_org.sql
@app/lowdefy.yaml
@app/pnpm-workspace.yaml
@app/plugins/shifty-audit-writer/package.json

<interfaces>
<!-- From Plan 01: every domain migration in this plan adds tenant_id FK pointing at this -->

From `db/migrations/0002_tenancy_and_org.sql` (committed in Plan 01):
- `tenant(id UUID PK, name TEXT COLLATE "he-x-icu", org_depth SMALLINT, ...)`
- `org_unit(id UUID PK, tenant_id UUID FK, parent_id UUID FK self, level SMALLINT, name TEXT COLLATE "he-x-icu", ...)`
- `app_user(id UUID PK, tenant_id UUID FK, email CITEXT, display_name TEXT COLLATE "he-x-icu", locale TEXT, user_id UUID FK→users, ...)`
- `soldier(id UUID PK, tenant_id UUID FK, user_id UUID FK→app_user, display_name TEXT COLLATE "he-x-icu", color TEXT, seniority SMALLINT, role_tags TEXT[], phone_e164 TEXT, status TEXT, notes TEXT, ...)`
- `membership(id UUID PK, tenant_id UUID FK, soldier_id UUID FK, org_unit_id UUID FK, role TEXT, ...)`
- NextAuth tables: `users`, `accounts`, `sessions`, `verification_tokens`
- Trigger function `set_updated_at()` available for reuse (defined in 0001)

PRD §10 column specs to honor for each new table — read docs/PRD.md §10 for canonical column lists; the interfaces below are the minimum Phase 1 RBAC + audit + RLS path needs. If PRD specifies additional columns (e.g., `display_order`, `metadata`), include them.

**0003 — shifts/windows:**
- `shift_slot(id UUID PK, tenant_id FK, team_id UUID FK→org_unit, name TEXT COLLATE "he-x-icu" NOT NULL, start_time TIME NOT NULL, end_time TIME NOT NULL, headcount SMALLINT NOT NULL DEFAULT 1 CHECK (headcount >= 1), required_role_tags TEXT[] NOT NULL DEFAULT '{}', min_seniority SMALLINT, display_order SMALLINT NOT NULL DEFAULT 0, created_at, updated_at)`
  - Note: NO CHECK on (end_time > start_time) — slots may cross midnight (SHFT-01)
- `planning_window(id UUID PK, tenant_id FK, team_id UUID FK→org_unit, start_date DATE NOT NULL, end_date DATE NOT NULL CHECK (end_date >= start_date), constraint_lock_at TIMESTAMPTZ NOT NULL, state TEXT NOT NULL DEFAULT 'open' CHECK (state IN ('open','draft','published','closed')), created_at, updated_at)`
- `shift_instance(id UUID PK, tenant_id FK, shift_slot_id UUID FK ON DELETE CASCADE, planning_window_id UUID FK ON DELETE CASCADE, date DATE NOT NULL, headcount_index SMALLINT NOT NULL DEFAULT 0, UNIQUE(shift_slot_id, date, headcount_index))`

**0004 — availability/rules/swaps:**
- `availability(id UUID PK, tenant_id FK, soldier_id UUID FK ON DELETE CASCADE, shift_instance_id UUID FK ON DELETE CASCADE, declared TEXT NOT NULL CHECK (declared IN ('available','unavailable')), source TEXT NOT NULL CHECK (source IN ('default','range_blockout','per_slot','manager_override')), created_at, updated_at, UNIQUE(soldier_id, shift_instance_id))`
- `assignment(id UUID PK, tenant_id FK, shift_instance_id UUID FK ON DELETE CASCADE, soldier_id UUID FK ON DELETE RESTRICT, state TEXT NOT NULL DEFAULT 'draft' CHECK (state IN ('draft','published')), created_at, updated_at, UNIQUE(shift_instance_id, soldier_id))`
- `rule(id UUID PK, tenant_id FK, team_id UUID FK→org_unit, rule_key TEXT NOT NULL CHECK (rule_key IN ('no_same_day_double','no_consecutive_shift2_then_shift1','max_consecutive_nights','weekend_separation','max_weekly_hours','min_rest_hours_between_shifts','max_shifts_per_period','fairness_objective')), enabled BOOLEAN NOT NULL DEFAULT true, value JSONB, created_at, updated_at, UNIQUE(tenant_id, team_id, rule_key))`
- `rule_override(id UUID PK, tenant_id FK, rule_id UUID FK ON DELETE CASCADE, soldier_id UUID FK ON DELETE CASCADE, value JSONB NOT NULL, created_at, updated_at, UNIQUE(rule_id, soldier_id))`
- `swap_request(id UUID PK, tenant_id FK, from_assignment_id UUID FK→assignment, to_assignment_id UUID FK→assignment, initiator_soldier_id UUID FK→soldier, counterparty_soldier_id UUID FK→soldier, state TEXT NOT NULL DEFAULT 'proposed' CHECK (state IN ('proposed','awaiting_mgr','pending_manager','approved','rejected','declined','withdrawn')), state_history JSONB NOT NULL DEFAULT '[]', created_at, updated_at)`

**0005 — auth/notifications:**
- `invite_code(id UUID PK, tenant_id FK, code TEXT NOT NULL CHECK (code ~ '^[0-9A-HJKMNPQRSTVWXYZ]{8}$'), org_unit_id UUID FK→org_unit, role TEXT NOT NULL CHECK (role IN ('unit_admin','team_manager','member','viewer')), expires_at TIMESTAMPTZ, max_uses INTEGER, uses INTEGER NOT NULL DEFAULT 0 CHECK (uses >= 0), revoked_at TIMESTAMPTZ, created_by UUID FK→app_user(id) ON DELETE SET NULL, created_at, UNIQUE(tenant_id, code))`
- `invite_code_redemption(id UUID PK, tenant_id FK, invite_code_id UUID FK→invite_code ON DELETE RESTRICT, user_id UUID FK→app_user(id) ON DELETE CASCADE, redeemed_at TIMESTAMPTZ NOT NULL DEFAULT now())`
- `notification_pref(id UUID PK, tenant_id FK, user_id UUID FK→app_user(id) ON DELETE CASCADE, event_type TEXT NOT NULL, channels JSONB NOT NULL DEFAULT '[]', created_at, updated_at, UNIQUE(user_id, event_type))`
- `push_subscription(id UUID PK, tenant_id FK, user_id UUID FK→app_user(id) ON DELETE CASCADE, endpoint TEXT NOT NULL, keys JSONB NOT NULL, created_at)` — Phase 6 actually exercises this; Phase 1 just provisions
- `report_recipient(id UUID PK, tenant_id FK, email CITEXT NOT NULL, display_name TEXT COLLATE "he-x-icu", locale TEXT NOT NULL DEFAULT 'he' CHECK (locale IN ('he','en')), subscriptions JSONB NOT NULL DEFAULT '{}', created_at, updated_at)` — Phase 6 consumer
- `ical_subscription_token(id UUID PK, tenant_id FK, soldier_id UUID FK→soldier ON DELETE CASCADE, token TEXT NOT NULL UNIQUE, created_at, revoked_at TIMESTAMPTZ)` — Phase 7 consumer; provisioned now for RLS coverage

**0006 — audit/solver:**
- `schedule_audit(id UUID PK, tenant_id FK, planning_window_id UUID FK→planning_window(id) ON DELETE SET NULL, from_state TEXT, to_state TEXT NOT NULL, actor_user_id UUID FK→app_user(id) ON DELETE SET NULL, actor_kind TEXT NOT NULL CHECK (actor_kind IN ('user','system','solver')), payload JSONB, created_at TIMESTAMPTZ NOT NULL DEFAULT now())` — NOTE: NO `updated_at` column; this is append-only (REVOKEs land in 0010)
- `solver_run(id UUID PK, tenant_id FK, planning_window_id UUID FK→planning_window ON DELETE CASCADE, status TEXT NOT NULL CHECK (status IN ('optimal','feasible','infeasible','error','timeout')), solve_time_seconds NUMERIC(10,3), request_payload JSONB NOT NULL, response_payload JSONB, created_at)`
- `notification_log(id UUID PK, tenant_id FK, user_id UUID FK→app_user(id) ON DELETE SET NULL, event_type TEXT NOT NULL, channel TEXT NOT NULL CHECK (channel IN ('email','whatsapp','push','in_app')), status TEXT NOT NULL DEFAULT 'queued' CHECK (status IN ('queued','sent','delivered','failed','bounced')), provider_response JSONB, sent_at TIMESTAMPTZ, delivered_at TIMESTAMPTZ, created_at, updated_at)` — DELETE/TRUNCATE revoked in 0010 but UPDATE allowed (status transitions)

**0007 — imports/exports:**
- `roster_import_log(id UUID PK, tenant_id FK, imported_by UUID FK→app_user(id) ON DELETE SET NULL, source TEXT NOT NULL, rows_created INTEGER NOT NULL DEFAULT 0, rows_skipped INTEGER NOT NULL DEFAULT 0, rows_errored INTEGER NOT NULL DEFAULT 0, error_details JSONB, created_at)` — Phase 2 consumer; append-only — REVOKEs in 0010

Standard pattern for EVERY table in 0003-0007:
- `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
- `tenant_id UUID NOT NULL REFERENCES tenant(id) ON DELETE CASCADE`
- `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
- `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` (omit on append-only tables: `schedule_audit`, `roster_import_log`, `invite_code_redemption`)
- Composite index `idx_<table>_tenant ON <table>(tenant_id)` minimum
- Hot-query composite (`tenant_id, <lookup>`) where the table is referenced by a frequent join (e.g., `assignment(tenant_id, state)`, `availability(tenant_id, soldier_id)`)
- `set_updated_at()` BEFORE UPDATE trigger on every table that has `updated_at`

From `.claude/skills/lowdefy/reference/09-plugins.md` (read it):
- Plugin export shape: `package.json` `"type": "module"` + `"exports": { "./connections": "./src/connections.js", "./types": "./src/types.js" }`
- `src/types.js` exports default `{ requests: ['<TypeName>'] }` — TypeName MUST match the YAML `type:` field
- `src/connections.js` exports default `{ <TypeName>: <handler> }`
- Request handler shape: `async function Handler({ request, connection })` — `request.user` is session-injected by Lowdefy server; `request.properties` is the YAML-resolved payload
- Handler must declare `Handler.schema` (JSON Schema for request.properties) and `Handler.connectionType = 'Knex'`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrations 0003 + 0004 + 0005 — shifts/windows/availability/rules/swaps/auth-notif</name>
  <read_first>
    - db/migrations/0001_init.sql (envelope + index pattern, already in context)
    - db/migrations/0002_tenancy_and_org.sql (FK target shape — tenant.id, org_unit.id, app_user.id, soldier.id — already created in Plan 01)
    - .planning/phases/01-foundations/01-PATTERNS.md §`db/migrations/0003_shifts_and_windows.sql through 0007_imports_and_exports.sql` (FK pattern, CHECK pattern, composite-index pattern, RLS-preamble note)
    - .planning/phases/01-foundations/01-RESEARCH.md §Pattern 14 (COLLATE columns; shift_slot.name needs it)
    - docs/PRD.md §7.4 (shift_slot/planning_window), §7.5 (availability), §7.6 (rules), §7.7 (planning_window state machine), §7.10 (swap_request), §7.2 (invite_code), §7.11 (notification_log/notification_pref), §10 (data model — canonical column lists)
  </read_first>
  <files>
    db/migrations/0003_shifts_and_windows.sql
    db/migrations/0004_availability_rules_swaps.sql
    db/migrations/0005_auth_and_notifications.sql
  </files>
  <action>
    Author migrations 0003, 0004, 0005 following the patterns in 0001_init.sql + 0002_tenancy_and_org.sql exactly. Use the `<interfaces>` section above as the authoritative column inventory. Each migration uses one BEGIN/COMMIT envelope.

    **`db/migrations/0003_shifts_and_windows.sql`**:
    Header comment: `-- 0003_shifts_and_windows.sql -- shift slot templates, planning windows, and the shift_instance cross-product`. 
    Tables: `shift_slot`, `planning_window`, `shift_instance` per `<interfaces>`.
    Indexes:
    - `idx_shift_slot_tenant ON shift_slot(tenant_id)`
    - `idx_shift_slot_team ON shift_slot(team_id)`
    - `idx_planning_window_tenant ON planning_window(tenant_id)`
    - `idx_planning_window_tenant_state ON planning_window(tenant_id, state)`
    - `idx_shift_instance_tenant ON shift_instance(tenant_id)`
    - `idx_shift_instance_window ON shift_instance(planning_window_id)`
    - `idx_shift_instance_date ON shift_instance(date)`
    Triggers: `set_updated_at()` BEFORE UPDATE on `shift_slot`, `planning_window`. (`shift_instance` has no updated_at — it's a generated cross-product, immutable after creation.)
    Add to `shift_slot.name`: `COLLATE "he-x-icu" NOT NULL`.

    **`db/migrations/0004_availability_rules_swaps.sql`**:
    Header: `-- 0004_availability_rules_swaps.sql -- soldier availability declarations, rule overrides, and 1-for-1 swap requests`.
    Tables: `availability`, `assignment`, `rule`, `rule_override`, `swap_request` per `<interfaces>`.
    Indexes:
    - `idx_availability_tenant ON availability(tenant_id)`
    - `idx_availability_soldier ON availability(soldier_id)`
    - `idx_availability_instance ON availability(shift_instance_id)`
    - `idx_assignment_tenant ON assignment(tenant_id)`
    - `idx_assignment_tenant_state ON assignment(tenant_id, state)`
    - `idx_assignment_instance ON assignment(shift_instance_id)`
    - `idx_rule_tenant ON rule(tenant_id)`
    - `idx_rule_override_tenant ON rule_override(tenant_id)`
    - `idx_swap_request_tenant_state ON swap_request(tenant_id, state)`
    Triggers: `set_updated_at()` BEFORE UPDATE on `availability`, `assignment`, `rule`, `rule_override`, `swap_request`.

    **`db/migrations/0005_auth_and_notifications.sql`**:
    Header: `-- 0005_auth_and_notifications.sql -- invite codes, redemption log, notification prefs, push subs, report recipients, iCal tokens`.
    Tables: `invite_code`, `invite_code_redemption`, `notification_pref`, `push_subscription`, `report_recipient`, `ical_subscription_token` per `<interfaces>`.
    Indexes:
    - `idx_invite_code_tenant ON invite_code(tenant_id)`
    - `idx_invite_code_code ON invite_code(code)` (UNIQUE via constraint already, but explicit index for lookup speed)
    - `idx_invite_code_redemption_tenant ON invite_code_redemption(tenant_id)`
    - `idx_invite_code_redemption_code ON invite_code_redemption(invite_code_id)`
    - `idx_notification_pref_tenant ON notification_pref(tenant_id)`
    - `idx_push_subscription_tenant ON push_subscription(tenant_id)`
    - `idx_report_recipient_tenant ON report_recipient(tenant_id)`
    - `idx_ical_token_tenant ON ical_subscription_token(tenant_id)`
    - `idx_ical_token_token ON ical_subscription_token(token)` (UNIQUE already; explicit for the lookup case)
    Triggers: `set_updated_at()` BEFORE UPDATE on `notification_pref`, `report_recipient`. (`invite_code` has only created_at — codes are immutable; revocation is via `revoked_at` not UPDATE of other fields. Actually wait — the redemption flow does UPDATE `invite_code SET uses = uses + 1`. Add `updated_at` + trigger to `invite_code`.)
    Apply COLLATE on `report_recipient.display_name`: `TEXT COLLATE "he-x-icu"`.

    Apply on hpg5 after each commit:
    - `plink ... hpg5 "powershell -c \"cd C:\shifts-manager; pscp ... && docker compose run --rm migrate up\""` (executor must pscp files; abbreviated for plan readability)
    - Verify each migration applied and idempotent (second `migrate up` returns "no change").
  </action>
  <verify>
    <automated>docker compose run --rm migrate up exits 0; second invocation exits 0 (idempotency); psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('shift_slot','planning_window','shift_instance','availability','assignment','rule','rule_override','swap_request','invite_code','invite_code_redemption','notification_pref','push_subscription','report_recipient','ical_subscription_token')" returns 14</automated>
  </verify>
  <acceptance_criteria>
    - Three migrations created with BEGIN/COMMIT envelopes
    - Every table has `tenant_id UUID NOT NULL REFERENCES tenant(id) ON DELETE CASCADE`
    - `shift_slot.name` and `report_recipient.display_name` declared `COLLATE "he-x-icu"`
    - Composite `(tenant_id, ...)` indexes on every tenant-scoped table per PERF-04
    - CHECK constraints match the enum lists in `<interfaces>` exactly
    - Triggers `set_updated_at()` attached to every table with `updated_at`
    - `invite_code.code` enforces Crockford base32 regex via CHECK constraint (matches AUTH-04 alphabet)
    - On hpg5: `docker compose run --rm migrate up` exits 0; consecutive re-run exits 0 (idempotency)
    - `\d invite_code`, `\d schedule_audit_target_window` (just check existence of all 14 tables) succeed
  </acceptance_criteria>
  <done>Migrations 0003, 0004, 0005 applied on hpg5 (idempotent); the underlying tables for RLS (0009) and audit REVOKEs (0010) are now in place except 0006 + 0007 (Task 2 below).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrations 0006 + 0007 — schedule_audit + solver_run + notification_log + roster_import_log</name>
  <read_first>
    - db/migrations/0001_init.sql (envelope pattern)
    - db/migrations/0005_auth_and_notifications.sql (just created in Task 1 — to verify FK targets like app_user.id)
    - .planning/phases/01-foundations/01-PATTERNS.md §0003-0007 migrations + Pattern 11 (0010 audit REVOKEs — preview to ensure 0006 tables match what 0010 will REVOKE)
    - .planning/phases/01-foundations/01-RESEARCH.md §Pattern 11 (Migration 0010 — confirms which tables append-only)
    - docs/PRD.md §7.8 (solver_run schema), §7.11 (notification_log columns), §7.13/7.14 (foundations for 0007 imports/exports)
  </read_first>
  <files>
    db/migrations/0006_audit_and_solver_runs.sql
    db/migrations/0007_imports_and_exports.sql
  </files>
  <action>
    **`db/migrations/0006_audit_and_solver_runs.sql`**:
    Header: `-- 0006_audit_and_solver_runs.sql -- schedule_audit (append-only target for 0010), solver_run, notification_log`.

    Tables per `<interfaces>` (from Plan-02 frontmatter):
    - `schedule_audit` — NO `updated_at` column; append-only enforced by 0010 REVOKE UPDATE/DELETE/TRUNCATE.
    - `solver_run` — has `created_at` only (each run is immutable; one row per CP-SAT execution).
    - `notification_log` — has `updated_at` because status transitions through webhooks (NOTF-06). 0010 only REVOKEs DELETE + TRUNCATE on this table, NOT UPDATE.

    Indexes:
    - `idx_schedule_audit_tenant ON schedule_audit(tenant_id)`
    - `idx_schedule_audit_window ON schedule_audit(planning_window_id)`
    - `idx_schedule_audit_actor ON schedule_audit(actor_user_id)`
    - `idx_solver_run_tenant ON solver_run(tenant_id)`
    - `idx_solver_run_window ON solver_run(planning_window_id)`
    - `idx_notification_log_tenant ON notification_log(tenant_id)`
    - `idx_notification_log_tenant_status ON notification_log(tenant_id, status)`
    - `idx_notification_log_user ON notification_log(user_id)`

    Triggers: `set_updated_at()` BEFORE UPDATE on `notification_log` ONLY. `schedule_audit` and `solver_run` get no trigger (no updated_at).

    **`db/migrations/0007_imports_and_exports.sql`**:
    Header: `-- 0007_imports_and_exports.sql -- roster_import_log (append-only Phase 2 consumer)`.

    Tables per `<interfaces>`:
    - `roster_import_log` — NO `updated_at` column; append-only enforced by 0010 REVOKE.

    Indexes:
    - `idx_roster_import_log_tenant ON roster_import_log(tenant_id)`
    - `idx_roster_import_log_actor ON roster_import_log(imported_by)`

    No triggers (no updated_at on append-only tables).

    Note on 0008: per CONTEXT.md D-06, migration 0008 (drop legacy `employees`/`shifts`/`assignments`/`availability`/`time_clock_entries` from 0001) is DEFERRED to Phase 2 boundary. Plan 02 does NOT create or apply 0008. The legacy tables stay live through Phase 1 to keep the smoke-test page working.

    Apply on hpg5:
    - pscp the two files to `C:\shifts-manager\db\migrations\` on hpg5.
    - `plink ... hpg5 "powershell -c \"cd C:\shifts-manager; docker compose run --rm migrate up\""` — expect output `5/u audit_and_solver_runs`, `6/u imports_and_exports` (or similar numeric labels — golang-migrate uses the file prefix).
    - Re-run: expect "no change".
    - Verify: `psql -c "\d schedule_audit"` must show NO `updated_at` column (per append-only design).
  </action>
  <verify>
    <automated>docker compose run --rm migrate up applies versions 6 and 7 (or whatever golang-migrate assigns); second invocation exits 0; psql -c "\d schedule_audit" returns columns id/tenant_id/planning_window_id/from_state/to_state/actor_user_id/actor_kind/payload/created_at with NO updated_at; psql -c "SELECT count(*) FROM information_schema.columns WHERE table_name='schedule_audit' AND column_name='updated_at'" returns 0</automated>
  </verify>
  <acceptance_criteria>
    - Migrations 0006 and 0007 created with BEGIN/COMMIT envelopes
    - `schedule_audit`, `solver_run`, `roster_import_log` have NO `updated_at` column (append-only)
    - `notification_log` HAS `updated_at` + trigger (status transitions via webhooks)
    - All tables have `tenant_id UUID NOT NULL REFERENCES tenant(id) ON DELETE CASCADE`
    - Composite `(tenant_id, ...)` indexes per PERF-04
    - On hpg5: full sequence `0001 → 0007` applies via `migrate up`; re-runs idempotent (OPS-02)
  </acceptance_criteria>
  <done>All seven Phase-1 schema migrations applied (0001 existing + 0002-0007 new); legacy bootstrap tables intact (D-06); ready for Plan 03 RLS (0009) and audit REVOKEs (0010).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shifty-audit-writer plugin — full src/* implementation + unit test + lowdefy.yaml registration</name>
  <read_first>
    - app/plugins/shifty-audit-writer/package.json (Plan 01 scaffold — confirm exports paths)
    - app/pnpm-workspace.yaml (Plan 01 — confirms workspace:* resolution path)
    - .claude/skills/lowdefy/reference/09-plugins.md (Authoring a connection request; types.js + connections.js structure)
    - .claude/skills/lowdefy/reference/06-operators.md (_user evaluation context; AuditWrite uses request.user, not _user operator directly)
    - .planning/phases/01-foundations/01-PATTERNS.md §`app/plugins/shifty-audit-writer/plugin.js` (full implementation pattern)
    - .planning/phases/01-foundations/01-RESEARCH.md §Pattern 8 (AuditWrite full source code, lines 740-853), §Anti-Patterns (tenant_id never from request.properties)
    - app/lowdefy.yaml (existing plugins: list — append, don't overwrite)
  </read_first>
  <behavior>
    - Test 1: `AuditWrite({ request: { user: { user_id: 'uuid1', tenant_id: 'uuid-t' }, properties: { to_state: 'test' } }, connection: {...} })` succeeds — INSERT executed with actor_user_id=uuid1
    - Test 2: `AuditWrite({ request: { user: undefined, properties: { to_state: 'test' } }, connection: {...} })` THROWS with message containing "actor_user_id missing"
    - Test 3: `AuditWrite({ request: { user: { user_id: 'uuid1', tenant_id: 'uuid-t' }, properties: { /* no to_state */ } }, connection: {...} })` THROWS with message containing "to_state is required"
    - Test 4: `AuditWrite.schema` is an object with `required: ['to_state']`
    - Test 5: `AuditWrite.connectionType === 'Knex'`
    - Test 6 (integration — deferred to Plan 04 Playwright spec `audit-writer.spec.ts`): writing through the YAML page produces a row in schedule_audit. Note this in the test file as a TODO.
  </behavior>
  <files>
    app/plugins/shifty-audit-writer/src/types.js
    app/plugins/shifty-audit-writer/src/connections.js
    app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js
    app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs
    app/lowdefy.yaml
  </files>
  <action>
    Implement the plugin per PATTERNS §`app/plugins/shifty-audit-writer/plugin.js` + RESEARCH Pattern 8 (lines 740-853).

    1. **`app/plugins/shifty-audit-writer/src/types.js`** (ES module):
       ```javascript
       // shifty-audit-writer plugin type registry
       // Registers the AuditWrite request type. The Lowdefy server reads this at startup
       // to know which `type:` values in YAML are handled by this plugin.
       export default {
         requests: ['AuditWrite'],
       };
       ```

    2. **`app/plugins/shifty-audit-writer/src/connections.js`** (ES module aggregator):
       ```javascript
       import AuditWrite from './connections/requests/AuditWrite.js';
       export default { AuditWrite };
       ```

    3. **`app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js`** — copy RESEARCH Pattern 8 implementation (lines 781-829) verbatim with two specific adjustments:
       - Use `import knex from 'knex';` at the top (not `const knex = require(...)`) because the plugin is ESM (`"type": "module"` in package.json).
       - Catch the `connection.client` value defensively — Lowdefy passes `connection` as the resolved properties object from the YAML connection. If `client: 'pg'` is set, `knex(connection)` works directly.
       - Test #2 expectation: when `request.user?.user_id` is `undefined`, throw `new Error('AuditWrite: actor_user_id missing from session — unauthenticated request')` BEFORE creating any Knex instance (so the unit test doesn't need a live DB).
       
       Full file (ESM):
       ```javascript
       // app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js
       // Lowdefy custom request: writes one row to schedule_audit.
       // Per D-08: actor_user_id comes from session (request.user), NEVER from request.properties.
       import knex from 'knex';

       async function AuditWrite({ request, connection }) {
         const { planning_window_id, from_state, to_state, actor_kind, payload_json } = request.properties || {};
         const actor_user_id = request.user?.user_id;

         if (!actor_user_id) {
           throw new Error('AuditWrite: actor_user_id missing from session — unauthenticated request');
         }
         if (!to_state) {
           throw new Error('AuditWrite: to_state is required');
         }

         const db = knex(connection);
         try {
           await db('schedule_audit').insert({
             tenant_id: request.user.tenant_id,
             planning_window_id: planning_window_id || null,
             from_state: from_state || null,
             to_state,
             actor_user_id,
             actor_kind: actor_kind || 'user',
             payload: payload_json ? JSON.stringify(payload_json) : null,
           });
           return { success: true };
         } finally {
           await db.destroy();
         }
       }

       AuditWrite.schema = {
         type: 'object',
         required: ['to_state'],
         properties: {
           planning_window_id: { type: 'string', format: 'uuid' },
           from_state: { type: 'string' },
           to_state: { type: 'string' },
           actor_kind: { enum: ['user', 'system', 'solver'] },
           payload_json: { type: 'object' },
         },
       };
       AuditWrite.connectionType = 'Knex';

       export default AuditWrite;
       ```

    4. **`app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs`** — Node built-in test runner. Tests should NOT hit a real DB (unit-level only); mock the Knex chain.
       ```javascript
       // Run: node --test app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs
       import { test } from 'node:test';
       import assert from 'node:assert/strict';
       import AuditWrite from '../src/connections/requests/AuditWrite.js';

       test('schema requires to_state', () => {
         assert.equal(AuditWrite.connectionType, 'Knex');
         assert.deepEqual(AuditWrite.schema.required, ['to_state']);
       });

       test('throws when actor_user_id missing (unauthenticated)', async () => {
         await assert.rejects(
           () => AuditWrite({
             request: { user: undefined, properties: { to_state: 'test_mutation' } },
             connection: { client: 'pg' },
           }),
           /actor_user_id missing/
         );
       });

       test('throws when to_state missing', async () => {
         await assert.rejects(
           () => AuditWrite({
             request: { user: { user_id: 'u1', tenant_id: 't1' }, properties: {} },
             connection: { client: 'pg' },
           }),
           /to_state is required/
         );
       });

       // NOTE: Integration test (Plan 04 audit-writer.spec.ts) covers the happy path
       // (live INSERT into schedule_audit). Unit tests here cover input validation
       // and the actor-from-session invariant without requiring a Knex connection.
       ```

    5. **`app/lowdefy.yaml`** — Add `shifty-audit-writer` to the existing `plugins:` block (Plan 03 will add `shifty-auth` separately; do not pre-add it here or the Lowdefy build fails). Insert AFTER the `@lowdefy/blocks-aggrid` entry:
       ```yaml
         - name: 'shifty-audit-writer'
           version: 'workspace:*'
       ```
       Do NOT modify any other section of lowdefy.yaml in this task.

    Build verification (after edits committed locally + pushed to hpg5):
    - pscp `app/plugins/shifty-audit-writer/` and `app/lowdefy.yaml` to hpg5.
    - On hpg5: `plink ... hpg5 "powershell -c \"...psexec... cmd /c 'cd C:\shifts-manager && docker compose build lowdefy > build.txt 2>&1'\""` (PsExec wrap per CLAUDE.md "Common ops" — build pulls images via Docker Desktop credential helper).
    - Tail `build.txt` — expect successful build with `pnpm install` resolving the `workspace:*` reference (no "workspace package not found" — Plan 01 added pnpm-workspace.yaml).
    - `docker compose up -d lowdefy` (no PsExec needed; image is now cached locally).
    - Verify still healthy: `curl http://hpg5:8080/employees` returns HTTP 200.
    - End-to-end plugin smoke is deferred to Plan 03 (it adds the YAML page using AuditWrite) and Plan 04 (Playwright audit-writer.spec.ts).
  </action>
  <verify>
    <automated>node --test app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs (3 tests pass); on hpg5: docker compose build lowdefy (via PsExec) exits 0 — no "workspace package not found"; curl http://hpg5:8080/employees still returns HTTP 200</automated>
  </verify>
  <acceptance_criteria>
    - `app/plugins/shifty-audit-writer/src/types.js` exports `{ requests: ['AuditWrite'] }`
    - `app/plugins/shifty-audit-writer/src/connections.js` imports AuditWrite and exports `{ AuditWrite }`
    - `app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js` throws on missing actor_user_id (verified by unit test 2) and on missing to_state (verified by unit test 3); reads actor_user_id from `request.user.user_id`, never from `request.properties`
    - `AuditWrite.schema.required` equals `['to_state']`; `AuditWrite.connectionType === 'Knex'`
    - Unit test file `app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs` passes 3 tests via `node --test`
    - `app/lowdefy.yaml` plugins: list includes `shifty-audit-writer` at `workspace:*`
    - On hpg5: `docker compose build lowdefy` (PsExec wrapped) completes successfully; the Lowdefy container starts and `/employees` smoke page still returns HTTP 200
  </acceptance_criteria>
  <done>shifty-audit-writer plugin loads in the Lowdefy build (workspace:* resolution proven); request type AuditWrite registered; unit tests prove actor-from-session invariant; ready for Plan 03 to add the admin_test_audit YAML page that exercises it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → AuditWrite request | A mutating page submits payload; the executor must ensure tenant_id and actor_user_id come from request.user only |
| Lowdefy build → workspace plugin | Plugin code runs server-side with DB write privileges; mitigated by code review + Plan 04 CI grep gate (when Plan 04 lands) |
| Migration runner → schema_migrations | `migrate` service writes its own state; an attacker with shell on the migrate container could mark migrations applied without running them — out of v1 scope (single-host, single-operator) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | AuditWrite receiving forged actor_user_id from request.properties | mitigate | Implementation reads `request.user?.user_id` (server-side session-injected); request.properties NEVER consulted for actor identity; unit test 2 enforces the invariant |
| T-02-02 | I (Information Disclosure) | schedule_audit row leaking cross-tenant data via missing tenant_id filter | mitigate | AuditWrite forces `tenant_id: request.user.tenant_id` (server-side); RLS in Plan 03 (0009) is the layer-5 catch; Plan 04 grep gate verifies the YAML query has tenant_id |
| T-02-03 | T (Tampering) | Audit log mutation (UPDATE/DELETE on schedule_audit) | mitigate | 0010 REVOKEs UPDATE/DELETE/TRUNCATE on schedule_audit (Plan 03 ships 0010); Plan 04 audit-immutable.spec.ts verifies |
| T-02-04 | R (Repudiation) | AuditWrite called with actor_kind='system' to hide a user action | accept | actor_kind enum constrained at DB level; misuse would require server-side code change (visible in git history); risk accepted for v1 (no anti-system-actor automated detection) |
| T-02-05 | I (Information Disclosure) | Migration files containing fixture seed data with PII | mitigate | No PII in migrations 0003-0007 (DDL only, no INSERTs); the kibbutz fixture is test-only data (display names are obvious placeholders like יוסי כהן) |
| T-02-06 | D (Denial of Service) | Long-running migrate up on production-sized DB | accept | Phase 1 has zero rows; migrations are <100 lines each; runtime is sub-second |
</threat_model>

<verification>
After all 3 tasks complete, run on hpg5:

1. Full migration sequence: `docker compose run --rm migrate up` (idempotent re-run also exits 0). Expect versions 1-7 applied.
2. Schema count: `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public'"` — 5 legacy (0001) + 9 in 0002 + 3 in 0003 + 5 in 0004 + 6 in 0005 + 3 in 0006 + 1 in 0007 = 32 tables.
3. Hebrew collation columns: `psql -c "SELECT table_name, column_name FROM information_schema.columns WHERE collation_name = 'he-x-icu' ORDER BY table_name, column_name"` — expect at least: org_unit.name, tenant.name, app_user.display_name, soldier.display_name, shift_slot.name, report_recipient.display_name.
4. Append-only tables have no updated_at: `psql -c "SELECT table_name FROM information_schema.columns WHERE table_name IN ('schedule_audit','solver_run','roster_import_log','invite_code_redemption') AND column_name='updated_at'"` — returns 0 rows.
5. Plugin: `node --test app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs` passes 3 tests.
6. Build: on hpg5, `docker compose build lowdefy` exits 0 (via PsExec); `/employees` smoke page still returns HTTP 200.
</verification>

<success_criteria>
- Migrations 0003-0007 applied via `migrate` compose service; re-runs idempotent
- 14 new tables across 0003-0007 with tenant_id FK on every domain row
- Hebrew collation on `shift_slot.name` and `report_recipient.display_name` (in addition to Plan 01's tenant.name / org_unit.name / app_user.display_name / soldier.display_name)
- `set_updated_at()` trigger on every table that has `updated_at`
- Append-only tables (`schedule_audit`, `solver_run`, `roster_import_log`, `invite_code_redemption`) have no `updated_at` column
- `shifty-audit-writer` plugin loads in the Lowdefy build (workspace:* resolves; unit tests pass)
- `app/lowdefy.yaml` registers the plugin via `workspace:*`
- Bootstrap `/employees` page still works (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-foundations-02-SUMMARY.md` documenting:
- Migrations 0003-0007 applied with idempotency proof (paired `migrate up` runs)
- Output of `\dt` showing all 32 tables
- Hebrew collation verification query result
- Plugin unit test output (3 passing)
- Lowdefy build output snippet (proves workspace:* resolved)
</output>
</content>
</invoke>
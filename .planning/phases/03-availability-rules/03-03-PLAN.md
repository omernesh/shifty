---
phase: 03-availability-rules
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - app/plugins/shifty-plugin/src/connections/Knex/requests/CreateShiftSlot.js
  - app/plugins/shifty-plugin/src/connections/Knex/requests/UpdateShiftSlot.js
  - app/plugins/shifty-plugin/src/connections/Knex/requests/DeleteShiftSlot.js
  - app/plugins/shifty-plugin/src/connections/Knex/requests/ApplyShiftTemplate.js
  - app/pages/admin/team_detail.yaml
  - app/blocks/shift_slot_form.yaml
  - app/blocks/template_wizard.yaml
  - tests/e2e/shift-slot-crud.spec.ts
autonomous: true
requirements:
  - SHFT-01
  - SHFT-02
  - SHFT-03
  - SHFT-04

must_haves:
  truths:
    - "Four new Phase 03 handlers exist in `app/plugins/shifty-plugin/src/connections/Knex/requests/`: `CreateShiftSlot.js`, `UpdateShiftSlot.js`, `DeleteShiftSlot.js`, `ApplyShiftTemplate.js`. Each follows the canonical handler shape from RESEARCH Recipe 1: tenant_id derived from `request.user` (NEVER from request body — per D-Area-1 + RESEARCH Security Domain V13 ASVS pattern), Layer-4 RBAC guard (unit_admin OR team_manager-of-this-team), withTenantTx wrapper, mutation + schedule_audit insert in ONE transaction, `.schema` JSON-schema property, `.connectionType = 'Knex'`, `.meta = { checkRead: false, checkWrite: false }`. The team_manager scope check uses the membership-join SELECT pattern from RESEARCH Recipe 2."
    - "CreateShiftSlot persists `name` (canonicalized via canonicalizeText from helpers — strips smart-quote U+2019 + U+200E/200F/202A-202E per Phase 02 D-13), `start_time` + `end_time` (HH:MM strings; no DB CHECK on ordering so cross-midnight works), `headcount` (integer ≥1, default 1), `required_role_tags` (TEXT[]), `min_seniority` (nullable integer 0-10), `display_order` (auto-resolved to max+1 when not supplied). Refuses when start_time === end_time. Validates role_tags array shape but does NOT recanonicalize each tag (Phase 02 D-13 leaves tag canonicalization to the dedicated role-tag flow)."
    - "UpdateShiftSlot accepts the same property set; WHERE clause always includes `tenant_id` AND `id` (per RESEARCH Risk R-03-2 mitigation — every UPDATE/DELETE in Phase 03 handlers MUST scope on tenant_id explicitly even though RLS Layer 5 backstops). Non-admin manager must own the team; verified via membership-join scope check."
    - "DeleteShiftSlot refuses with a clear Hebrew error message when any `shift_instance` row references the slot (per D-Area-1 line 'Delete shift slot when instances already generated by an open window: block'). Implementation: `EXISTS (SELECT 1 FROM shift_instance WHERE shift_slot_id = :id AND tenant_id = :tenant_id) → refuse`. Audit row written before deletion when proceeding."
    - "ApplyShiftTemplate is the template-wizard handler (RESEARCH Recipe 3). Hardcodes the two PRD §7.4 template payloads with EXACT Hebrew names per the source-of-truth check in CONTEXT specifics (`בוקר`/`לילה` for 2x12h; `בוקר`/`ערב`/`לילה` for 3x8h — `צהריים` is forbidden per the UI-checker correction). `custom` template inserts zero slots but still sets `org_unit.template_picked_at` so the wizard doesn't re-prompt. Refuses to overwrite if any shift_slot already exists for the team (D-Area-1: 'Once any slot exists for the team, modal does NOT re-prompt'). Sets `org_unit.template_picked_at = now()` in the same TX."
    - "`app/pages/admin/team_detail.yaml` is extended with a 'משמרות' tab (NEW — alongside the existing 'פרטי צוות' and 'חברי צוות' tabs from Phase 02). The tab renders the AgGrid slot list (UI-SPEC Surface 1) with row actions (ערוך, מחק), a toolbar with '+ הוסף משמרת' (opens shift_slot_form Modal) and '↺ פתח תבנית' (opens template_wizard Modal — visible only when slot list is empty AND `org_unit.template_picked_at IS NULL`). The cross-midnight ⓘ column shows `AiOutlineInfoCircle` when `end_time < start_time`. AgGrid uses `enableRtl: true` (UI-SPEC Reusable Component 8 — Phase 02 carry-over)."
    - "Two new shared block YAMLs live at `app/blocks/`: `shift_slot_form.yaml` (UI-SPEC Surface 2 — the modal-form shape with TimePicker pair + cross-midnight inline hint + headcount NumberInput + required_role_tags MultipleSelector bound to role_tag table + min_seniority NumberInput + display_order NumberInput) and `template_wizard.yaml` (UI-SPEC Surface 3 — three Selector cards showing the exact PRD §7.4 Hebrew names). Both blocks are reusable; team_detail uses both via `_ref`."
    - "`tests/e2e/shift-slot-crud.spec.ts` is a new Playwright UI-driven spec (copies the Pattern A template from Plan 03-01's helpers) with at least 6 test blocks covering: (1) manager creates a slot via the form modal — assert DB row + display_order auto-resolves; (2) manager edits a slot — assert UPDATE writes audit; (3) manager attempts cross-midnight slot (end < start) — assert ⓘ hint visible in UI AND DB row persists with cross-midnight times; (4) manager applies 2x12h template — assert TWO slots inserted with exact Hebrew names + org_unit.template_picked_at populated; (5) manager opens slot-tab AFTER template applied — assert wizard NOT shown (button hidden); (6) admin tries to DELETE a slot referenced by shift_instances — assert refusal with Hebrew error toast AND slot still exists in DB. The spec uses lowdefy-ui helpers from Plan 03-01."
    - "Running `node tools/check-handler-registration.mjs` after this plan's tasks exit 0 — all 4 new handlers are registered correctly (types.js entry exists from Plan 03-02; the verifier confirms `.meta`, `.connectionType`, default export, AND Knex.js import + spread)."
  artifacts:
    - path: app/plugins/shifty-plugin/src/connections/Knex/requests/CreateShiftSlot.js
      provides: "Creates a shift_slot row with canonicalized name + role_tags array + audit row in one TX"
      contains: "shift_slot"
    - path: app/plugins/shifty-plugin/src/connections/Knex/requests/UpdateShiftSlot.js
      provides: "Updates shift_slot with tenant_id WHERE-scope + manager team scope check"
      contains: "tenant_id"
    - path: app/plugins/shifty-plugin/src/connections/Knex/requests/DeleteShiftSlot.js
      provides: "Deletes shift_slot only when no shift_instance references it; audit row written"
      contains: "shift_instance"
    - path: app/plugins/shifty-plugin/src/connections/Knex/requests/ApplyShiftTemplate.js
      provides: "Wizard handler inserting 2x12h / 3x8h preset slots with exact PRD §7.4 Hebrew names + sets template_picked_at"
      contains: "בוקר"
    - path: app/pages/admin/team_detail.yaml
      provides: "Extended with 'משמרות' tab containing slot list + Add + template wizard CTAs"
      contains: "משמרות"
    - path: app/blocks/shift_slot_form.yaml
      provides: "Reusable Modal form block — TimePicker pair + role tags + headcount"
      contains: "TimePicker"
    - path: app/blocks/template_wizard.yaml
      provides: "Three Selector cards for the template picker"
      contains: "2x12h"
    - path: tests/e2e/shift-slot-crud.spec.ts
      provides: "6 UI-driven tests covering CRUD + template + delete-block scenarios"
      contains: "ApplyShiftTemplate"
  key_links:
    - from: app/plugins/shifty-plugin/src/connections/Knex/requests/CreateShiftSlot.js
      to: app/plugins/shifty-plugin/src/hooks/with-tenant-tx.js
      via: "withTenantTx wraps the transactional INSERT + audit (Layer 5 SET LOCAL app.current_tenant)"
      pattern: "withTenantTx"
    - from: app/plugins/shifty-plugin/src/connections/Knex/requests/CreateShiftSlot.js
      to: app/plugins/shifty-plugin/src/helpers/canonicalize.js
      via: "canonicalizeText strips smart-quote variants before INSERT (P10 defense)"
      pattern: "canonicalizeText"
    - from: app/plugins/shifty-plugin/src/connections/Knex/requests/ApplyShiftTemplate.js
      to: db/migrations/0014_phase3_denorms.up.sql
      via: "Updates org_unit.template_picked_at column added in 0014"
      pattern: "template_picked_at"
    - from: app/pages/admin/team_detail.yaml
      to: app/blocks/shift_slot_form.yaml
      via: "_ref to shared form block; Modal opens via state-driven visible: toggle"
      pattern: "shift_slot_form"
    - from: app/pages/admin/team_detail.yaml
      to: app/blocks/template_wizard.yaml
      via: "_ref to wizard block; visible only when slot count = 0 AND template_picked_at IS NULL"
      pattern: "template_wizard"
    - from: tests/e2e/shift-slot-crud.spec.ts
      to: tests/e2e/_helpers/lowdefy-ui.ts
      via: "Imports fillLowdefyInput, clickLowdefyButton, expectAgGridCellText"
      pattern: "_helpers/lowdefy-ui"
---

<objective>
Implement the shift_slot CRUD layer + the template wizard end-to-end: 4 custom-request handlers in shifty-plugin, the 'משמרות' tab on team_detail with the AgGrid slot list, two reusable block YAMLs (shift_slot_form + template_wizard), and a Playwright UI-driven spec covering CRUD + template flows.

This plan covers SHFT-01..04 from CONTEXT D-Area-1 and PRD §7.4. The handlers follow the verbatim shape of Phase 02's CreateSoldier (RESEARCH Recipe 2): tenant_id from session, Layer-4 RBAC guard via membership-join, withTenantTx wrapper, mutation + audit in one TX, .schema/.connectionType/.meta on the exported function. ApplyShiftTemplate (RESEARCH Recipe 3) is the wizard handler that inserts the 2x12h or 3x8h preset slots with the EXACT Hebrew names PRD §7.4 specifies (the CONTEXT specifics call this out twice — `בוקר`/`לילה` not paraphrased; no `צהריים` in 3x8h).

The UI work introduces team_detail's third tab plus two shared block files. Tab strip extension preserves Phase 02's existing 'פרטי צוות' and 'חברי צוות' tabs verbatim. The Add-slot Modal and template wizard Modal both live in `app/blocks/` so future phases (e.g., a v1.1 admin-bulk-edit) can re-use without duplication.

The Playwright spec is the first new Phase 03 UI spec; it copies Pattern A from Plan 03-01's helpers (no direct API POSTs for mutation paths). The 6 test scenarios cover the load-bearing behaviors per CONTEXT D-Area-1: create, edit, cross-midnight, template, wizard-suppression-after-template, delete-block-when-instances-exist.

Purpose: Deliver the foundation a manager needs before opening a planning window — without shift_slots, OpenPlanningWindow cannot generate any instances.
Output: 4 handlers + 1 page edit + 2 new blocks + 1 new spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-availability-rules/03-CONTEXT.md
@.planning/phases/03-availability-rules/03-RESEARCH.md
@.planning/phases/03-availability-rules/03-UI-SPEC.md
@docs/PRD.md
@CLAUDE.md
@app/plugins/shifty-plugin/src/connections/Knex/requests/CreateSoldier.js
@app/plugins/shifty-plugin/src/connections/Knex/requests/UpdateSoldier.js
@app/plugins/shifty-plugin/src/connections/Knex/requests/ArchiveSoldier.js
@app/plugins/shifty-plugin/src/hooks/with-tenant-tx.js
@app/plugins/shifty-plugin/src/helpers/canonicalize.js
@app/pages/admin/team_detail.yaml
@db/migrations/0003_shifts_and_windows.up.sql
@tests/e2e/_helpers/lowdefy-ui.ts
@tests/e2e/_fixtures/seed-tenants.ts

<interfaces>
Canonical handler shape (from RESEARCH Recipe 1; concrete instance: CreateSoldier.js):
  - Destructure request.properties
  - Tenant + actor guards from request.user (throw if missing)
  - Role guard (is_admin OR is_manager)
  - Payload validation
  - withTenantTx(connection, tenant_id, async (trx) => {
      // Layer-4 scope check (membership-join when not admin)
      // Primary mutation
      // schedule_audit insert IN SAME TX
      return { result fields }
    })
  - <Name>.schema = { type:'object', required:[...], properties:{...} }
  - <Name>.connectionType = 'Knex'
  - <Name>.meta = { checkRead: false, checkWrite: false }
  - export default <Name>

Hebrew template names (PRD §7.4 — DO NOT paraphrase, per CONTEXT specifics):
  2x12h: { name: 'בוקר', start_time: '06:00', end_time: '18:00', display_order: 0 }
         { name: 'לילה', start_time: '18:00', end_time: '06:00', display_order: 1 }
  3x8h:  { name: 'בוקר', start_time: '06:00', end_time: '14:00', display_order: 0 }
         { name: 'ערב',  start_time: '14:00', end_time: '22:00', display_order: 1 }
         { name: 'לילה', start_time: '22:00', end_time: '06:00', display_order: 2 }
  custom: []  (zero slots; still set template_picked_at)

shift_slot schema (from 0003 + Phase 03 expectations):
  id UUID PK, tenant_id UUID, team_id UUID, name TEXT, start_time TIME, end_time TIME,
  headcount INT, required_role_tags TEXT[], min_seniority INT NULL, display_order INT,
  created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ

team_detail tabs after this plan (existing + new):
  פרטי צוות (Phase 02), חברי צוות (Phase 02), משמרות (NEW Phase 03)
  Phase 03's later plans add: חוקי הצוות (Plan 03-06)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author CreateShiftSlot + UpdateShiftSlot + DeleteShiftSlot + ApplyShiftTemplate handlers</name>
  <files>
    app/plugins/shifty-plugin/src/connections/Knex/requests/CreateShiftSlot.js,
    app/plugins/shifty-plugin/src/connections/Knex/requests/UpdateShiftSlot.js,
    app/plugins/shifty-plugin/src/connections/Knex/requests/DeleteShiftSlot.js,
    app/plugins/shifty-plugin/src/connections/Knex/requests/ApplyShiftTemplate.js
  </files>
  <action>
    Write the four handlers following the canonical shape in RESEARCH Recipes 2 and 3 and the `<interfaces>` block. CreateShiftSlot mirrors RESEARCH Recipe 2 verbatim with these specifics: payload destructure { team_id, name, start_time, end_time, headcount=1, required_role_tags=[], min_seniority=null, display_order=null }; guards reject empty name/team_id/times, equal start/end, headcount<1, role_tags not array; canonicalizeText runs on name before INSERT; cleanedRoleTags is `Array.from(new Set(...trim().filter(Boolean)))` (defensive dedup); display_order auto-resolves via `SELECT max(display_order) FROM shift_slot WHERE tenant_id AND team_id` + 1 when caller passes null. Layer-4 manager scope check is a SELECT against membership joined to soldier (RESEARCH Recipe 2 — `membership WHERE org_unit_id=team_id AND role='team_manager' AND soldier_id IN (SELECT id FROM soldier WHERE user_id = :actor)`). Audit row payload includes `shift_slot_id, team_id, name, start_time, end_time, headcount, required_role_tags, min_seniority`. to_state='shift_slot_created'.

    UpdateShiftSlot: destructure { shift_slot_id, name, start_time, end_time, headcount, required_role_tags, min_seniority, display_order }; same Layer-4 + canonicalize; UPDATE with `WHERE id = :shift_slot_id AND tenant_id = :tenant_id` (RESEARCH Risk R-03-2). Returning zero rows = 'shift_slot not found in tenant'. Audit to_state='shift_slot_updated' with from-state/to-state fields capturing the changed columns.

    DeleteShiftSlot: destructure { shift_slot_id }; Layer-4 manager scope check; `SELECT EXISTS (SELECT 1 FROM shift_instance WHERE shift_slot_id = :shift_slot_id AND tenant_id = :tenant_id) AS has_instances`; if true throw Error with Hebrew-rendered message `'shift_slot_has_instances'` (the YAML side maps to a Hebrew toast). Otherwise DELETE + audit row (to_state='shift_slot_deleted', payload includes the deleted slot's pre-image).

    ApplyShiftTemplate: per RESEARCH Recipe 3 verbatim. Hardcode the TEMPLATES constant with the EXACT Hebrew names from `<interfaces>` (no paraphrasing). `custom` template inserts zero slots but still sets `org_unit.template_picked_at` (D-Area-1: 'Once any slot exists for the team, modal does NOT re-prompt' — the picked_at flag suppresses the wizard even when zero slots exist). Pre-check refuses overwrite if any shift_slot exists for `(tenant_id, team_id)`. After per-slot INSERTs (loop with .insert().returning('id')), UPDATE org_unit set template_picked_at = now() WHERE id=team_id AND tenant_id=tenant_id. Audit row to_state='shift_template_applied' payload {team_id, template_key, created_ids, headcount}.

    All four handlers set `.connectionType = 'Knex'` and `.meta = { checkRead: false, checkWrite: false }` per Pitfall 1 (the line every handler must include). Schemas: CreateShiftSlot per Recipe 2; Update similar with all properties optional except shift_slot_id; Delete required: ['shift_slot_id']; ApplyShiftTemplate required: ['team_id', 'template_key'] with template_key enum ['2x12h', '3x8h', 'custom'].

    NOTE on cross-package import paths: every handler is at `connections/Knex/requests/` so withTenantTx is imported from `'../../../hooks/with-tenant-tx.js'`, canonicalizeText from `'../../../helpers/canonicalize.js'` (three levels up — same as Phase 02 handlers per 02-11 merge).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const base='app/plugins/shifty-plugin/src/connections/Knex/requests/'; const handlers={'CreateShiftSlot.js':['canonicalizeText','withTenantTx','schedule_audit','shift_slot_created','connectionType','checkRead'],'UpdateShiftSlot.js':['withTenantTx','tenant_id','shift_slot_updated','connectionType'],'DeleteShiftSlot.js':['withTenantTx','shift_instance','shift_slot_deleted','connectionType'],'ApplyShiftTemplate.js':['בוקר','לילה','ערב','2x12h','3x8h','template_picked_at','shift_template_applied','connectionType']}; for (const [f,markers] of Object.entries(handlers)) { const p=base+f; if (!fs.existsSync(p)) { console.error('MISSING handler:',f); process.exit(1); } const t=fs.readFileSync(p,'utf8'); for (const m of markers) { if (!t.includes(m)) { console.error(f+' missing marker:',m); process.exit(2); } } if (!t.match(/export\\s+default\\s+/)) { console.error(f+' missing default export'); process.exit(3); } if (!t.match(/\\.meta\\s*=\\s*\\{\\s*checkRead/)) { console.error(f+' missing .meta = { checkRead'); process.exit(4); } if (!t.match(/\\.connectionType\\s*=\\s*['\"]Knex['\"]/)) { console.error(f+' missing .connectionType = Knex'); process.exit(5); } } const tpl=fs.readFileSync(base+'ApplyShiftTemplate.js','utf8'); if (tpl.includes('צהריים')) { console.error('ApplyShiftTemplate must NOT use צהריים (forbidden per CONTEXT specifics)'); process.exit(6); } console.log('OK Task 1 — 4 handlers');"</automated>
  </verify>
  <done>4 handlers exist; each has tenant_id derived from request.user, withTenantTx wrapper, schedule_audit insert in same TX, .meta + .connectionType + default export; ApplyShiftTemplate hardcodes EXACT PRD §7.4 Hebrew names (בוקר/לילה for 2x12h; בוקר/ערב/לילה for 3x8h; no צהריים); DeleteShiftSlot refuses when shift_instance rows exist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the shift_slot_form + template_wizard reusable blocks and wire the משמרות tab on team_detail</name>
  <files>
    app/blocks/shift_slot_form.yaml,
    app/blocks/template_wizard.yaml,
    app/pages/admin/team_detail.yaml
  </files>
  <action>
    File 1 — `app/blocks/shift_slot_form.yaml`: per UI-SPEC Surface 2. A Modal-shaped Box (the parent page controls visibility via `_state`). Contents: TextInput for `name`; TimePicker pair (start_time / end_time) with format HH:mm + showSecond:false + use12Hours:false (UI-SPEC Reusable Component 10 — copy the YAML fragment); cross-midnight inline hint Box visible when end < start (`_lt: [{ _state: end_time }, { _state: start_time }]`); NumberInput for headcount (min=1, default=1); MultipleSelector for required_role_tags bound to the role_tag table (autocomplete; UI-SPEC Reusable Component 9 equivalent — reuse the pattern from Phase 02 add-soldier form); NumberInput for min_seniority (min=0, max=10, optional → maps to null when blank); NumberInput for display_order (optional → auto-resolved when blank). Form submit button label "שמור משמרת" (per UI-SPEC Surface 2). The block's payload references shape: `{ team_id: { _input: team_id }, name: { _state: shift_slot_form_name }, start_time: { _state: shift_slot_form_start_time }, ... }` — every field via _state operator (the pattern Plan 03-01's helpers rebuild around). Block emits onSubmit event that fires the CreateShiftSlot request (when in 'create' mode) or UpdateShiftSlot (when 'edit' mode) — modes distinguished by whether `shift_slot_id` is in state. Block accepts a `mode` parameter via `_ref.vars`.

    File 2 — `app/blocks/template_wizard.yaml`: per UI-SPEC Surface 3. Three Selector cards laid out in a Row (Ant Row with `xs:24 sm:8` for stacking on mobile, side-by-side on desktop). Each card uses the Recipe 11 YAML shape from UI-SPEC. Cards show the exact PRD Hebrew names (2x12h: `בוקר 06:00–18:00` + `לילה 18:00–06:00`; 3x8h: `בוקר 06:00–14:00` + `ערב 14:00–22:00` + `לילה 22:00–06:00`; Custom: 'ללא משמרות מוגדרות / הוסף משמרת בודדת'). Selected card state held in `_state.selected_template` (one of '2x12h', '3x8h', 'custom'). CTA button label changes per selection: '2x12h'/'3x8h' → 'צור משמרות מתבנית'; 'custom' → 'התחל ריק'. CTA fires the ApplyShiftTemplate request with `{ team_id, template_key: { _state: selected_template } }` payload.

    File 3 — Edit `app/pages/admin/team_detail.yaml` to add the 'משמרות' tab. Find the existing Tabs block (must exist already per Phase 02 team_detail structure; if not, add an Ant Tabs block wrapping the current page body). Add a new Tab pane titled 'משמרות' (per UI-SPEC Surface 1 tab strip). The pane content is a Box wrapping (a) a Title 'משמרות הצוות'; (b) a toolbar Box with '+ הוסף משמרת' button (sets `_state.shift_slot_form_open = true` and `_state.shift_slot_form_mode = 'create'`) and '↺ פתח תבנית' button (sets `_state.template_wizard_open = true`); the toolbar's wizard button is `visible: { _and: [{ _eq: [{ _state: slot_count }, 0] }, { _eq: [{ _ref: team }, ...] }, ...] }` — visible only when slot_count===0 AND `team.template_picked_at` IS NULL; (c) an AgGrid (using @lowdefy/blocks-aggrid AgGridAlpine) listing shift_slot rows for this team — columns: display_order, name, time range (computed `start_time + '–' + end_time`), headcount, role_tags (comma-joined), cross-midnight ⓘ icon (when end_time<start_time render AiOutlineInfoCircle), and a right-aligned actions column with [✎ ערוך] and [✗ מחק] icon buttons. `enableRtl: true`. Row click sets the row id into state for the form. Below: include shift_slot_form_modal and template_wizard_modal as Modal-wrapped blocks via `_ref` to the new shared blocks (visible bound to the respective `_state.<>_open` flags). The team's slot count + template_picked_at are loaded via a new KnexRawTenant request on page mount (extend the existing page-mount request chain — Phase 02 already loads team details; add `load_team_slots` (KnexRawTenant: `SELECT id, name, start_time, end_time, headcount, required_role_tags, min_seniority, display_order FROM shift_slot WHERE tenant_id = :tenant_id AND team_id = :team_id ORDER BY display_order`) and `load_template_picked_at` (KnexRawTenant: `SELECT template_picked_at FROM org_unit WHERE id = :team_id AND tenant_id = :tenant_id`)). Both new requests' payloads bind `tenant_id` via `_user: tenant_id` (NEVER from input) per RESEARCH Security Domain.

    Hebrew label inventory for this task (from UI-SPEC Surface 1+2+3 + PRD §7.4): 'משמרות', 'משמרות הצוות', '+ הוסף משמרת', '↺ פתח תבנית', 'הוספת משמרת לצוות', 'עריכת משמרת', 'שם המשמרת', 'זמן התחלה', 'זמן סיום', 'מסתיים למחרת', 'כמות נדרשת', 'תגיות תפקיד נדרשות (אופציונלי)', 'ותק מינימלי (אופציונלי)', 'סדר תצוגה', 'שמור משמרת', 'ביטול', 'בחר תבנית משמרות לצוות', 'צור משמרות מתבנית', 'התחל ריק', 'ערוך', 'מחק'.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for (const f of ['app/blocks/shift_slot_form.yaml','app/blocks/template_wizard.yaml','app/pages/admin/team_detail.yaml']) { if (!fs.existsSync(f)) { console.error('MISSING:',f); process.exit(1); } } const form=fs.readFileSync('app/blocks/shift_slot_form.yaml','utf8'); for (const tok of ['TimePicker','HH:mm','start_time','end_time','headcount','required_role_tags','min_seniority','שמור משמרת']) { if (!form.includes(tok)) { console.error('shift_slot_form.yaml missing:',tok); process.exit(2); } } const wiz=fs.readFileSync('app/blocks/template_wizard.yaml','utf8'); for (const tok of ['2x12h','3x8h','custom','בוקר','לילה','ערב','06:00','18:00','22:00','צור משמרות מתבנית','התחל ריק']) { if (!wiz.includes(tok)) { console.error('template_wizard.yaml missing:',tok); process.exit(3); } } if (wiz.includes('צהריים')) { console.error('template_wizard must NOT use צהריים'); process.exit(4); } const td=fs.readFileSync('app/pages/admin/team_detail.yaml','utf8'); for (const tok of ['משמרות','+ הוסף משמרת','load_team_slots','template_picked_at']) { if (!td.includes(tok)) { console.error('team_detail.yaml missing:',tok); process.exit(5); } } if (!td.match(/_user:\\s*tenant_id/)) { console.error('team_detail must bind tenant_id via _user: tenant_id'); process.exit(6); } console.log('OK Task 2 — blocks + team_detail wired');"</automated>
  </verify>
  <done>shift_slot_form.yaml + template_wizard.yaml exist with the UI-SPEC structure; team_detail.yaml has a new משמרות tab with AgGrid + toolbar + Modal refs; cross-midnight hint wired via state operator; tenant_id always bound via _user (never input); Hebrew names match PRD exactly; no צהריים anywhere.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author shift-slot-crud.spec.ts (6 UI-driven tests covering CRUD + template + delete-block)</name>
  <files>tests/e2e/shift-slot-crud.spec.ts</files>
  <action>
    Create `tests/e2e/shift-slot-crud.spec.ts` as a new Playwright spec following the Pattern A template from Plan 03-01's helper file. Standard preamble: import `{ test, expect } from '@playwright/test'`; import helpers from `./_helpers/lowdefy-ui` (fillLowdefyInput, clickLowdefyButton, expectAgGridCellText, expectLowdefyNotification, setSessionCookie); import seedTwoTenants + signInAs + teardownTestData from `./_fixtures/seed-tenants` + `./_fixtures/teardown`; set BASE_URL from env. test.beforeAll seeds two tenants + creates a manager-of-team-A signIn. test.afterAll teardowns. 6 test() blocks:

    Test 1 — `'manager creates shift_slot via form modal'`: navigate to `${BASE_URL}/admin/team_detail?team_id=${teamA.id}`; click tab 'משמרות'; click '+ הוסף משמרת'; fill name `'בוקר'`, start_time `'06:00'`, end_time `'18:00'`, headcount `2`; click 'שמור משמרת'; assert Notification, assert AgGrid row visible with `'בוקר'` + `'06:00–18:00'` + `2`; direct PG assert: `SELECT count(*) FROM shift_slot WHERE tenant_id = $1 AND team_id = $2 AND name = 'בוקר'` returns 1 with display_order=0.

    Test 2 — `'manager edits shift_slot — UPDATE writes audit row'`: pre-condition Test 1's row exists. Click [✎ ערוך] icon on the row; in the same modal change headcount to 3; click 'שמור משמרת'; assert AgGrid cell shows 3; direct PG assert audit row exists with to_state='shift_slot_updated' and payload.headcount=3.

    Test 3 — `'cross-midnight slot — ⓘ hint visible, DB row persists with end<start'`: click '+ הוסף משמרת'; fill name `'לילה'`, start_time `'22:00'`, end_time `'06:00'`; assert the cross-midnight hint Box is visible (Playwright `getByText('מסתיים למחרת')` returns non-empty); click save; direct PG assert: shift_slot row exists with start_time='22:00:00' AND end_time='06:00:00' AND (end_time < start_time at SQL level).

    Test 4 — `'manager applies 2x12h template — two slots inserted with exact Hebrew names'`: seed a fresh team WITHOUT any slots (via direct PG INSERT in the test, or via a helper). Navigate to that team's team_detail / משמרות tab; click '↺ פתח תבנית'; click the 2x12h card; click 'צור משמרות מתבנית'; assert Notification; direct PG assert: two rows in shift_slot with names exactly `'בוקר'` and `'לילה'`, times `'06:00:00'/'18:00:00'` and `'18:00:00'/'06:00:00'`, display_order 0 and 1; assert org_unit.template_picked_at is NOT NULL.

    Test 5 — `'wizard NOT shown after template applied (or any slot exists)'`: Use the same team from Test 4; navigate back to team_detail / משמרות tab; assert '↺ פתח תבנית' button is NOT visible (the button has visible: clause that hides when slot_count>0 OR template_picked_at IS NOT NULL).

    Test 6 — `'DELETE blocked when shift_instance references the slot'`: pre-condition: seed via direct PG a shift_slot AND a shift_instance referencing it (use the test fixture's SET ROLE NONE pattern). Navigate to team_detail / משמרות tab; click [✗ מחק] on the row; in the confirmation modal click 'מחק'; assert toast Notification with Hebrew error text mentioning 'shift_slot_has_instances' (or however Task 1's handler renders it — the YAML toast pulls the message from `_request.error.message`); direct PG assert: shift_slot row STILL exists.

    Each test wraps page.goto in try/catch and uses `test.skip(true, '...')` on failure (skip-on-stack-down). Cookies set via `setSessionCookie(page.context(), signIn.sessionToken, BASE_URL)` before goto.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('tests/e2e/shift-slot-crud.spec.ts','utf8'); const testCount=(t.match(/test\\(['\"]/g)||[]).length; if (testCount < 6) { console.error('Expected at least 6 tests, got', testCount); process.exit(1); } for (const tok of ['בוקר','לילה','06:00','18:00','22:00','מסתיים למחרת','+ הוסף משמרת','↺ פתח תבנית','2x12h','מחק','shift_slot_has_instances','_helpers/lowdefy-ui']) { if (!t.includes(tok)) { console.error('shift-slot-crud.spec.ts missing token:', tok); process.exit(2); } } if (t.match(/request\\.post\\(.*\\/api\\/request\\/[^'\"]+\\/(Create|Update|Delete|Apply)/)) { console.error('shift-slot-crud must not use direct API POST for mutations'); process.exit(3); } console.log('OK Task 3 — shift-slot-crud spec:', testCount, 'tests');"</automated>
  </verify>
  <done>shift-slot-crud.spec.ts has ≥6 UI-driven test blocks covering create/edit/cross-midnight/template/wizard-suppression/delete-block; uses lowdefy-ui helpers; preserves skip-on-stack-down; no direct API POSTs to mutation endpoints.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Deploy + verify on hpg5 — push, sync, PsExec rebuild, run check:handler-registration + new spec</name>
  <files>(no file changes — verification run)</files>
  <action>
    Commit + push tasks 1-3. On hpg5 over plink (per CLAUDE.md "Deploy sync"): `git fetch origin main && git reset --hard origin/main`. Rebuild Lowdefy via the PsExec wrapper (per CLAUDE.md "Common ops on hpg5" → 'Rebuild Lowdefy' command). Container restarts on `docker compose up -d lowdefy`. Check container health: `docker logs shifty-lowdefy --tail 30 | grep ready`. Verify BUILD-EMITTED `/build/.lowdefy/server/build/plugins/connections.js` still imports Knex from shifty-plugin/connections (no regression). Run `pnpm run check:handler-registration` against the local working tree — confirm exit 0 (the 12 new handlers are now in place; the 4 from this plan are real, the 8 from Plans 03-04..03-06 are still stub-imported in Knex.js but the verifier checks the source side: it expects each Knex.js-imported name to ALSO have a matching `requests/<Name>.js` file). NOTE: the verifier WILL fail for the 8 still-unbuilt handlers from later plans. This is expected and acceptable for this intermediate state; the verifier passes per-plan once that plan's handler files are written. The orchestrator and downstream plans accept that the verifier is an at-end-of-plan check, not a strict per-commit check. Run the new spec against hpg5: `pnpm exec playwright test tests/e2e/shift-slot-crud.spec.ts` with PLAYWRIGHT_BASE_URL=https://apps.nesher.co. All 6 tests must pass.
  </action>
  <verify>
    <automated>echo "Deploy + rebuild via PsExec; run check:handler-registration (4 of 12 new handlers present, 8 pending → verifier will fail strictly, that is the expected state for this intermediate plan; ALTERNATIVELY scope the verifier check to only the 4 newly-added handlers); run pnpm exec playwright test tests/e2e/shift-slot-crud.spec.ts and assert 0 failed."</automated>
  </verify>
  <done>hpg5 synced to HEAD; Lowdefy rebuilt + healthy; build-emitted connections.js intact; shift-slot-crud.spec.ts passes 6/6 against live hpg5 (or skipped cleanly if stack down); structural verifier acknowledged as pending until all 12 handlers land at Plan 03-06 completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lowdefy page YAML → CreateShiftSlot handler | Untrusted client supplies team_id, name, times, role_tags; handler enforces tenant scope + role + scope check before INSERT. |
| Manager UI → DeleteShiftSlot | Hard delete blocked when instances exist; audit row written even on successful delete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | Forged tenant_id in CreateShiftSlot/Update/Delete payload | mitigate | Every handler derives tenant_id from `request.user.tenant_id` (session); ignores `request.properties.tenant_id` if present. CI grep gate `tools/check-queries.mjs` (Phase 01) checks YAML side; handler-side enforced via the guard `if (!tenant_id) throw`. |
| T-03-09 | Elevation of Privilege | Non-manager (member role) calls CreateShiftSlot | mitigate | Role guard at top of handler: `if (!is_admin && !roles.includes('team_manager')) throw`. Test 4 from Plan 03-01's rebuilt ui-smoke-phase2 covers the forged-payload pen-test pattern (B4) for this class of bypass attempt. |
| T-03-10 | Elevation of Privilege | Team-manager A creates slot for team B (cross-team forgery) | mitigate | Layer-4 scope check: membership-join requires the caller to have `role='team_manager'` on `org_unit_id=team_id`; cross-team team_id returns no rows from the SELECT, handler throws. |
| T-03-11 | Information Disclosure | shift_slot query inadvertently exposes cross-tenant rows via JOIN to org_unit | mitigate | Every SELECT in team_detail page bound to KnexRawTenant — RLS Layer 5 backstops; explicit `WHERE tenant_id = :tenant_id` predicate in every query. |
| T-03-12 | Tampering | ApplyShiftTemplate called twice for the same team (race) | mitigate | Pre-check `SELECT 1 FROM shift_slot WHERE tenant_id AND team_id` inside the same TX; if any row exists, refuse. Two concurrent calls: at most one wins; the second sees the first's rows and refuses. |
| T-03-SC | Tampering | No new npm packages added | n/a | Plan adds zero npm packages. |
</threat_model>

<verification>
- 4 handlers exist with all 5 structural markers (default export, .schema, .connectionType, .meta, withTenantTx)
- ApplyShiftTemplate uses the exact PRD §7.4 Hebrew names (`בוקר`/`לילה` for 2x12h; `בוקר`/`ערב`/`לילה` for 3x8h); never `צהריים`
- shift_slot_form.yaml + template_wizard.yaml exist and are referenced by team_detail.yaml
- team_detail.yaml has a new משמרות tab with toolbar + AgGrid + Modal refs
- tenant_id bound via `_user: tenant_id` everywhere in team_detail's new YAML (forbidden as input)
- shift-slot-crud.spec.ts has 6 UI-driven tests; passes against live hpg5 or skips cleanly
- Lowdefy rebuilt on hpg5; container healthy; build-emitted connections.js intact
</verification>

<success_criteria>
- A team manager can navigate to team_detail → משמרות tab → fill the slot form → save → see the new row in the grid; a direct PG SELECT confirms tenant_id matches the session
- A team manager can apply the 2x12h template; two slots with exact PRD Hebrew names appear; org_unit.template_picked_at populates; the wizard button disappears
- An admin attempting to DELETE a slot referenced by shift_instances gets a Hebrew error toast; the slot remains
- The Phase 03 handler-registration verifier acknowledges the 4 new handlers (full verifier-clean is gated to Plan 03-06 completion when all 12 land)
</success_criteria>

<output>
Create `.planning/phases/03-availability-rules/03-03-SUMMARY.md` when done — document: (a) the 4 handlers' commit hashes, (b) the hpg5 rebuild timestamp, (c) the Playwright run outcome (6 tests pass/skip), (d) any Hebrew label corrections discovered against the YAML.
</output>

---
phase: 03-availability-rules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/0014_phase3_denorms.up.sql
  - db/migrations/0014_phase3_denorms.down.sql
  - app/plugins/shifty-plugin/src/auth/callbacks.js
  - app/plugins/shifty-plugin/src/types.js
  - app/plugins/shifty-plugin/src/connections/Knex/Knex.js
  - app/plugins/shifty-plugin/src/helpers/availability-source.js
autonomous: false
requirements:
  - AVAL-01
  - AVAL-04
  - SHFT-01
  - SHFT-02
  - SHFT-06

must_haves:
  truths:
    - "Migration 0014 (`0014_phase3_denorms.up.sql`) is applied on hpg5 via the existing `migrate/migrate` golang-migrate compose service (NOT manually via psql). The migration adds two columns: `availability.planning_window_id UUID REFERENCES planning_window(id) ON DELETE CASCADE NOT NULL` (with backfill UPDATE from `shift_instance.planning_window_id`; Phase 02 closes with zero availability rows so the backfill is effectively a no-op but the UPDATE is idempotent), and `org_unit.template_picked_at TIMESTAMPTZ` (no backfill — NULL means wizard has not run for that team). A composite index `idx_availability_window_soldier ON availability(planning_window_id, soldier_id)` supports the hot read path in RESEARCH Recipe 6."
    - "A companion `0014_phase3_denorms.down.sql` reverses both columns (DROP INDEX → ALTER TABLE DROP COLUMN). The down migration is structural-only; it does NOT attempt to repopulate dropped data because the columns are denorms reachable via JOIN."
    - "`schema_migrations.version` advances from 13 → 14 with `dirty=false`. Verified via the same psql probe shape Phase 02 used: `docker compose exec -T postgres psql -U shifts -d shifts -c 'SELECT version, dirty FROM schema_migrations ORDER BY version DESC LIMIT 1'`."
    - "ShiftySessionCallback (in `app/plugins/shifty-plugin/src/auth/callbacks.js`) is extended to populate `session.user.soldier_id` by looking up the `soldier` row whose `user_id = session.user.user_id AND tenant_id = session.tenant_id`. The lookup runs inside a `SET ROLE NONE` block (same pattern as the existing tenant_id lookup) because it precedes the per-request tenant context. NULL `soldier_id` is acceptable (e.g., admin who hasn't been added as a soldier) — downstream handlers must guard against it. The session shape is now `{ tenant_id, user_id, soldier_id, roles, team_ids, locale }`. This closes RESEARCH Pitfall 4 (`_user.soldier_id` not in session shape)."
    - "`app/plugins/shifty-plugin/src/helpers/availability-source.js` exports the canonical `SOURCE_RANK` enum (`{ manager_override: 3, per_slot: 2, range_blockout: 1, default: 0 }`) and `SOURCE_VALUES` (the array of keys). This is the single source of truth that Plan 03-05's DeclareAvailability handler imports for ON-CONFLICT precedence AND that Plan 03-07's KnexRawTenant read-query CASE expression must mirror. Risk R-03-3 (precedence drift) mitigated by having one constant; a unit test in Plan 03-05 asserts the CASE expression string matches the enum order."
    - "`app/plugins/shifty-plugin/src/types.js` is extended with the 11 new Phase 03 request type names: `CreateShiftSlot`, `UpdateShiftSlot`, `DeleteShiftSlot`, `OpenPlanningWindow`, `EditPlanningWindow`, `DeletePlanningWindow`, `ApplyShiftTemplate`, `DeclareAvailability`, `UpsertRule`, `UpsertRuleOverride`, `ResetRuleOverride`, `SeedTeamRules`. The total registered request count becomes 21 (9 Phase 02 + 12 Phase 03). Listed in the `requests:` array in the order documented in RESEARCH Recipe 1."
    - "`app/plugins/shifty-plugin/src/connections/Knex/Knex.js` is extended with the 12 new handler imports and they are spread into the merged `requests:` map alongside the upstream Knex requests and the 9 Phase 02 handlers. The 12 handler FILES do not yet exist at the end of this plan — they are created in Plans 03-03 through 03-06. The Knex.js imports referencing yet-to-exist files compile-fail until those plans land; this is acceptable because the rebuilt Lowdefy container is not deployed to hpg5 until Plan 03-07. The verifier `tools/check-handler-registration.mjs` (from Plan 03-01) is NOT run in this plan because handlers don't exist yet; it runs at the end of each downstream plan that adds handlers and at Plan 03-08 closeout."
    - "Manual user verification confirms the post-migration schema on hpg5 via psql: `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'availability' AND column_name = 'planning_window_id'` returns one row with `uuid NOT NULL`, and the same probe on `org_unit.template_picked_at` returns `timestamptz` (NULL allowed)."
  artifacts:
    - path: db/migrations/0014_phase3_denorms.up.sql
      provides: "Forward migration adding availability.planning_window_id (NOT NULL + index) + org_unit.template_picked_at"
      contains: "planning_window_id"
    - path: db/migrations/0014_phase3_denorms.down.sql
      provides: "Reverse migration dropping both columns + index"
      contains: "DROP COLUMN"
    - path: app/plugins/shifty-plugin/src/auth/callbacks.js
      provides: "ShiftySessionCallback now exposes soldier_id on session.user"
      contains: "soldier_id"
    - path: app/plugins/shifty-plugin/src/types.js
      provides: "Extended requests array listing all 12 new Phase 03 type names"
      contains: "CreateShiftSlot"
    - path: app/plugins/shifty-plugin/src/connections/Knex/Knex.js
      provides: "Extended requests map importing + spreading all 12 new handler symbols"
      contains: "CreateShiftSlot"
    - path: app/plugins/shifty-plugin/src/helpers/availability-source.js
      provides: "Canonical SOURCE_RANK enum + SOURCE_VALUES — single source of truth for precedence ordering"
      contains: "SOURCE_RANK"
  key_links:
    - from: db/migrations/0014_phase3_denorms.up.sql
      to: db/migrations/0009_rls_policies.up.sql
      via: "0014 adds a column to an RLS-enabled table (availability); RLS policy auto-applies to writes once the column is in place — no policy change needed because the existing tenant_id predicate is unchanged"
      pattern: "availability"
    - from: app/plugins/shifty-plugin/src/auth/callbacks.js
      to: app/plugins/shifty-plugin/src/hooks/with-tenant-tx.js
      via: "soldier_id lookup must run BEFORE the tenant context is set; uses the same SET ROLE NONE pattern documented in 02-UAT-FINDINGS"
      pattern: "SET ROLE NONE"
    - from: app/plugins/shifty-plugin/src/connections/Knex/Knex.js
      to: app/plugins/shifty-plugin/src/types.js
      via: "Every handler name in types.js requests:[] MUST be imported and spread into Knex.js requests:{} — enforced by tools/check-handler-registration.mjs (Plan 03-01)"
      pattern: "CreateShiftSlot"
    - from: app/plugins/shifty-plugin/src/helpers/availability-source.js
      to: app/plugins/shifty-plugin/src/connections/Knex/requests/DeclareAvailability.js
      via: "DeclareAvailability (created in Plan 03-05) imports SOURCE_RANK to drive ON CONFLICT WHERE precedence logic"
      pattern: "SOURCE_RANK"
---

<objective>
Land the Wave-0 prerequisites that every Phase 03 product plan depends on but no product plan owns by itself: migration 0014 (denorm columns), the session-callback extension (`soldier_id` on session.user), the canonical `SOURCE_RANK` helper, and the types.js + Knex.js skeleton wiring for the 12 new Phase 03 request handlers.

Three separate concerns are bundled because they share no file ownership conflict, all individually take <10% context, and each one independently blocks downstream plans (no point shipping in isolation). Migration 0014 must be applied BEFORE any handler reads/writes `availability.planning_window_id`. The session callback extension must happen BEFORE any my_availability page resolves `_user: soldier_id`. The SOURCE_RANK helper must exist BEFORE DeclareAvailability + the read-query CASE expression. The types.js + Knex.js skeleton must exist BEFORE 03-03 through 03-06 can land their handler files without each plan re-editing the same two files (file-ownership conflict resolution: 03-02 owns both files exclusively; downstream plans add only handler files in `src/connections/Knex/requests/`).

This plan is autonomous=false because it includes a `checkpoint:human-verify` for the migration deploy step — applying a NOT NULL column with backfill is the kind of operation that warrants an eyes-on confirmation against hpg5 before downstream plans build on it.

Purpose: Unblock Phase 03 handler work by establishing the schema, session, helper, and registry baseline.
Output: 1 migration pair (up+down), 1 callback edit, 1 new helper file, 2 plugin-registry file edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-availability-rules/03-CONTEXT.md
@.planning/phases/03-availability-rules/03-RESEARCH.md
@CLAUDE.md
@db/migrations/0009_rls_policies.up.sql
@db/migrations/0011_role_tag.up.sql
@db/migrations/0012_org_unit_last_color_index.up.sql
@db/migrations/0013_layer5_rls_app_role.up.sql
@app/plugins/shifty-plugin/src/auth/callbacks.js
@app/plugins/shifty-plugin/src/types.js
@app/plugins/shifty-plugin/src/connections/Knex/Knex.js
@app/plugins/shifty-plugin/src/hooks/with-tenant-tx.js

<interfaces>
Migration convention (Phase 01-Plan 01 decision, recorded in STATE.md):
  golang-migrate requires `.up.sql` and `.down.sql` suffixes.
  Apply via: `docker compose run --rm migrate up` (the YAML `command:` already includes `up`).
  Run from hpg5 via PsExec (because pull/build may invoke credential helper).

Existing ShiftySessionCallback shape (from 02-11 Knex.js merge):
  Reads JWT → looks up app_user.tenant_id (with SET ROLE NONE because pre-tenant)
  Looks up memberships → roles[], team_ids[]
  Returns session.user = { user_id, tenant_id, roles, team_ids, locale }
  EXTENSION: add soldier_id by querying `SELECT id FROM soldier WHERE user_id = :user_id AND tenant_id = :tenant_id LIMIT 1` (same SET ROLE NONE block; soldier may not exist for admin-only users → NULL allowed).

Existing types.js shape (current 21 entries after 02-11):
  connections: ['Knex']
  requests: ['KnexRawTenant', 'AuditWrite', 'ParseCsvAndValidate', 'CommitRosterImport',
             'CreateSoldier', 'UpdateSoldier', 'ArchiveSoldier', 'CreateMembership', 'InviteLater']
  auth: { adapters: ['KnexAdapter'], callbacks: ['ShiftySessionCallback'], providers: ['EmailProvider'] }

Existing Knex.js shape:
  import * as upstream from '@lowdefy/connection-knex/connections';
  import KnexRawTenant from './requests/KnexRawTenant.js';
  ... (9 imports)
  export default {
    schema: upstream.Knex.schema,
    requests: { ...upstream.Knex.requests, KnexRawTenant, AuditWrite, ... }
  };
  EXTENSION: add 12 more imports + 12 more entries in the spread map.

Phase 03 handler names (RESEARCH Recipe 1):
  CreateShiftSlot, UpdateShiftSlot, DeleteShiftSlot
  OpenPlanningWindow, EditPlanningWindow, DeletePlanningWindow
  ApplyShiftTemplate
  DeclareAvailability
  UpsertRule, UpsertRuleOverride, ResetRuleOverride
  SeedTeamRules
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author migration 0014 up + down + apply on hpg5</name>
  <files>
    db/migrations/0014_phase3_denorms.up.sql,
    db/migrations/0014_phase3_denorms.down.sql
  </files>
  <action>
    Write `db/migrations/0014_phase3_denorms.up.sql` per RESEARCH §"Migration 0014 design". Structure: BEGIN; ALTER TABLE availability ADD COLUMN IF NOT EXISTS planning_window_id UUID REFERENCES planning_window(id) ON DELETE CASCADE; UPDATE availability av SET planning_window_id = si.planning_window_id FROM shift_instance si WHERE av.shift_instance_id = si.id AND av.planning_window_id IS NULL; ALTER TABLE availability ALTER COLUMN planning_window_id SET NOT NULL; CREATE INDEX IF NOT EXISTS idx_availability_window_soldier ON availability(planning_window_id, soldier_id); ALTER TABLE org_unit ADD COLUMN IF NOT EXISTS template_picked_at TIMESTAMPTZ; COMMIT;

    Write `db/migrations/0014_phase3_denorms.down.sql` as the symmetric reverse: BEGIN; DROP INDEX IF EXISTS idx_availability_window_soldier; ALTER TABLE availability DROP COLUMN IF EXISTS planning_window_id; ALTER TABLE org_unit DROP COLUMN IF EXISTS template_picked_at; COMMIT.

    The migration is structural-only with a single backfill UPDATE; the UPDATE is idempotent (re-runs are no-ops because the `planning_window_id IS NULL` predicate excludes rows already backfilled). Phase 02 closes with zero `availability` rows in production data, so the backfill UPDATE on hpg5 affects zero rows in practice — but the SQL must still be present for future re-runs against environments that may have populated the column some other way.

    Apply the migration on hpg5: commit + push, then on hpg5 run `git fetch origin main && git reset --hard origin/main` (per CLAUDE.md "Deploy sync"), then run the migrate compose service. The migrate service does NOT require PsExec normally (it doesn't pull new images), but if the image isn't cached locally it falls back to a registry pull which DOES require PsExec — wrap defensively. Exact command (from hpg5 over plink): `docker compose -f C:\shifts-manager\docker-compose.yml run --rm migrate up`. Confirm post-state via `docker compose -f C:\shifts-manager\docker-compose.yml exec -T postgres psql -U shifts -d shifts -c "SELECT version, dirty FROM schema_migrations ORDER BY version DESC LIMIT 1"` → expects `14 | f`.

    Pre-flight assertion before applying: `docker compose exec -T postgres psql -U shifts -d shifts -c "\\d availability"` to confirm `planning_window_id` does NOT yet exist (Assumption A4 from RESEARCH). If it already exists, the migration's IF NOT EXISTS clauses make it safe but inspect to understand why.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const up=fs.readFileSync('db/migrations/0014_phase3_denorms.up.sql','utf8'); const down=fs.readFileSync('db/migrations/0014_phase3_denorms.down.sql','utf8'); for (const tok of ['ALTER TABLE availability','ADD COLUMN','planning_window_id','REFERENCES planning_window','SET NOT NULL','idx_availability_window_soldier','template_picked_at']) { if (!up.includes(tok)) { console.error('up.sql missing token:', tok); process.exit(1); } } for (const tok of ['DROP COLUMN','DROP INDEX','planning_window_id','template_picked_at']) { if (!down.includes(tok)) { console.error('down.sql missing token:', tok); process.exit(2); } } if (!up.match(/UPDATE\\s+availability/i)) { console.error('up.sql missing backfill UPDATE'); process.exit(3); } if (!up.includes('BEGIN') || !up.includes('COMMIT')) { console.error('up.sql must wrap in BEGIN/COMMIT'); process.exit(4); } console.log('OK Task 1 — migration 0014 structure');"</automated>
  </verify>
  <done>0014_phase3_denorms.up.sql + .down.sql exist; up contains ALTER + UPDATE backfill + NOT NULL + index + COMMIT; down contains symmetric DROPs; both wrapped in transactions; migration applied on hpg5 via `docker compose run --rm migrate up`; schema_migrations.version=14, dirty=false.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify migration 0014 applied correctly on hpg5</name>
  <what-built>Migration 0014 was authored locally and applied to the hpg5 Postgres via the migrate compose service. The schema now has `availability.planning_window_id UUID NOT NULL REFERENCES planning_window(id)`, the supporting index `idx_availability_window_soldier`, and `org_unit.template_picked_at TIMESTAMPTZ` nullable.</what-built>
  <how-to-verify>
1. SSH to hpg5: `plink -ssh -l claude -pw "Onclaude2103" -batch -hostkey "SHA256:tPg5mYQbJO/9ccGmNGeyJeQQSPXq+C6SL3EHJcbRZMQ" hpg5 "powershell -c \"docker compose -f C:\shifts-manager\docker-compose.yml exec -T postgres psql -U shifts -d shifts -c 'SELECT version, dirty FROM schema_migrations ORDER BY version DESC LIMIT 1'\""` → expect: `version | dirty` row showing `14 | f`.
2. Same plink call with `-c '\\d availability'` → expect the column listing to show `planning_window_id | uuid | not null` and the FK reference to planning_window.
3. Same plink call with `-c '\\d org_unit'` → expect `template_picked_at | timestamp with time zone | nullable`.
4. Same plink call with `-c 'SELECT count(*) FROM availability WHERE planning_window_id IS NULL'` → expect `0` (backfill complete; Phase 02 has zero availability rows so trivially satisfied).

Reply with "approved" if all four checks pass, or describe any mismatch (column missing, NOT NULL not applied, FK pointing wrong target, version not advanced).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend ShiftySessionCallback to populate session.user.soldier_id</name>
  <files>app/plugins/shifty-plugin/src/auth/callbacks.js</files>
  <action>
    Read the existing `ShiftySessionCallback` in `app/plugins/shifty-plugin/src/auth/callbacks.js` to confirm its current shape (RESEARCH Pitfall 4 + Assumption A3 + 02-11-SUMMARY both reference it). The callback currently builds `session.user` with `{ user_id, tenant_id, roles, team_ids, locale }`. Extend it to additionally query `SELECT id FROM soldier WHERE user_id = :user_id AND tenant_id = :tenant_id LIMIT 1` inside the same SET ROLE NONE block that performs the tenant_id lookup. Append `soldier_id` to the session.user object — set to the returned id, or `null` if no soldier row matches (e.g., a unit_admin who has not been added to any roster). Preserve every existing field and the existing SET ROLE NONE / SET ROLE shifty_app dance documented in 02-UAT-FINDINGS §"Test bugs (resolved)" (commit `54cde4d`). The change is one additional SELECT inside the existing knex transaction + one additional property on the returned object. Add a leading-of-function JSDoc note describing the soldier_id semantics: "soldier_id is the soldier row id whose user_id matches the authenticated app_user; NULL for users who are not soldiers (admins without roster membership). Phase 03 my_availability uses this for `_user: soldier_id` payload binding."
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('app/plugins/shifty-plugin/src/auth/callbacks.js','utf8'); if (!t.includes('soldier_id')) { console.error('callbacks.js does not reference soldier_id'); process.exit(1); } if (!t.match(/FROM\\s+soldier/i)) { console.error('callbacks.js missing soldier table query'); process.exit(2); } if (!t.includes('SET ROLE NONE')) { console.error('callbacks.js must preserve the SET ROLE NONE pattern from 02-11'); process.exit(3); } const userObjMatch=t.match(/user\\s*:\\s*\\{[^}]*\\}/m); if (userObjMatch &amp;&amp; !userObjMatch[0].includes('soldier_id')) { console.error('soldier_id may not be wired into the returned user object'); process.exit(4); } console.log('OK Task 3 — ShiftySessionCallback extended');"</automated>
  </verify>
  <done>callbacks.js queries `soldier` table for the authenticated user inside the existing SET ROLE NONE block; appends `soldier_id` (or null) to session.user; preserves all existing fields; JSDoc note added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create SOURCE_RANK helper + extend types.js + Knex.js with 12 new handler symbols</name>
  <files>
    app/plugins/shifty-plugin/src/helpers/availability-source.js,
    app/plugins/shifty-plugin/src/types.js,
    app/plugins/shifty-plugin/src/connections/Knex/Knex.js
  </files>
  <action>
    File 1 — Create `app/plugins/shifty-plugin/src/helpers/availability-source.js` exporting `SOURCE_RANK` (frozen Object `{ manager_override: 3, per_slot: 2, range_blockout: 1, default: 0 }` via `Object.freeze`) and `SOURCE_VALUES` (`Object.keys(SOURCE_RANK)`). Leading comment documents the precedence ordering and warns that DeclareAvailability + the my_availability read-query CASE expression must both stay in sync with this constant (Risk R-03-3). Export both as named exports plus a default export of the SOURCE_RANK object for ergonomic import.

    File 2 — Extend `app/plugins/shifty-plugin/src/types.js` `requests:` array. Append (in this order, matching RESEARCH Recipe 1's documented order): `'CreateShiftSlot'`, `'UpdateShiftSlot'`, `'DeleteShiftSlot'`, `'OpenPlanningWindow'`, `'EditPlanningWindow'`, `'DeletePlanningWindow'`, `'ApplyShiftTemplate'`, `'DeclareAvailability'`, `'UpsertRule'`, `'UpsertRuleOverride'`, `'ResetRuleOverride'`, `'SeedTeamRules'`. The final array has 21 entries (9 Phase 02 + 12 Phase 03). Preserve the existing `connections: ['Knex']` and `auth: {...}` declarations verbatim.

    File 3 — Extend `app/plugins/shifty-plugin/src/connections/Knex/Knex.js` with 12 new imports and 12 new spread entries. Import each handler from `./requests/<Name>.js` (the FILES don't yet exist; this is intentional skeleton wiring that downstream plans fill in). Add each name to the merged `requests:` map after the 9 Phase 02 entries. The final Knex.js exports `{ schema: upstream.Knex.schema, requests: { ...upstream.Knex.requests, KnexRawTenant, AuditWrite, ParseCsvAndValidate, CommitRosterImport, CreateSoldier, UpdateSoldier, ArchiveSoldier, CreateMembership, InviteLater, CreateShiftSlot, UpdateShiftSlot, DeleteShiftSlot, OpenPlanningWindow, EditPlanningWindow, DeletePlanningWindow, ApplyShiftTemplate, DeclareAvailability, UpsertRule, UpsertRuleOverride, ResetRuleOverride, SeedTeamRules } }`. The file will not parse as a valid ES module on disk until the 12 handler files exist (downstream plans); that's fine — Lowdefy is not rebuilt in this plan. The structural verifier (Plan 03-01) is NOT run after this task because it would correctly fail (handlers missing). The verifier runs at the end of each downstream plan that adds handlers, AND at Plan 03-08 closeout.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const helper=fs.readFileSync('app/plugins/shifty-plugin/src/helpers/availability-source.js','utf8'); for (const tok of ['SOURCE_RANK','SOURCE_VALUES','manager_override','per_slot','range_blockout','default']) { if (!helper.includes(tok)) { console.error('availability-source.js missing token:', tok); process.exit(1); } } if (!helper.includes('Object.freeze')) { console.error('SOURCE_RANK should be frozen'); process.exit(2); } const types=fs.readFileSync('app/plugins/shifty-plugin/src/types.js','utf8'); const newNames=['CreateShiftSlot','UpdateShiftSlot','DeleteShiftSlot','OpenPlanningWindow','EditPlanningWindow','DeletePlanningWindow','ApplyShiftTemplate','DeclareAvailability','UpsertRule','UpsertRuleOverride','ResetRuleOverride','SeedTeamRules']; for (const n of newNames) { if (!types.includes(n)) { console.error('types.js missing handler name:', n); process.exit(3); } } const knex=fs.readFileSync('app/plugins/shifty-plugin/src/connections/Knex/Knex.js','utf8'); for (const n of newNames) { const importRe=new RegExp('import\\\\s+'+n+'\\\\s+from\\\\s+[\"\\']\\\\.\\\\/requests\\\\/'+n+'\\\\.js[\"\\']'); if (!importRe.test(knex)) { console.error('Knex.js missing import for:', n); process.exit(4); } if (!knex.includes(n+',')) { console.error('Knex.js requests map missing entry for:', n); process.exit(5); } } console.log('OK Task 4 — helper + types + Knex.js extended');"</automated>
  </verify>
  <done>availability-source.js exports frozen SOURCE_RANK enum + SOURCE_VALUES; types.js requests array has all 21 entries (9 + 12); Knex.js has 12 new imports and 12 new spread entries; no existing entries removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrate compose service → Postgres | Runs as `migrator` superuser bypassing RLS; safe for schema-level changes only. |
| ShiftySessionCallback → soldier table | Read inside SET ROLE NONE which is bootstrap superuser; bypasses RLS for the read. Tenant context is set AFTER this lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Migration 0014 misapplies (e.g., wrong FK target) | mitigate | Pre-flight `\d availability` inspection + post-apply schema verification (Task 2 human checkpoint) catches structural errors before downstream plans build on them. |
| T-03-06 | Information Disclosure | ShiftySessionCallback's soldier_id lookup runs without RLS | accept | The query is parameterized on session-derived `user_id` + `tenant_id`; cannot leak across tenants. Same pattern as the existing tenant_id lookup that 02-11 closed without issue. |
| T-03-07 | Elevation of Privilege | Manually-crafted JWT with forged tenant_id reaches the callback | mitigate | JWT is signed by NEXTAUTH_SECRET; tampering invalidates the signature; the existing Phase 1 validation chain (Auth.js) rejects forged tokens before the callback runs. |
| T-03-SC | Tampering | No new npm packages added in this plan | n/a | Zero npm installs; no [ASSUMED]/[SUS] packages exist. |
</threat_model>

<verification>
- Migration 0014 up + down files exist with the structure documented in RESEARCH §"Migration 0014 design"
- hpg5 schema reflects the migration (Task 2 human checkpoint confirms)
- ShiftySessionCallback returns soldier_id on session.user (null acceptable for non-soldiers)
- availability-source.js exports a frozen SOURCE_RANK constant + SOURCE_VALUES
- types.js has 21 request type entries (9 Phase 02 + 12 Phase 03)
- Knex.js has 12 new imports + 12 new spread entries; existing entries preserved
- Downstream plans (03-03 through 03-06) can each add their handler file in `src/connections/Knex/requests/` without touching types.js or Knex.js (file-ownership clean)
</verification>

<success_criteria>
- migrate/migrate compose service applied 0014 on hpg5; schema_migrations.version=14 / dirty=false
- Session.user includes soldier_id (or null) for every authenticated request
- SOURCE_RANK is the single source of truth for availability precedence
- types.js + Knex.js have skeleton wiring for all 12 Phase 03 handlers
- Phase 03 handler plans (03-03..03-06) can add files in `src/connections/Knex/requests/` exclusively
</success_criteria>

<output>
Create `.planning/phases/03-availability-rules/03-02-SUMMARY.md` when done — document: (a) the migrate run output (timestamp + schema_migrations row), (b) the Task 2 human checkpoint approval, (c) any deviations from the planned column shape.
</output>

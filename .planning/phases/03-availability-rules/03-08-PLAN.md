---
phase: 03-availability-rules
plan: 08
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06, 03-07]
files_modified:
  - .planning/phases/03-availability-rules/03-VERIFICATION.md
  - .planning/phases/03-availability-rules/03-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SHFT-01
  - SHFT-02
  - SHFT-03
  - SHFT-04
  - SHFT-05
  - SHFT-06
  - SHFT-07
  - AVAL-01
  - AVAL-02
  - AVAL-03
  - AVAL-04
  - AVAL-05
  - AVAL-06
  - AVAL-07
  - AVAL-08
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
  - RULE-06
  - RULE-07

must_haves:
  truths:
    - "`.planning/phases/03-availability-rules/03-VERIFICATION.md` exists with per-requirement evidence: every Phase 03 REQ id from REQUIREMENTS.md (SHFT-01..07 minus none + AVAL-01..08 minus AVAL-09 (Phase 06) + RULE-01..07) has a documented status (PASS / DEFERRED / BLOCKED) with a test or commit reference. AVAL-09 explicitly noted as 'deferred to Phase 06 per traceability matrix — table flag + trigger logic shipped in Phase 03 but the dispatcher firing the notification is Phase 06's responsibility'. RULE-06 noted as 'rule_key surface in DB; Fri+Sat hardcoded in Phase 04 solver — Phase 03 surface complete'."
    - "`.planning/phases/03-availability-rules/03-SUMMARY.md` exists summarizing Phase 03's deliverables: (a) 1 migration pair (0014) with both denorm columns active on hpg5; (b) 12 new shifty-plugin request handlers registered and verifier-clean; (c) 6 new YAML pages (planning_windows.yaml, planning_window_open_form, my_availability, planning_window_detail, plus team_detail + soldier_detail tab extensions); (d) 5 new reusable blocks (shift_slot_form, template_wizard, planning_window_open_form, availability_date_row, rules_form, rule_override_row); (e) 7 new test specs (5 e2e rebuilt for P02-HF-05 + new e2e for shift-slot-crud + planning-window-open + availability-declare + team-rules + rule-override + planning-window-detail) + 2 integration specs (availability-source-precedence + seed-team-rules) + 1 unit spec (rule-override-tightening); (f) 1 new ops tool (tools/check-handler-registration.mjs); (g) the P02-HF-05 21-spec rebuild closes that deferral. Total Phase 03 commits: ≥1 per plan = ≥7 commits."
    - "`.planning/ROADMAP.md` Phase 3 row in the 'Progress' table flips from `0/8 | Not started | -` to `8/8 | Complete | 2026-MM-DD`. The Phase 3 'Plans:' list under '### Phase 3: Availability & Rules' is populated with the 8 plan filenames + brief objectives (matching the format used by completed Phase 2 entry). The Phase 4 row remains 'Not started' (no spillover)."
    - "`.planning/STATE.md` 'Current Position' updated: Phase=03 (availability-rules) — COMPLETE; Next phase: 04 — Solver & Schedule. `progress.completed_phases` increments from 1 to 3 (Phases 1 + 2 + 3 complete) — verify against existing tracking. `last_activity` updated to the closeout date."
    - "A git tag `v0.3.0-phase3` is created and pushed to origin/main, marking the Phase 03 close. Tag message includes a short summary of the phase's load-bearing deliverables (handlers added + pages added + the verifier gate). Tag commit is the merge commit / last commit of Plan 03-07."
    - "hpg5 is synced to the post-tag HEAD: `git fetch origin main && git reset --hard origin/main` run via plink; Lowdefy rebuilt via PsExec; container healthy via `docker ps --filter name=shifty-lowdefy --format '{{.Status}}'` returns 'Up ... (healthy)'."
    - "Full test suite gate: running `pnpm exec playwright test && node --test --experimental-strip-types tests/unit/*.spec.ts && node --test --experimental-strip-types tests/integration/*.spec.ts` returns zero failures (skips acceptable when stack unreachable). The cross-tenant-leak.spec.ts auto-discovery suite (Phase 01 inherited) automatically picks up the 4 new Phase 03 pages (planning_windows, planning_window_open_form is a block not a page, my_availability, planning_window_detail) and asserts each one's cross-tenant block — 17 → ≥19 tests after auto-discovery."
    - "RTL smoke verification: manually navigate (or via Playwright a single test) `/my_availability/{any_pw_id}` on a 320px viewport (Playwright `viewport: { width: 320, height: 800 }`) and assert no horizontal scroll. The viewport test is owned by `tests/e2e/availability-declare.spec.ts` (Plan 03-05) if not already present, OR added here as a final QA check task."
    - "tools/check-handler-registration.mjs exits 0 against the final state: 9 Phase 02 handlers + 12 Phase 03 handlers = 21 handlers; types.js requests array has 21 entries; Knex.js imports + spreads all 21. Output: `OK 21/21 handlers registered correctly`."
    - "`.planning/RETROSPECTIVE.md` (project-level cross-milestone trends doc — if exists) gets a Phase 03 entry appended: what worked (recipes-first Phase, low context burn; the 02-11 retrospective recommendations all landed), what didn't (Lowdefy mobile media-query support unverified at Plan-write time — Open Question 6 carry-forward), key lessons (the structural verifier is now the canonical pre-merge gate). If RETROSPECTIVE.md does not exist, skip this step (it's outside Phase 03's mandate to create; project init owns it)."
  artifacts:
    - path: .planning/phases/03-availability-rules/03-VERIFICATION.md
      provides: "Per-requirement evidence table for Phase 03 closeout"
      contains: "SHFT-01"
    - path: .planning/phases/03-availability-rules/03-SUMMARY.md
      provides: "Phase 03 deliverables summary + tag + retrospective links"
      contains: "v0.3.0-phase3"
    - path: .planning/ROADMAP.md
      provides: "Phase 3 row in progress table flipped to Complete; plans list populated"
      contains: "8/8"
    - path: .planning/STATE.md
      provides: "Current position updated to Phase 04 — Solver & Schedule"
      contains: "04"
  key_links:
    - from: .planning/phases/03-availability-rules/03-SUMMARY.md
      to: .planning/phases/03-availability-rules/03-VERIFICATION.md
      via: "SUMMARY references VERIFICATION for the per-requirement evidence detail"
      pattern: "VERIFICATION"
    - from: .planning/ROADMAP.md
      to: .planning/phases/03-availability-rules/
      via: "Phase 3 plans-list rows link to each 03-NN-SUMMARY.md"
      pattern: "03-availability-rules"
    - from: .planning/STATE.md
      to: .planning/ROADMAP.md
      via: "STATE.md current position cross-references the ROADMAP row state"
      pattern: "03"
---

<objective>
Close Phase 03 with the standard GSD closeout artifacts: 03-VERIFICATION.md (per-requirement evidence), 03-SUMMARY.md (deliverables summary), ROADMAP.md row flipped to Complete, STATE.md current position advanced, v0.3.0-phase3 git tag pushed, hpg5 synced to the tagged HEAD with Lowdefy rebuilt and healthy.

This plan does no new feature work. Its purpose is to perform the closeout ritual that GSD requires at every phase boundary, produce the audit trail Phase 04 (Solver & Schedule) needs to start from a known-good baseline, and surface any deferred or carry-forward items to the orchestrator for explicit acknowledgment.

The plan is autonomous=false because the closeout includes a checkpoint:human-verify task — the human signs off on the full Phase 03 deliverable surface before the tag is pushed and the ROADMAP flipped. This is the same pattern Phase 02 used (Plan 02-11 Tasks 7-8 deferral + Closeout section in 02-11-SUMMARY).

Purpose: Phase 03 closes cleanly; Phase 04 starts from a tagged baseline; ROADMAP + STATE reflect the new current position; all Phase 03 requirements have documented evidence.
Output: 2 new closeout docs + 2 cross-cutting doc updates + 1 git tag + 1 hpg5 sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-org-people/02-VERIFICATION.md
@.planning/phases/02-org-people/02-11-SUMMARY.md
@.planning/phases/03-availability-rules/03-CONTEXT.md
@.planning/phases/03-availability-rules/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 03-VERIFICATION.md with per-requirement evidence + sanity-check the full test suite</name>
  <files>.planning/phases/03-availability-rules/03-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/03-availability-rules/03-VERIFICATION.md` following the 02-VERIFICATION.md template shape. Header: "# Phase 03: Availability & Rules — Verification" + "**Status:** PASSED" + "**Date:** YYYY-MM-DD" + "**Test suite outcome:** {N tests passed / 0 failed / M skipped against hpg5 at commit <hash>}".

    Sections:

    1. "## Requirement evidence" — a table with 22 rows (the Phase 03 requirements from REQUIREMENTS.md traceability minus AVAL-09 which is Phase 06): SHFT-01..07, AVAL-01..08, RULE-01..07. Each row: REQ id | brief description | status (PASS / PARTIAL / DEFERRED) | evidence (test file + line OR commit hash + file). For PARTIAL/DEFERRED rows, explain why. AVAL-09 has its own footnote noting it's Phase 06.

    2. "## Test outcomes" — list every Phase 03 spec file (e2e + unit + integration) with its test count and pass count. Format:
       - `tests/e2e/shift-slot-crud.spec.ts`: 6/6 pass
       - `tests/e2e/planning-window-open.spec.ts`: 6/6 pass
       - `tests/e2e/availability-declare.spec.ts`: 7/7 pass
       - `tests/e2e/team-rules.spec.ts`: 4/4 pass
       - `tests/e2e/rule-override.spec.ts`: 5/5 pass
       - `tests/e2e/planning-window-detail.spec.ts`: 6/6 pass
       - `tests/e2e/soldier-crud.spec.ts` (rebuilt): 5/5 pass (P02-HF-05 closure)
       - `tests/e2e/roster-csv-import.spec.ts` (rebuilt): 5/5 pass
       - `tests/e2e/org-unit-crud.spec.ts` (rebuilt): 4/4 pass
       - `tests/e2e/tenant-isolation.spec.ts` (rebuilt): 2/2 pass
       - `tests/e2e/ui-smoke-phase2.spec.ts` (rebuilt): 6/6 pass
       - `tests/e2e/cross-tenant-leak.spec.ts` (auto-discovery): {final count}/X pass — 4 new Phase 03 pages picked up
       - `tests/integration/availability-source-precedence.spec.ts`: 5/5 pass
       - `tests/integration/seed-team-rules.spec.ts`: 3/3 pass
       - `tests/unit/rule-override-tightening.spec.ts`: 8/8 pass
       - Other Phase 01/02 specs (layer5-rls-activation, audit-immutable, role-gate, etc.): N/N pass
       Then a `tools/check-handler-registration.mjs` line: `OK 21/21 handlers registered correctly`.

    3. "## Migration state" — `schema_migrations.version = 14`; `dirty = false`; diff vs Phase 02 close: 0013 → 0014 (one new migration applied).

    4. "## Carry-forwards into Phase 04" — list known open items: (a) RULE-06 weekend = Fri+Sat lives in Phase 04 solver; (b) Phase 04 must read rule + rule_override rows when assembling the /solve payload; (c) availability source-precedence MUST be evaluated by reading via the LATERAL query (the SQL is in `app/pages/my_availability.yaml`'s load_availability request — Phase 04 solver-payload-builder should copy that SQL or extract it into a helper); (d) Phase 04 takes ownership of state transitions OPEN → DRAFT → PUBLISHED; (e) Plan 03-07 Test 6 (cross-team manager on planning_window_detail) documented behavior — orchestrator decides if tighter page-level scope is needed in a Phase 03.x hotfix or v1.1.

    5. "## Deferred items" — list Phase 03 deferred ideas from CONTEXT.md `<deferred>` section: shift slot per-day variants (v1.1), per-tenant weekend definition (v1.1), bulk-edit availability (v1.1), calendar-widget UI (v1.1), rule_override audit log UI (Phase 07), planning window cloning (v1.1).

    6. "## Sanity-check runs" — paste output snippets (or commit hashes referencing them) from the final test suite run + the handler-registration verifier + the hpg5 container health check.

    Run the full test suite to capture the actual numbers before writing the VERIFICATION.md (don't fabricate counts).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); if (!fs.existsSync('.planning/phases/03-availability-rules/03-VERIFICATION.md')) { console.error('MISSING 03-VERIFICATION.md'); process.exit(1); } const t=fs.readFileSync('.planning/phases/03-availability-rules/03-VERIFICATION.md','utf8'); for (const tok of ['SHFT-01','SHFT-07','AVAL-01','AVAL-08','RULE-01','RULE-07','Requirement evidence','Test outcomes','Migration state','schema_migrations','Carry-forwards','Deferred items','tools/check-handler-registration','Phase 03']) { if (!t.includes(tok)) { console.error('VERIFICATION missing:',tok); process.exit(2); } } if (t.includes('AVAL-09')) { /* must be footnoted as Phase 06 */ if (!t.match(/AVAL-09[\\s\\S]{0,500}Phase 06/i)) { console.error('AVAL-09 must be footnoted as Phase 06'); process.exit(3); } } console.log('OK Task 1 — VERIFICATION.md');"</automated>
  </verify>
  <done>03-VERIFICATION.md exists with all 22 Phase 03 requirements documented (status + evidence); test outcomes match actual run; migration state recorded; carry-forwards + deferreds listed; full suite run output captured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 03-SUMMARY.md + update ROADMAP.md + STATE.md</name>
  <files>
    .planning/phases/03-availability-rules/03-SUMMARY.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    File 1 — Create `.planning/phases/03-availability-rules/03-SUMMARY.md` following the 02-11-SUMMARY.md template shape (frontmatter + body). Frontmatter: phase: 03-availability-rules, subsystem: availability-and-rules, tags: [phase-3, availability, rules, shift-slots, planning-window, p02-hf-05-closure], requires (list the upstream artifacts — Phase 02 plugin merge, 02-11 hotfix, migration 0013 + 0014), provides (the new pages, blocks, handlers, tools, tests, tag), affects (the touched YAML pages + types.js + Knex.js), tech-stack-added: [], tech-stack-patterns (list any new patterns introduced — source-precedence ON CONFLICT WHERE + LATERAL CASE rank; tightening guard semantic comparison; multi-mode handler dispatcher), key-files-created (list every new file across all 7 plans), key-files-modified, key-files-deleted (none expected), decisions (list 3-5 load-bearing Phase 03 decisions per the decisions field in 02-11-SUMMARY — e.g., 'lazy SeedTeamRules avoids touching Phase 02 CreateTeam'; 'AG Grid master-detail Enterprise-only → Lowdefy visible: toggle fallback'; 'simple wipe + re-default on EditPlanningWindow over preservation'; 'forged-API exception pattern for server-guard-only tests'), metrics (duration estimate from STATE.md velocity), completed_date.

    Body sections:

    "# Phase 03 Plan 8: Closeout Summary"

    "## Load-bearing deliverables"
       - 1 migration (0014 with planning_window_id NOT NULL + index + template_picked_at)
       - 12 new request handlers (shift slot CRUD ×3 + planning window CRUD ×3 + ApplyShiftTemplate + DeclareAvailability + rule CRUD ×4)
       - 1 session callback extension (soldier_id on session.user)
       - 6 new pages / page edits (planning_windows, my_availability, planning_window_detail; team_detail tabs ×2; soldier_detail tab; lowdefy.yaml menu entries)
       - 5 reusable blocks (shift_slot_form, template_wizard, planning_window_open_form, availability_date_row, rules_form, rule_override_row)
       - 1 structural verifier tool (tools/check-handler-registration.mjs)
       - 11 new test specs (4 e2e new + 5 e2e rebuilt + 2 integration + 1 unit)
       - 1 fixture helper (seedFullWindow in seed-tenants.ts)

    "## P02-HF-05 closure" — explicitly note the 21 mutation specs are rebuilt as UI-driven flows; Phase 02's deferred gap is closed.

    "## What worked"
       - The recipes-first phase model — every handler had a verbatim template from RESEARCH or 02-11
       - The structural verifier (tools/check-handler-registration.mjs) caught the .meta omissions BEFORE they reached UAT
       - The forged-API exception pattern keeps the test-suite UI-driven (preserving the Lowdefy `_state:` contract) while still exercising server guards

    "## What didn't work (carry-forwards)"
       - AgGrid master-detail Enterprise-only forced the visible: toggle fallback (Pitfall 5 closed but the mobile UX is less polished)
       - EditPlanningWindow's wipe + re-default flow (Pitfall 6 accepted; v1.1 considers preservation)
       - Lowdefy 5.3 inline media-query support remains unverified (Open Question 2 from UI-SPEC)

    "## Lessons for Phase 04"
       - Phase 04 solver-payload-builder must read availability via the LATERAL+CASE-rank query (extract to a shared helper if used in multiple places)
       - Phase 04 must NEVER touch the rule/rule_override DB CHECK enums — those are frozen
       - Phase 04 takes ownership of state transitions; Phase 03 left state='open' as the only writable state

    "## Self-Check: PASSED" — list verified existence of key artifacts + the tag + the test counts.

    File 2 — Edit `.planning/ROADMAP.md`:
       (a) Under "### Phase 3: Availability & Rules" section, update the `**Plans:**` line to read `8 plans` (was TBD); add a 'Plans:' bullet list with all 8 plan filenames + brief objectives (matching the format used in completed Phase 2's entry above):
         - [x] 03-01-PLAN.md — P02-HF-05 rebuild (21 mutation specs as Playwright UI flows) + tools/check-handler-registration.mjs structural verifier
         - [x] 03-02-PLAN.md — Wave 0: migration 0014 (denorm columns) + ShiftySessionCallback soldier_id extension + types.js/Knex.js skeleton + SOURCE_RANK helper
         - [x] 03-03-PLAN.md — Wave 1: shift_slot CRUD handlers + template wizard + team_detail משמרות tab + 2 blocks + spec
         - [x] 03-04-PLAN.md — Wave 1: planning_window lifecycle handlers + planning_windows index page + open form block + menu entry + spec
         - [x] 03-05-PLAN.md — Wave 2: DeclareAvailability 3-mode handler + my_availability mobile-first RTL page + availability_date_row block + e2e + integration specs + seedFullWindow fixture
         - [x] 03-06-PLAN.md — Wave 3: rules + rule_override handlers + team_detail/soldier_detail tab extensions + 2 blocks + 4 test specs (unit + integration + 2 e2e)
         - [x] 03-07-PLAN.md — Wave 3: planning_window_detail page (3 tabs — details/pivot/team status) + manager-override entry-point + spec
         - [x] 03-08-PLAN.md — Closeout: VERIFICATION.md + SUMMARY.md + ROADMAP/STATE updates + v0.3.0-phase3 tag + hpg5 sync
       (b) In the "## Progress" table at the bottom, find the `| 3. Availability & Rules | 0/TBD | Not started | - |` row and update to `| 3. Availability & Rules | 8/8 | Complete | 2026-MM-DD |` (use actual date).
       (c) In the top-level Phases checkbox list near the top, flip `- [ ] **Phase 3: Availability & Rules** ...` to `- [x] **Phase 3: Availability & Rules** - ... (completed 2026-MM-DD; v0.3.0-phase3)`.

    File 3 — Edit `.planning/STATE.md`:
       (a) Frontmatter: `stopped_at` updated to 'Phase 03 complete; v0.3.0-phase3 tagged'; `last_updated` to closeout timestamp; `last_activity` to '2026-MM-DD — Phase 03 closeout'; `progress.completed_phases` from 1 to 2 (verify against actual prior value — STATE shows Phase 01 complete + Phase 02 complete = 2 completed, plus this plan closing Phase 03 = 3); `progress.percent` adjust accordingly.
       (b) Body 'Current Position' section: Phase: 03 — COMPLETE; Plan: 8 of 8; Status: Complete; Last activity: closeout date; Next phase: 04 — Solver & Schedule.
       (c) Accumulated Context / Decisions: append the load-bearing Phase 03 decisions (taken from 03-SUMMARY frontmatter's decisions list) — use the same shape as existing entries.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for (const f of ['.planning/phases/03-availability-rules/03-SUMMARY.md','.planning/ROADMAP.md','.planning/STATE.md']) { if (!fs.existsSync(f)) { console.error('MISSING:',f); process.exit(1); } } const sum=fs.readFileSync('.planning/phases/03-availability-rules/03-SUMMARY.md','utf8'); for (const tok of ['v0.3.0-phase3','Self-Check','Phase 03','P02-HF-05','12 new','source-precedence']) { if (!sum.includes(tok)) { console.error('SUMMARY missing:',tok); process.exit(2); } } const rm=fs.readFileSync('.planning/ROADMAP.md','utf8'); if (!rm.includes('8/8')) { console.error('ROADMAP Phase 3 row not flipped to 8/8'); process.exit(3); } if (!rm.match(/Phase 3:\\s*Availability[\\s\\S]*Complete/)) { console.error('ROADMAP Phase 3 checkbox not flipped'); process.exit(4); } if (!rm.includes('03-01-PLAN.md')) { console.error('ROADMAP Phase 3 Plans list not populated'); process.exit(5); } const st=fs.readFileSync('.planning/STATE.md','utf8'); if (!st.match(/Next phase:\\s*04/)) { console.error('STATE Next phase not updated to 04'); process.exit(6); } if (!st.match(/Phase 03|03\\s*\\(availability/i)) { console.error('STATE current position not updated'); process.exit(7); } console.log('OK Task 2 — SUMMARY + ROADMAP + STATE');"</automated>
  </verify>
  <done>03-SUMMARY.md exists with frontmatter + load-bearing deliverables + retro sections + Self-Check; ROADMAP.md Phase 3 row flipped + checkbox flipped + Plans list populated; STATE.md current position advanced to Phase 04; all three files committed in one closeout commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human signs off on Phase 03 closeout artifacts BEFORE tag push</name>
  <what-built>03-VERIFICATION.md + 03-SUMMARY.md + ROADMAP.md + STATE.md updates are all in place. The full test suite ran green or skip-clean against hpg5 (counts captured in VERIFICATION). The structural verifier exited 0 (21/21 handlers). Migration 0014 is applied on hpg5 with version=14 / dirty=false.</what-built>
  <how-to-verify>
1. Read `.planning/phases/03-availability-rules/03-VERIFICATION.md` and confirm: every Phase 03 REQ id has a documented status; AVAL-09 is footnoted as Phase 06; deferred items match CONTEXT.md.
2. Read `.planning/phases/03-availability-rules/03-SUMMARY.md` and confirm: the load-bearing deliverables list matches the actual files in `app/plugins/shifty-plugin/src/connections/Knex/requests/` (CreateShiftSlot, UpdateShiftSlot, DeleteShiftSlot, OpenPlanningWindow, EditPlanningWindow, DeletePlanningWindow, ApplyShiftTemplate, DeclareAvailability, UpsertRule, UpsertRuleOverride, ResetRuleOverride, SeedTeamRules = 12 new handlers).
3. Read `.planning/ROADMAP.md` and confirm: Phase 3 row in progress table shows `8/8 | Complete | <date>`; Phase 3 Plans list has 8 entries; top-level Phase 3 checkbox is flipped.
4. Read `.planning/STATE.md` and confirm: Current Position shows Phase 03 COMPLETE + Next phase 04; progress.completed_phases incremented.
5. Sanity-spot-check one rebuilt P02-HF-05 spec (e.g., `tests/e2e/soldier-crud.spec.ts`) and confirm it uses `page.goto` + `page.locator(...).fill(...)` style — not `request.post('/api/request/...')` for mutations.
6. Confirm tools/check-handler-registration.mjs exits 0 by running `pnpm run check:handler-registration` locally.

Reply with "approved" to proceed to the tag push + hpg5 sync, or describe any mismatch.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Push v0.3.0-phase3 git tag + sync hpg5 + verify container healthy</name>
  <files>(no file changes — release operations)</files>
  <action>
    After Task 3's human approval: commit the Plan 03-08 file changes (VERIFICATION, SUMMARY, ROADMAP, STATE) with a single commit message `docs(03-08): Phase 03 closeout — VERIFICATION + SUMMARY + ROADMAP + STATE; ready for v0.3.0-phase3 tag`. Push to origin/main.

    Create the annotated tag with a multi-line message capturing the phase's load-bearing deliverables:
    ```
    git tag -a v0.3.0-phase3 -m "Phase 03: Availability & Rules — completed YYYY-MM-DD.

    Load-bearing deliverables:
    - 1 migration (0014 denorm columns)
    - 12 new shifty-plugin request handlers (shift slot CRUD + planning window lifecycle + DeclareAvailability + rule + rule_override + seed)
    - 6 new/extended pages (planning_windows, my_availability, planning_window_detail; team_detail + soldier_detail tabs)
    - 5 reusable blocks
    - 1 structural verifier tool
    - 11 new test specs (e2e + integration + unit)
    - P02-HF-05 closure (21 mutation specs rebuilt as Playwright UI flows)

    Closes: SHFT-01..07, AVAL-01..08 (minus AVAL-09 → Phase 06), RULE-01..07."
    ```
    Push the tag: `git push origin v0.3.0-phase3`.

    Sync hpg5 to the tagged HEAD (per CLAUDE.md "Deploy sync"): plink to hpg5 then `cd C:\shifts-manager; git fetch origin main; git reset --hard origin/main; git log -1 --oneline`. Verify the tag is reachable via `git describe --tags --abbrev=0` should return `v0.3.0-phase3`. Rebuild Lowdefy via PsExec (per CLAUDE.md "Common ops on hpg5" → "Rebuild Lowdefy" command). Wait for container health: `docker ps --filter name=shifty-lowdefy --format '{{.Status}}'` must include 'healthy'. Verify the BUILD-EMITTED `/build/.lowdefy/server/build/plugins/connections.js` still imports Knex from `shifty-plugin/connections`. Final sanity: `docker logs shifty-lowdefy --tail 30 | grep -i 'ready\\|error'` → expect 'ready' present, no ERR_MODULE_NOT_FOUND.

    Run the full Phase 03 spec suite one more time against live hpg5 to confirm tag commit + hpg5 sync produced a stable end-state: `pnpm exec playwright test tests/e2e/{shift-slot-crud,planning-window-open,availability-declare,team-rules,rule-override,planning-window-detail}.spec.ts tests/e2e/{soldier-crud,roster-csv-import,org-unit-crud,tenant-isolation,ui-smoke-phase2}.spec.ts` with `PLAYWRIGHT_BASE_URL=https://apps.nesher.co` — all specs PASS or skip-clean (no failures).
  </action>
  <verify>
    <automated>echo "Verify: git tag v0.3.0-phase3 exists; hpg5 git describe shows the tag; docker ps shows shifty-lowdefy healthy; full Phase 03 spec suite has 0 failures against live hpg5."</automated>
  </verify>
  <done>v0.3.0-phase3 tag exists on origin/main; hpg5 synced to tag; Lowdefy rebuilt + healthy; build-emitted connections.js confirmed; final spec-suite run green or skip-clean; Phase 03 is officially closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git push --tags | Tag push is signed by the user's git config; no additional signing required for v1. |
| hpg5 sync (git reset --hard) | Wipes uncommitted state; .gitignore excludes .env / backups / postgres-data per CLAUDE.md — safe to reset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-32 | Tampering | git reset --hard wipes uncommitted work on hpg5 | accept | Standard Phase 02+ deploy pattern documented in CLAUDE.md; .gitignore excludes secrets + data; .env preserved by exclusion. |
| T-03-33 | Repudiation | Tag pushed without human approval | mitigate | Task 3 is checkpoint:human-verify gating Task 4; the tag push is gated by explicit "approved" signal. |
| T-03-SC | Tampering | No new npm packages added | n/a | Zero npm installs. |
</threat_model>

<verification>
- 03-VERIFICATION.md exists with all 22 Phase 03 requirements documented
- 03-SUMMARY.md exists with the deliverables summary + retrospective + Self-Check
- ROADMAP.md Phase 3 row + checkbox + Plans list all reflect Complete state
- STATE.md current position is Phase 03 COMPLETE + Next phase 04
- v0.3.0-phase3 tag exists on origin/main
- hpg5 synced to tag HEAD; container healthy; final spec-suite run clean
</verification>

<success_criteria>
- Phase 03 is closed per GSD ritual: all 4 doc artifacts + tag + hpg5 sync
- Phase 04 starts from a tagged baseline (v0.3.0-phase3)
- Every Phase 03 REQ has documented evidence
- P02-HF-05 deferral is closed and noted in 03-SUMMARY
- The orchestrator has the full carry-forward list for Phase 04 planning
</success_criteria>

<output>
This plan IS the SUMMARY (03-08-SUMMARY.md not required because the phase-level 03-SUMMARY.md serves the same purpose). Optionally create `.planning/phases/03-availability-rules/03-08-SUMMARY.md` if the orchestrator wants per-plan tracking; default behavior is to skip and rely on 03-SUMMARY.md as the phase aggregate.
</output>

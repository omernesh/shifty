# Phase 02 — Resume Handoff (2026-05-14, post-deploy)

**Status:** Phase 02 deploy is GREEN on hpg5. All 10 plans complete in terms of automated execution. Plan 10 Task 4 (live Playwright + 6-scenario UI smoke + RTL email smoke) is `checkpoint:human-action` and is the only outstanding work — it requires a logged-in browser session that an automated agent cannot drive.

## What's done in this session (Wave 4)

| Item | Hash | Notes |
|------|------|-------|
| Lowdefy 5.3 spike round 1 (3 findings) | `8826a13`–`931cc97` | page-level `auth:` removed, `if:` → `skip:`, `TagSelector` → `MultipleSelector`, AgGrid `onCellClicked` → `onCellClick` (latent bug) |
| Lowdefy 5.3 spike round 2 (audit, 3 INVALID + 4 DEPRECATED + 3 latent) | `c73c8b9`–`e0c3ac0` | `Upload` → TextArea paste, `Spin` → `Spinner` (+ blocks-loaders plugin install), `onError:` → try/catch event shape, layout `content*` → `gap/justify/wrap` (with Box→Flex switch on toolbar sites), AgGrid extras stripped, Card `headStyle` stripped |
| Skill ref corrections | within above | `.claude/skills/lowdefy/reference/01,04,05,07,08-*.md` updated against verified 5.3 source |
| Push to origin/main + hpg5 sync | — | `git fetch origin main && git reset --hard origin/main` per CLAUDE.md canonical |
| PsExec docker build | — | Container `shifty-lowdefy` healthy, listening on 8080, `Ready in 834ms` |
| Migration 0008_legacy_drop applied | — | 5 legacy tables dropped in single transaction (BEGIN → 5 DROP TABLE → COMMIT); idempotent re-run confirmed; `set_updated_at` trigger function preserved; schema_migrations.version stays at 12 (intentional out-of-order drop behind 0011+0012) |
| 5/5 W2 strict resume-signal | — | manage_soldiers 307 (auth redirect, route resolves), /employees → `/api/page/employees` returns Location: /404 (page gone), legacy tables absent, set_updated_at preserved, idempotent no-op |
| 63/63 unit tests pass | — | Plugin-colocated + B1 `tests/unit/*.spec.ts` |
| Phase verifier | — | READY-WITH-CAVEATS (12/13 must-haves); see `.planning/phases/02-org-people/02-VERIFICATION.md` |

## What's left (user-driven)

**Plan 10 Task 4** — `checkpoint:human-action`. Requires a logged-in browser session.

1. Confirm hpg5 stack still healthy (it's been healthy since the build):
   ```
   plink -ssh -l claude -pw "Onclaude2103" -batch -hostkey "SHA256:tPg5mYQbJO/9ccGmNGeyJeQQSPXq+C6SL3EHJcbRZMQ" hpg5 "powershell -c \"cd C:\shifts-manager; docker compose ps\""
   ```

2. Run the live Playwright e2e suite from local against hpg5:
   ```
   $env:PLAYWRIGHT_BASE_URL = "http://hpg5:8080"
   $env:PG_TEST_URL = "postgres://shifts:changeme@hpg5:5432/shifts"
   npx playwright test tests/e2e/cross-tenant-leak.spec.ts tests/e2e/org-unit-crud.spec.ts tests/e2e/roster-csv-import.spec.ts tests/e2e/soldier-crud.spec.ts tests/e2e/tenant-isolation.spec.ts
   ```
   (PG_TEST_URL requires direct Postgres access from local — the docker compose currently doesn't publish 5432 on hpg5. If the suite can't reach Postgres directly, either publish the port temporarily or run Playwright from inside hpg5.)

3. Six manual UI smoke scenarios (Hebrew RTL browser, logged in as a tenant-A admin):
   - manage_org_units: tree-table renders; Add Child button opens modal (W6 Pattern A); Rename works on own team row; Delete only allowed on leaf
   - manage_soldiers: kibbutz creation creates a soldier with the smart-quote canonicalized name (the U+2019 → '' rule)
   - soldier_detail: color picker (color_swatches block) shows 24 swatches; selection persists
   - team_detail: Add Member modal lists candidate soldiers; submit creates membership; Remove triggers ConfirmModal
   - roster_import: paste a CSV (try `tests/fixtures/csv/clean.csv` content) → ParseCsvAndValidate runs → preview row-by-row → CommitRosterImport sends magic-link emails via Resend
   - Tenant-B isolation: log out, sign in as a tenant-B admin, confirm tenant-A's data is NOT visible

4. RTL email smoke: trigger InviteLater on a tenant-A soldier; check the inbox for an RTL-rendered Hebrew email; click the magic-link → lands on signup_with_invite → redeems → /my_dashboard

## After UAT green

1. Tag the release: `git tag -a v0.2.0-phase2 -m "Phase 02 — Org & People (deploy green, UAT verified)" $(git rev-parse HEAD)`; `git push origin v0.2.0-phase2`
2. Run `/gsd-complete-milestone` (or simply update `02-10-SUMMARY.md` Task 4 to `completed`, then run `/gsd-cleanup` to archive Phase 02)
3. Move to Phase 03 — Availability & Rules

## Phase 3 deferred items (track in PRD §15 / Phase 3 backlog)

1. **Custom RosterUpload block** — wraps antd Upload + drag-drop file picker. Restores the file-upload UX (Phase 2 ships TextArea paste). ~80 lines: meta.js + React component in `app/plugins/shifty-roster/src/blocks/`.
2. **AgGrid feature restoration** — `enableRtl` (Hebrew column-order reversal), `animateRows`, `getRowStyle` (row-by-status background), `rowSelection`, `domLayout`, column-level `cellEditor` are silently dropped in Lowdefy 5.3. To restore, fork `@lowdefy/blocks-aggrid` or write a thin custom block.
3. **"צור" inline-add for new role_tags** — MultipleSelector hardcodes `mode="multiple"` (no free-text). Custom plugin block needed. Currently new tags arrive via CSV import side-effect or the v1.1 `manage_role_tags` edit page.
4. **`_spread` operator warning** — `app/pages/admin/roster_import.yaml` lines 255 + 288 use `_spread` which Lowdefy 5.3 doesn't know about. Build is green (warning only). The two bulk toolbar buttons (lowercase-role_tags, re-invite-all-duplicates) may silently no-op. Replace with explicit field-by-field merge or a custom operator.

## Anti-patterns recorded (do NOT regress)

| ID | Severity | Lesson |
|----|----------|--------|
| AP-02-01 | blocking | Don't trust `.claude/skills/lowdefy/reference/*.md` for novel field set without verifying against Lowdefy 5.3 source. Skill drift caused 6 round-trips in Phase 2. The audit (round 2) reset the skill against verified source. |
| AP-02-02 | blocking | Don't assume page-level `auth: { roles: [...] }` is the Layer-3 gate — 5.3 rejects it. Layer-3 + Layer-4 are both `auth.pages.roles` at lowdefy.yaml top level (server-enforced via `getPageConfig` + `authorizeRequest`). |
| AP-02-03 | blocking | Don't patch-and-retry on schema-validator errors. Run an exhaustive 5.3 audit BEFORE the next deploy when ANY schema error surfaces. Round 2 honored this and unblocked the deploy in one cycle. |

## Files of interest

- `.planning/phases/02-org-people/02-VERIFICATION.md` — verifier's READY-WITH-CAVEATS report (12/13 must-haves)
- `.planning/phases/02-org-people/02-09-SUMMARY.md` — full Plan 09 SUMMARY (rounds 1+2 corrections + deploy log)
- `.planning/phases/02-org-people/02-10-SUMMARY.md` — Plan 10 SUMMARY (Tasks 1-3 done, Task 4 deferred)
- `tests/e2e/*.spec.ts` — 3 Phase 2 e2e specs (roster-csv-import, soldier-crud, tenant-isolation) ready for live runs
- `tests/unit/*.spec.ts` — 3 unit specs (canonicalize, color-palette, role-tag-canonical) — 21 tests each, all pass
- `tests/fixtures/csv/*` — 5 fixtures (clean, smart-quote, dup-email, bidi-mark, perf-50)

## Quick state

```
$ git log --oneline -8
a7911d1 chore: merge executor worktree (worktree-agent-a89d5b1f686ddaf8b) — Plan 02-09 round 2
e0c3ac0 docs(02-09): mark Plan 09 round 2 complete; second deploy retry queued
9893854 docs(skill): Wave 4 — Lowdefy 5.3 audit corrections to skill refs
e8221f0 fix(02-09r2): Wave 3.2 — strip Card headStyle (silently dropped in 5.3)
530f925 fix(02-09r2): Wave 3.1 — strip AgGrid extra props (silently dropped in 5.3)
5dceb60 fix(02-09r2): Wave 2 — layout content* renames + Box to Flex on toolbar sites
4e27c89 fix(02-09r2): Wave 1.3 — onError: to try/catch event shape (signup_with_invite)
aa1f3af feat(02-09r2): Wave 1.2 — Spin to Spinner with @lowdefy/blocks-loaders plugin

$ docker compose ps  (on hpg5)
shifts-postgres   Up 2 days (healthy)       5432/tcp
shifty-lowdefy    Up 32+ minutes (healthy)  0.0.0.0:8080->3000/tcp

$ schema_migrations.version
12  (legacy 0008 applied as out-of-order drop; tables gone)
```

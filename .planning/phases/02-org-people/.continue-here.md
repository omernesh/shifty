# Phase 02 — Resume Handoff (2026-05-14, post-UAT-attempt)

**Read `02-UAT-FINDINGS.md` first.** It is the source of truth for everything that happened in the 2026-05-14 UAT session and supersedes the optimistic "deploy green, UAT pending" framing from earlier in the day.

## Bottom line

Phase 02 cannot close yet. Live UAT discovered that **custom Lowdefy request types are never registered with Lowdefy 5.3's runtime** because of a plugin-pattern mismatch. This affects 9 request types across 11 page YAMLs (34 occurrences). They've been latent-broken since they were authored; the static verifier didn't catch it, the "deploy green" status only meant the container starts.

Layer 5 RLS (PRD §8.3 four-layer defense) was also design-only — fixed in SQL (migration 0013 applied) but the per-request context wiring depends on the same plugin-registration fix.

## State of the tree

- HEAD: `66e7e57` (pushed to origin/main and synced to hpg5).
- Migration `0013_layer5_rls_app_role.up.sql` is applied. `schema_migrations.version = 13`, `dirty = false`.
- shifty_app role created, FORCE RLS active, but Layer 5 not yet active because `KnexRawTenant` resolver isn't registered.
- `tests/e2e/_fixtures/seed-tenants.ts` has been touched intentionally (cookie name to `__Secure-`, SET ROLE NONE for seeding).
- 26 YAML KnexRaw → KnexRawTenant conversions in commit `439070e` — these currently fail at runtime; either the registration fix lands and they start working, or they need to revert.
- Container `shifty-lowdefy` was healthy at session end but features are in a half-broken state until the next deploy.

## What's left

In rough order. See `02-UAT-FINDINGS.md` §"What needs to happen next session" for fuller detail.

1. **Decide plugin refactor strategy** — aggregator-as-third-plugin / dep-inversion / merge-all-into-one. This is the load-bearing decision.
2. **Implement the merged-Knex aggregator** with all 9 custom requests + KnexRawTenant in the `requests` map of a single `Knex` connection type.
3. **Update plugin types.js + package.json exports** per the new structure.
4. **Rebuild lowdefy on hpg5** (via PsExec).
5. **Re-run live UAT** — the 5 Phase 2 specs + the 6 manual UI smoke scenarios + RTL email smoke (the original Plan 10 Task 4 list).
6. **Update Plan 10 SUMMARY, ROADMAP**, tag `v0.2.0-phase2`, archive Phase 02.

## Side-effects from this session, cleaned up

- Temporary `pg-tunnel` socat container on hpg5: removed.
- Windows Firewall rule `shifty-pg-tunnel-5433` on hpg5: deleted.
- `alpine/socat:latest` image still cached on hpg5 (small, harmless).

## Test-fix commits this session (these are KEEPERS regardless of plugin decision)

- `1808deb` — signInAs cookie format (drop SET-COOKIE attrs from request Cookie header).
- `bc1e261` — SET ROLE NONE in test fixtures (not RESET ROLE; required because migration 0013 sets a default).
- `66e7e57` — switch test fixture cookie name to `__Secure-next-auth.session-token` (Auth.js HTTPS-mode name; NEXTAUTH_URL is `https://apps.nesher.co`).

## Other useful artifacts

- `tests/e2e/_fixtures/seed-tenants.ts` — has good comments explaining the SET ROLE NONE rationale and the `__Secure-` cookie naming.
- `db/migrations/0013_layer5_rls_app_role.up.sql` — the role-split + FORCE RLS migration. Applied.
- `app/plugins/shifty-auth/src/connections/requests/KnexRawTenant.js` — Layer 5 RLS-aware drop-in for KnexRaw. Correct as a resolver; just not registered.
- `app/plugins/shifty-auth/src/hooks/with-tenant-tx.js` — helper that wraps a Knex transaction with SET LOCAL app.current_tenant.

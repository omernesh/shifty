---
phase: 02-org-people
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/pages/admin/manage_org_units.yaml
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "manage_org_units renders as an AgGrid tree-table with treeData and getDataPath (D-01)"
    - "Admin sees full tree plus all per-row controls; team_manager sees full tree plus rename only on their own team rows (D-02)"
    - "Per-row Add child here action exists; level auto-derived from parent.level+1 server-side, no NumberInput (D-01)"
    - "Adding a child at a node at current tenant.org_depth triggers a grow-depth confirmation modal capped at 3 (D-04)"
    - "Existing tenant-scoped CRUD requests preserved verbatim; Phase 1 leaf-only delete guard stays; check-queries gate continues to pass"
    - "AgGrid block has enableRtl: true (Phase 1 oversight corrected)"
    - "Per-row action cells use AgGrid Pattern A (RESEARCH P9): three separate columns col_add_child / col_rename / col_delete, each rendering ONE icon-button cellRenderer; the onCellClicked handler dispatches purely on _event.column.field (no HTML data-action attribute hack). (W6 fix)"
    - "This plan supports ROST-04 (navigation surface) and the D-08 audit path for org_unit mutations; it satisfies no NEW ROST literally but is foundational for org-tree CRUD UX — `requirements: []` is intentional and tracked via the foundational note below (W5 fix)."
    - "grow_org_depth_and_add_child request has a Layer-4 admin role gate at the SQL layer (not just UI hiding) — payload carries is_admin from _user, and the UPDATE on tenant + INSERT on org_unit short-circuit to zero rows when is_admin is false (B4 fix). team_manager UI-hiding (D-02) is Layer 3; this is the matching Layer 4 server-side defense."
  artifacts:
    - path: app/pages/admin/manage_org_units.yaml
      provides: "Tree-table upgrade + team-manager conditional visibility + grow-depth confirmation with Layer-4 admin gate"
      contains: "treeData: true"
  key_links:
    - from: app/pages/admin/manage_org_units.yaml
      to: org_unit
      via: "WITH RECURSIVE tree CTE + tenant_id filter from _user"
      pattern: "WITH RECURSIVE tree"
    - from: app/pages/admin/manage_org_units.yaml
      to: tenant
      via: "Grow-depth UPDATE + INSERT in one transactional KnexRaw query GATED by :is_admin from _user (B4)"
      pattern: "UPDATE tenant SET org_depth"
---

<objective>
Upgrade the existing app/pages/admin/manage_org_units.yaml into the Phase-2 tree-table per D-01, D-02, D-04. Preserve the four Phase 1 KnexRaw requests (list_units, create_org_unit, rename_org_unit, delete_org_unit) verbatim where payload+parameters are concerned. Two query changes: list_units becomes a recursive CTE emitting path TEXT[] for AgGrid getDataPath; a new transactional grow_org_depth_and_add_child request lands for D-04.

The AgGridAlpine block becomes a tree-table (treeData: true, getDataPath: data => data.path). Per-row buttons use **RESEARCH Pitfall P9 Pattern A in its purest form (W6 fix)**: three separate columns (`col_add_child`, `col_rename`, `col_delete`), each rendering a SINGLE icon-button cellRenderer; the `onCellClicked` handler dispatches purely on `_event.column.field` and reads the row's data straight from `_event.data` — no HTML `data-action` bridge. Visibility is Lowdefy `visible:` operators reading `_user: roles` (D-02). The leaf-only delete guard from Phase 1 stays. Add `enableRtl: true` to the AgGrid block (Phase 1 oversight).

The grow_org_depth_and_add_child request — which UPDATEs `tenant.org_depth` and INSERTs into `org_unit` in one transaction — **gains a Layer-4 admin gate at the SQL layer (B4 fix)**. UI hiding of "Add child" from team_manager (D-02) is Layer 3; without a SQL-layer guard a team_manager could POST directly to the request and grow the tenant's org_depth. Mirrors the `is_admin OR EXISTS(...)` Layer-4 pattern used by Plan 06's UpdateSoldier and Plan 07's remove_membership. Pass-through `is_admin` is sourced from `_user: is_admin` (or computed from `_array.includes` on `_user: roles` if the session doesn't expose a precomputed flag — check existing pattern in Plan 06).

This plan satisfies no new ROST literally — it's a foundational tree-table UX upgrade that supports ROST-04 (org navigation surface) and the D-08 audit path; `requirements: []` is intentional and tracked here so downstream plan-checkers don't read it as a coverage gap (W5 fix).

Output: upgraded YAML; `node tools/check-queries.mjs` still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-org-people/02-CONTEXT.md
@.planning/phases/02-org-people/02-RESEARCH.md
@.planning/phases/02-org-people/02-PATTERNS.md
@.planning/phases/02-org-people/02-UI-SPEC.md
@app/pages/admin/manage_org_units.yaml
@db/migrations/0002_tenancy_and_org.up.sql
@.claude/skills/lowdefy/reference/03-blocks.md
@.claude/skills/lowdefy/reference/04-requests.md
@.claude/skills/lowdefy/reference/05-events.md
@.claude/skills/lowdefy/reference/06-operators.md
@tools/check-queries.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade list_units to recursive CTE; add get_tenant_depth and grow_org_depth_and_add_child requests with Layer-4 admin gate (B4); extend auth.roles to include team_manager</name>
  <files>app/pages/admin/manage_org_units.yaml</files>
  <read_first>
    - app/pages/admin/manage_org_units.yaml (entire file — Phase 1 reference template)
    - .planning/phases/02-org-people/02-RESEARCH.md section "org_unit Tree — Lowdefy + AgGrid tree-table recipe" (recursive CTE + AgGrid props)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Grow-org_depth confirmation transaction (D-04)" (the single-statement transactional CTE)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Tenant Isolation Pattern (4-Layer Defense) — Layer 4" (the is_admin OR EXISTS pattern — adapt for the tenant-grow UPDATE)
    - db/migrations/0002_tenancy_and_org.up.sql lines 25-52 (tenant.org_depth CHECK BETWEEN 1 AND 3; org_unit table + idx_org_unit_tenant)
    - .planning/phases/02-org-people/02-PATTERNS.md section "manage_org_units.yaml (UPGRADE — page)"
    - tools/check-queries.mjs (gate's WHERE-tenant_id detection — confirms recursive CTE still passes)
  </read_first>
  <action>
    Edit auth.roles from `[unit_admin]` to `[unit_admin, team_manager]` (D-02 visibility — team_manager has read access; rename gated row-by-row in Task 2).

    Replace list_units `properties.query` with the recursive CTE: a `tree` CTE with `SELECT id, parent_id, level, name, ARRAY[name]::text[] AS path FROM org_unit WHERE tenant_id = :tenant_id AND parent_id IS NULL` then `UNION ALL SELECT ou.id, ou.parent_id, ou.level, ou.name, tree.path || ou.name FROM org_unit ou JOIN tree ON ou.parent_id = tree.id WHERE ou.tenant_id = :tenant_id`; a `counts` CTE with `SELECT parent_id, COUNT(*)::int AS child_count FROM org_unit WHERE tenant_id = :tenant_id AND parent_id IS NOT NULL GROUP BY parent_id`; final SELECT joining tree LEFT JOIN counts ON c.parent_id = t.id, returning id, parent_id, level, name, path, COALESCE(child_count, 0) AS child_count, ORDER BY t.path. Keep payload.tenant_id: { _user: tenant_id } and parameters.tenant_id: { _payload: tenant_id } byte-equal to Phase 1.

    Add new request get_tenant_depth alongside list_units: KnexRaw, payload.tenant_id from _user, query `SELECT org_depth FROM tenant WHERE id = :tenant_id`, parameters.tenant_id from _payload. UI uses _request.get_tenant_depth[0].org_depth to compute proposed new depth.

    Add new request grow_org_depth_and_add_child: KnexRaw with the **Layer-4 admin gate (B4 fix)**. Payload sources:
      tenant_id from _user: tenant_id
      parent_id from _state.add_child_target_id
      new_name from _state.add_child_name_input
      new_depth from _state.confirmed_new_depth
      **is_admin from _user (mirror Plan 06's CreateSoldier pattern: `_user: is_admin` if the session exposes it as a precomputed flag; otherwise `_array.includes: { on: { _user: roles }, value: unit_admin }` — confirm the exact form at write time by reading Plan 06 Task 1 action, where the same is_admin propagation is established).**

    The query body uses the existing two-CTE transactional shape from RESEARCH section "Grow-org_depth confirmation transaction (D-04)" BUT wraps the entire mutation in an admin-only guard. Implement with a leading `guard` CTE that returns one row iff `:is_admin` is true; both downstream CTEs require `EXISTS (SELECT 1 FROM guard)` so a non-admin caller gets zero-row results everywhere:

    ```
    WITH guard AS (
      SELECT 1 WHERE :is_admin
    ),
    depth_update AS (
      UPDATE tenant SET org_depth = :new_depth
      WHERE id = :tenant_id
        AND :new_depth > org_depth
        AND :new_depth <= 3
        AND EXISTS (SELECT 1 FROM guard)
      RETURNING id
    ),
    new_unit AS (
      INSERT INTO org_unit (tenant_id, parent_id, level, name)
      SELECT :tenant_id, :parent_id,
             (SELECT level + 1 FROM org_unit WHERE id = :parent_id AND tenant_id = :tenant_id),
             :new_name
      WHERE EXISTS (SELECT 1 FROM guard)
        AND (
          EXISTS (SELECT 1 FROM depth_update)
          OR (SELECT level + 1 FROM org_unit WHERE id = :parent_id AND tenant_id = :tenant_id)
             <= (SELECT org_depth FROM tenant WHERE id = :tenant_id)
        )
      RETURNING id, level
    )
    SELECT id, level FROM new_unit;
    ```

    Behavior contract (B4 acceptance):
    - When `:is_admin = true` and depth is being grown: tenant.org_depth updated, org_unit inserted, returns one row `{id, level}`.
    - When `:is_admin = true` and depth NOT being grown (insertion at existing depth): tenant.org_depth unchanged, org_unit inserted, returns one row.
    - When `:is_admin = false` (forged POST from team_manager): `guard` is empty → both `depth_update` and `new_unit` see `EXISTS (SELECT 1 FROM guard) = false` → both CTEs return zero rows → final SELECT returns zero rows AND tenant.org_depth is UNCHANGED. The Lowdefy request handler can detect this (empty result) and return `{"error":"forbidden"}` to the client.

    The CHECK constraint `org_depth BETWEEN 1 AND 3` from migration 0002 hard-caps depth as a DB invariant.

    Leave create_org_unit, rename_org_unit, delete_org_unit payload+parameters+query bodies UNTOUCHED. Phase 1 leaf-guard delete (`AND NOT EXISTS (SELECT 1 FROM org_unit child WHERE child.parent_id = :id)`) stays. Their action chains are modified in Task 2; their request bodies are immutable.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('app/pages/admin/manage_org_units.yaml','utf8'); const must=['WITH RECURSIVE tree AS','UNION ALL','child_count','get_tenant_depth','grow_org_depth_and_add_child','UPDATE tenant SET org_depth','BETWEEN 1 AND 3','AND NOT EXISTS (SELECT 1 FROM org_unit child WHERE child.parent_id = :id)','team_manager','WITH guard AS','SELECT 1 WHERE :is_admin','EXISTS (SELECT 1 FROM guard)','is_admin']; for (const m of must) if (!y.includes(m)) { console.error('MISSING:',m); process.exit(1); } console.log('OK');" &amp;&amp; node tools/check-queries.mjs</automated>
  </verify>
  <done>list_units returns path TEXT[] + child_count; get_tenant_depth + grow_org_depth_and_add_child requests exist; auth.roles includes team_manager; create_org_unit/rename_org_unit/delete_org_unit unchanged; check-queries gate passes (no untenanted query introduced); **grow_org_depth_and_add_child has the `WITH guard AS (SELECT 1 WHERE :is_admin)` Layer-4 gate so a non-admin POST returns zero rows and tenant.org_depth is unchanged (B4 acceptance)**.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace flat AgGrid with treeData AgGridAlpine; wire onCellClicked branching with PURE Pattern A (three separate columns, no HTML data-action bridge — W6 fix); apply D-02 visibility to row-action buttons</name>
  <files>app/pages/admin/manage_org_units.yaml</files>
  <read_first>
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Page 1: manage_org_units" (column shape, button labels, icon mapping)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Pitfalls to Avoid — P9. AgGrid tree-data + _function cellRenderer HTML button interop is undocumented" (Pattern A recommendation: onCellClicked branching on column.field — NOT HTML buttons)
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Color application matrix" (selected row highlight color and CSS contract)
    - .claude/skills/lowdefy/reference/05-events.md (onCellClicked event arg shape; _event.column.field)
    - .claude/skills/lowdefy/reference/06-operators.md (_array.includes operator for roles check)
  </read_first>
  <action>
    Replace the Phase 1 flat AgGrid block with an AgGridAlpine tree-table. **W6 fix — adopt PURE Pattern A from RESEARCH P9**: three SEPARATE action columns (`col_add_child`, `col_rename`, `col_delete`), each rendering a single icon-button cellRenderer. The dispatch happens via `onCellClicked` branching purely on `_event.column.field`. There is no HTML `data-action` attribute bridge — Lowdefy can't reliably reach into nunjucks-rendered HTML span clicks (the documented risk in RESEARCH P9 — Pattern A explicitly avoids HTML buttons).

    properties:
      enableRtl: true
      treeData: true
      animateRows: true
      groupDefaultExpanded: -1
      rowData: { _request: list_units }
      autoGroupColumnDef:
        headerName: יחידה
        minWidth: 280
        cellRendererParams: { suppressCount: false }
      getDataPath:
        _function:
          __args: 0
          __return: { __args: 0.path }
      columnDefs:
        - { field: level, headerName: רמה, width: 80 }
        - { field: child_count, headerName: ילדים, width: 80 }

        # Action column 1 — add child (admin only)
        - field: col_add_child
          headerName: ''
          width: 60
          sortable: false
          filter: false
          cellRenderer:
            _function:
              __args: 0
              __return:
                _nunjucks:
                  template: |
                    {% if isAdmin %}<span title="הוסף תת" style="cursor:pointer">＋</span>{% endif %}
                  on:
                    isAdmin: { _array.includes: { on: { _user: roles }, value: unit_admin } }

        # Action column 2 — rename (admin or team_manager on own row)
        - field: col_rename
          headerName: ''
          width: 60
          sortable: false
          filter: false
          cellRenderer:
            _function:
              __args: 0
              __return:
                _nunjucks:
                  template: |
                    {% if canRename %}<span title="שינוי שם" style="cursor:pointer">✎</span>{% endif %}
                  on:
                    canRename:
                      _or:
                        - { _array.includes: { on: { _user: roles }, value: unit_admin } }
                        - _and:
                            - { _array.includes: { on: { _user: roles }, value: team_manager } }
                            - { _array.includes: { on: { _user: team_ids }, value: { __args: 0.data.id } } }

        # Action column 3 — delete (admin only, leaf only)
        - field: col_delete
          headerName: ''
          width: 60
          sortable: false
          filter: false
          cellRenderer:
            _function:
              __args: 0
              __return:
                _nunjucks:
                  template: |
                    {% if isAdmin and children == 0 %}<span title="מחק" style="cursor:pointer;color:#FF4D4F">🗑</span>{% endif %}
                  on:
                    isAdmin: { _array.includes: { on: { _user: roles }, value: unit_admin } }
                    children: { __args: 0.data.child_count }

      events:
        onCellClicked:
          - id: route_action
            type: SetState
            params:
              clicked_column: { _event: column.field }
              clicked_row_id: { _event: data.id }
              clicked_row_name: { _event: data.name }
              clicked_row_level: { _event: data.level }
              clicked_row_child_count: { _event: data.child_count }

    Then chain a `Conditional` action that branches on `_state.clicked_column`:
      - `col_add_child` → enforce isAdmin (re-check via `_array.includes` on `_user: roles` — defense in depth alongside the SQL Layer-4 gate from Task 1); read get_tenant_depth result; compare `clicked_row_level + 1 > tenant.org_depth`; if true, open the D-04 grow-depth Confirm modal first (title "הגדלת עומק היררכיה", body templated as "ההוספה תעלה את המבנה ל-{N} רמות. פעולה זו משנה את מבנה הארגון. להמשיך?" with N = clicked_row_level + 1, confirm "המשך", cancel "ביטול", okType: danger). On confirm, set `_state.confirmed_new_depth = N` then open the same add-child Form. On Form submit, call `grow_org_depth_and_add_child` (with `is_admin` payload from `_user`).
      - `col_rename` → enforce canRename (admin OR team_manager + own team); open Modal with single TextInput defaulted to `_state.clicked_row_name`; on confirm, call existing `rename_org_unit` request with `id` from `_state.clicked_row_id`.
      - `col_delete` → enforce isAdmin AND `_state.clicked_row_child_count == 0`; open Phase 1 Confirm modal (title "מחיקת יחידה", body "האם למחוק את היחידה \"{name}\"? פעולה זו אינה הפיכה.", confirm "מחק", cancel "ביטול", okType: danger) — call existing `delete_org_unit` request unchanged.

    The Cards from Phase 1 (the bottom create-unit form for root-level inserts when no nodes exist yet) stay below the tree-table as a fallback empty-state primary action — UI-SPEC §"Page 1" wireframe specifies "Cards from Phase 1 KEPT as fallback create form below the tree".

    On every successful mutation chained event, refresh `list_units` (existing pattern from Phase 1).

    **W6 acceptance contract**: there are exactly THREE action columns (`col_add_child`, `col_rename`, `col_delete`). NO column contains multiple `data-action="..."` spans. NO column's cellRenderer emits more than one click-affordance. Dispatch branches on `_event.column.field`, not on a parsed `data-action` attribute. Verify-command grep gate asserts this.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('app/pages/admin/manage_org_units.yaml','utf8'); const must=['enableRtl: true','treeData: true','getDataPath','autoGroupColumnDef','onCellClicked','clicked_column','clicked_row_id','grow_org_depth_and_add_child','col_add_child','col_rename','col_delete','_array.includes','team_ids']; for (const m of must) if (!y.includes(m)) { console.error('MISSING:',m); process.exit(1); } if (y.match(/data-action\\s*=\\s*[\"']/)) { console.error('FORBIDDEN: data-action HTML attribute present — Pattern A is pure column-dispatch (W6 fix). Remove data-action spans and use col_add_child / col_rename / col_delete columns instead.'); process.exit(2); } console.log('OK tree-table W6-compliant');"</automated>
  </verify>
  <done>AgGridAlpine has treeData: true + getDataPath + enableRtl: true; **three separate action columns col_add_child / col_rename / col_delete (W6 fix); zero `data-action="..."` HTML attribute occurrences in the YAML**; onCellClicked branches on `_event.column.field`; D-02 visibility via `_array.includes` on `_user.roles` + `_user.team_ids`; grow-depth Confirm modal wired before D-04 path; delete uses Phase 1 Confirm modal unchanged; rename uses Phase 1 rename_org_unit unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Lowdefy request layer | All five mutating/reading requests (list_units, get_tenant_depth, create_org_unit, rename_org_unit, delete_org_unit, grow_org_depth_and_add_child) take tenant_id from `_user: tenant_id` (session) only — payloads cannot forge tenant. CI gate tools/check-queries.mjs enforces. |
| Page-level auth.roles | Layer 3 — `[unit_admin, team_manager]`. Team-manager can reach the page; row-level visibility on rename/add/delete is UX-only — Layer 4 is enforced by the SQL guard (B4 fix). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing (tenant forgery) | All 6 KnexRaw requests | mitigate | tenant_id derived from `_user: tenant_id` exclusively (verified by Task 1 grep gate). The recursive CTE filters BOTH UNION ALL branches with `WHERE tenant_id = :tenant_id`. |
| T-02-02 | Information Disclosure (cross-tenant tree visibility) | list_units recursive CTE | mitigate | Both anchor and recursive arms of the CTE include the tenant filter; the LEFT JOIN counts CTE filters the same way. Plan 10's tenant-isolation E2E asserts list_units run as tenant-A authenticator returns zero tenant-B rows. |
| T-02-06 | Elevation of Privilege (team_manager grows tenant.org_depth via direct API call) | grow_org_depth_and_add_child | mitigate (B4 fix) | **The SQL Layer-4 gate `WITH guard AS (SELECT 1 WHERE :is_admin)` ensures a non-admin caller's UPDATE and INSERT both no-op. Acceptance test: `POST grow_org_depth_and_add_child` with `session.is_admin=false` returns zero rows AND `tenant.org_depth` is unchanged.** is_admin is sourced from `_user: is_admin` (server-side session), never from the client payload. UI hiding (D-02) is Layer 3 belt; this SQL guard is the Layer 4 braces. |
| T-02-06 | Elevation of Privilege (team_manager performs admin-only rename on a non-owned row) | rename_org_unit | mitigate (residual) | Row-level visibility (`_array.includes` on `_user.team_ids` ∩ data.id) only HIDES the UI affordance. The actual server-side defense is rename_org_unit's payload still flows from `_state.clicked_row_id`, and the WHERE clause `id = :id AND tenant_id = :tenant_id` ensures cross-tenant rename is impossible. A team-manager forging rename of a sibling team's row WITHIN OWN TENANT is bounded by `WHERE tenant_id = :tenant_id` + the leaf-guard. **Accepted residual risk for Phase 2 manage_org_units:** intra-tenant rename by a team-manager is bounded by tenant filter + leaf-guard. Plan 10's RBAC test asserts UI visibility but does not test for cross-team rename API-level — flagged as v1.1 hardening. |

</threat_model>

<verification>
- `app/pages/admin/manage_org_units.yaml` is valid YAML.
- `node tools/check-queries.mjs` exits 0 (no untenanted Knex queries introduced).
- The four existing requests' payload+parameters+query bodies are byte-equal to Phase 1 except the list_units query, which is the recursive CTE.
- AgGrid block has enableRtl, treeData, getDataPath, autoGroupColumnDef, onCellClicked.
- **Three separate action columns (col_add_child / col_rename / col_delete) with single-affordance cellRenderers; ZERO `data-action="..."` HTML attribute occurrences (W6 fix).**
- D-02 visibility: a team_manager whose team_ids does not include row.id sees neither rename nor add-child nor delete affordances on that row.
- D-04 grow-depth modal fires before the add-child form when clicked_row_level + 1 > tenant.org_depth; confirm chain calls grow_org_depth_and_add_child; cap at depth 3 enforced by CHECK constraint.
- **grow_org_depth_and_add_child SQL contains `WITH guard AS (SELECT 1 WHERE :is_admin)` and downstream CTEs gate on `EXISTS (SELECT 1 FROM guard)` (B4 fix); is_admin flows from `_user` only, not from payload.**

## B4 Acceptance Test (must be exercised in Plan 10 — tenant-isolation.spec.ts or a new RBAC scenario):

- `POST /api/request/manage_org_units/grow_org_depth_and_add_child` with `session.roles=['team_manager']`, `session.is_admin=false`, valid `tenant_id`, valid `parent_id`, `new_depth=2`, `new_name='forged'`.
- Expected: response has zero result rows OR explicit `{"error":"forbidden"}`.
- Expected: post-call psql `SELECT org_depth FROM tenant WHERE id=$1` returns the value from BEFORE the call (unchanged).
- Expected: post-call psql `SELECT count(*) FROM org_unit WHERE name='forged' AND tenant_id=$1` returns 0.
</verification>

<success_criteria>
- [ ] app/pages/admin/manage_org_units.yaml passes node tools/check-queries.mjs.
- [ ] All Task 1 + Task 2 verify-command tokens present.
- [ ] Phase 1 leaf-guard delete logic byte-equal.
- [ ] Phase 1 create_org_unit + rename_org_unit unchanged in body.
- [ ] Grow-depth path uses the single transactional grow_org_depth_and_add_child request (no separate UPDATE then INSERT — that would leave an inconsistent state on partial failure).
- [ ] **Layer-4 admin gate present: `WITH guard AS (SELECT 1 WHERE :is_admin)` + downstream CTEs gate on guard (B4).**
- [ ] **Pure Pattern A action dispatch: three separate columns, no HTML `data-action` (W6).**
- [ ] **must_haves.truths documents the foundational ROST-04 support relationship even though `requirements: []` (W5).**
</success_criteria>

<output>
After completion, create `.planning/phases/02-org-people/02-03-SUMMARY.md` describing: which requests were upgraded vs. left untouched; the verbatim recursive CTE; **the B4 admin-gate CTE shape (`WITH guard AS (SELECT 1 WHERE :is_admin)`) and the acceptance test result (forged POST returns zero rows / unchanged org_depth)**; the exact three-column dispatch table for the W6 Pattern A implementation; whether D-02 visibility hid all controls for a non-owning team_manager during smoke (visual confirmation via dev login session, optional); the output of `node tools/check-queries.mjs`.
</output>

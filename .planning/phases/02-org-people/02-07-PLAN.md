---
phase: 02-org-people
plan: 07
type: execute
wave: 2
depends_on: [01, 02, 04]
files_modified:
  - app/pages/admin/team_detail.yaml
  - app/pages/my_profile.yaml
  - app/plugins/shifty-roster/src/connections/requests/CreateMembership.js
autonomous: true
requirements:
  - ROST-04
  - ROST-05
  - ROST-06
user_setup: []

must_haves:
  truths:
    - "team_detail.yaml at /team_detail?id={uuid} shows team metadata, the embedded member roster (scoped via membership join), an Add-member modal, and a Phase 3 placeholder card (D-03, D-05, D-08)"
    - "my_profile.yaml renders the 24-swatch color picker for every authenticated user; UPDATE soldier.color WHERE user_id = _user.user_id AND tenant_id = _user.tenant_id (D-16)"
    - "CreateMembership handler INSERTs into membership using the SELECT-driven safe form (cross-tenant cross-check) + writes a schedule_audit row + bumps org_unit.last_color_index when needed"
    - "Removing a membership is also exposed (button on team_detail member rows; calls a sibling RemoveMembership KnexRaw with tenant_id-filtered WHERE) with caller_team_ids sourced literally from `_user: team_ids` — verify-command grep gate locks the binding so future edits don't accidentally rebind it to a forgeable source (W7 fix)"
    - "Membership write paths are auto-covered by tests/e2e/cross-tenant-leak.spec.ts"
  artifacts:
    - path: app/pages/admin/team_detail.yaml
      provides: "Team metadata + scoped member roster + Add-member Modal + Phase 3 placeholder"
      contains: "type: CreateMembership"
    - path: app/pages/my_profile.yaml
      provides: "Self-service color override via shared color_swatches block (D-16)"
      contains: "_ref: ../blocks/color_swatches.yaml"
    - path: app/plugins/shifty-roster/src/connections/requests/CreateMembership.js
      provides: "Safe membership INSERT using SELECT-driven cross-tenant cross-check"
      contains: "FROM soldier s, org_unit ou"
  key_links:
    - from: app/pages/admin/team_detail.yaml
      to: membership
      via: "CreateMembership plugin request invoked from Add-member Modal onOk"
      pattern: "type: CreateMembership"
    - from: app/pages/admin/team_detail.yaml
      to: remove_membership.payload.caller_team_ids
      via: "Literal `_user: team_ids` binding (W7 — verify-command-locked)"
      pattern: "_user: team_ids"
    - from: app/pages/my_profile.yaml
      to: soldier
      via: "UPDATE soldier SET color WHERE user_id AND tenant_id (both from _user)"
      pattern: "WHERE user_id = :user_id AND tenant_id = :tenant_id"
---

<objective>
Land the multi-team membership write path on team_detail and the soldier-facing color override on my_profile. CreateMembership handler (plan 02 stub) gets its DB body. The team_detail page shows the team's scoped roster + Add-member Modal that invokes CreateMembership; member removal is a sibling KnexRaw RemoveMembership wired on each row.

my_profile is the simplest page in Phase 2: every authenticated role reaches it (no `auth.roles` filter beyond `auth.pages.protected: true` from Phase 1 lowdefy.yaml line 24). It renders the same color_swatches block as soldier_detail (UI-SPEC §"Page 7"), saves via a single tenant-scoped UPDATE on soldier.

Phase 2 places NO write path on the soldier_detail Teams MultipleSelector (deferred in plan 06 — UI is read-only there). All membership add/remove is concentrated on team_detail in this plan. This is a deliberate scope choice: forcing add/remove through the team page means the manager always sees the team's current composition before acting, and the audit trail is unambiguous about which team the change affected.

**W7 fix — the remove_membership payload binding for caller_team_ids is locked.** The Layer-4 SQL gate `(:is_admin OR :team_id = ANY(:caller_team_ids))` is only as good as the source of `caller_team_ids`. If a future edit accidentally rebinds it to `_state.something` or `_input.something`, the gate becomes forgeable. Task 2's verify command grep-asserts that the YAML payload contains the literal string `_user: team_ids` (binding team_ids from session, not from anywhere else) — and `must_haves.truths` documents the requirement so the surface is traceable in code review.

Purpose: closes ROST-04 (multi-team membership), ROST-06 (color override), and the D-08 add/remove audit path. Together with plan 06, Wave 2 finishes the soldier/team CRUD surface ahead of plan 08's CSV import.

Output: 2 new pages + 1 fleshed-out CreateMembership handler; manage_org_units tree-table still passes; check-queries gate still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-org-people/02-CONTEXT.md
@.planning/phases/02-org-people/02-RESEARCH.md
@.planning/phases/02-org-people/02-PATTERNS.md
@.planning/phases/02-org-people/02-UI-SPEC.md
@app/pages/admin/manage_invites.yaml
@app/pages/admin/manage_org_units.yaml
@app/pages/dashboards/my_dashboard.yaml
@app/pages/admin/soldier_detail.yaml
@app/blocks/color_swatches.yaml
@app/plugins/shifty-roster/src/helpers/palette.js
@app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js
@db/migrations/0002_tenancy_and_org.up.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill in CreateMembership.js handler with the SELECT-driven safe INSERT (cross-tenant cross-check)</name>
  <files>app/plugins/shifty-roster/src/connections/requests/CreateMembership.js</files>
  <read_first>
    - .planning/phases/02-org-people/02-RESEARCH.md section "Membership (Multi-Team) — Constraints" (the SELECT-driven INSERT pattern with tenant cross-check)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Tenant Isolation — Layer 4"
    - app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js (plan 06 — reference for transaction + audit pattern)
    - app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js
    - db/migrations/0002_tenancy_and_org.up.sql lines 99–111 (membership table, UNIQUE(soldier_id, org_unit_id) constraint)
  </read_first>
  <action>
    Fill the body of CreateMembership.js. Keep the plan-02 guard-clause prelude (tenant_id, actor_user_id, caller_team_ids, is_admin). Required payload fields per .schema (already declared in plan 02): `soldier_id`, `team_id`, optional `role` defaulting to 'member'.

    Inside `db.transaction(async trx => { ... })`:

    1. Layer-4 scope check (admin OR team_manager whose team_ids includes team_id):
       const canWrite = is_admin || caller_team_ids.includes(team_id);
       if (!canWrite) throw new Error('CreateMembership: access denied — team not in caller scope');

    2. SELECT-driven safe INSERT per RESEARCH §"Membership — Constraints":
       INSERT INTO membership (tenant_id, soldier_id, org_unit_id, role)
       SELECT s.tenant_id, s.id, ou.id, :role
       FROM soldier s, org_unit ou
       WHERE s.id = :soldier_id AND ou.id = :team_id
         AND s.tenant_id = :tenant_id AND ou.tenant_id = :tenant_id
         AND s.status = 'active'                     -- archived soldiers cannot be added to teams (ROST-05)
       ON CONFLICT (soldier_id, org_unit_id) DO NOTHING
       RETURNING id, tenant_id, soldier_id, org_unit_id;
       The pattern guarantees a row is inserted ONLY if BOTH the soldier and the org_unit exist in the same tenant; ON CONFLICT makes the operation idempotent.

    3. If RETURNING produces zero rows: either the soldier or the team doesn't exist in tenant scope, OR membership already exists. Distinguish by querying once more:
       SELECT EXISTS(SELECT 1 FROM membership WHERE soldier_id = :soldier_id AND org_unit_id = :team_id AND tenant_id = :tenant_id) AS already_member;
       If already_member → return `{ success: true, already_member: true }` (idempotent, no error).
       Else → throw 'CreateMembership: soldier or team not found in tenant scope'.

    4. Round-robin color bump: if this is the soldier's FIRST membership in this team_id (i.e. the INSERT returned a row), update `org_unit.last_color_index` with a `SELECT ... FOR UPDATE` lock (same pattern as plan 06 CreateSoldier) — but ONLY if the soldier was just assigned a color from PALETTE[0] (the no-team default in CreateSoldier). Otherwise the soldier keeps their existing color (D-15 "Membership added to a team that already has a color → soldier keeps their current color"). Simplest implementation: check `soldier.color` against PALETTE[0]; if equal, the soldier likely has no real color yet — assign one. If unequal, do nothing.

       Recommended simplification given Phase 2 scope: DO NOT bump the team's last_color_index on membership add. Only CreateSoldier (plan 06) drives the round-robin; membership-add for an existing soldier preserves their color. This matches RESEARCH §"Color Palette — Assignment flow" item 3 ("soldier keeps their current color (no recolor on team-add)"). Document this explicitly in the handler comment.

    5. Write schedule_audit row: `to_state: 'membership_added'`, payload `{ soldier_id, team_id, role }`.

    6. Return `{ success: true, membership_id: row.id }`.

    Also extend CreateMembership.schema (plan 02 declared `['soldier_id', 'team_id']` required) to include `role` as an optional enum: `role: { type: 'string', enum: ['unit_admin', 'team_manager', 'member', 'viewer'], default: 'member' }` — matches the CHECK constraint on membership.role from migration 0002.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('app/plugins/shifty-roster/src/connections/requests/CreateMembership.js','utf8'); const must=['canWrite','caller_team_ids.includes','FROM soldier s, org_unit ou','s.tenant_id = :tenant_id','ou.tenant_id = :tenant_id',\"s.status = 'active'\",'ON CONFLICT (soldier_id, org_unit_id) DO NOTHING','membership_added','already_member']; for (const m of must) if (!t.includes(m)) { console.error('MISSING:',m); process.exit(1); } if (t.includes(\"todo: 'plan-\")) { console.error('still has stub'); process.exit(2); } require('child_process').execSync('node --check app/plugins/shifty-roster/src/connections/requests/CreateMembership.js'); console.log('OK');" &amp;&amp; node --test app/plugins/shifty-roster/tests/canonicalize.test.mjs app/plugins/shifty-roster/tests/palette.test.mjs app/plugins/shifty-roster/tests/role-tag.test.mjs</automated>
  </verify>
  <done>CreateMembership.js body uses the SELECT-driven INSERT pattern with both tenant cross-checks AND the `s.status = 'active'` filter as one canonical SQL token (W4 fix); ON CONFLICT DO NOTHING makes it idempotent; Layer-4 caller_team_ids gate fires before the INSERT; audit row written; existing unit tests still pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create app/pages/admin/team_detail.yaml — team metadata + scoped member AgGrid + Add-member Modal + remove-member action + Phase 3 placeholder. caller_team_ids in remove_membership.payload binds literally to `_user: team_ids` (W7).</name>
  <files>app/pages/admin/team_detail.yaml</files>
  <read_first>
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Page 4: team_detail/{id}" (4-card wireframe: team metadata, members AgGrid, Phase 3 placeholder)
    - .planning/phases/02-org-people/02-RESEARCH.md section "soldier CRUD — All soldiers under unit X query" (recursive CTE for sub-tree soldiers — used here scoped to a single org_unit_id, no recursion needed since we read direct memberships)
    - .planning/phases/02-org-people/02-PATTERNS.md section "team_detail.yaml" (analogs to manage_invites + admin_dashboard)
    - app/pages/admin/manage_soldiers.yaml (plan 04 — column shape for the member AgGrid; reuse the same column shape including color dot)
  </read_first>
  <action>
    Create app/pages/admin/team_detail.yaml.

    Top-level:
      id: team_detail
      type: PageHeaderMenu
      auth: { roles: [unit_admin, team_manager] }
      properties: { title: '{{ team.name }} | צוות' }

    requests:

    1. get_team — KnexRaw, payload {tenant_id: _user.tenant_id, team_id: _input.id}:
       SELECT ou.id, ou.name, ou.parent_id, ou.level, ou.last_color_index,
              (SELECT name FROM org_unit p WHERE p.id = ou.parent_id) AS parent_name
       FROM org_unit ou
       WHERE ou.tenant_id = :tenant_id AND ou.id = :team_id

    2. list_team_members — KnexRaw, payload {tenant_id: _user.tenant_id, team_id: _input.id}:
       SELECT s.id, s.display_name, s.color, s.seniority, s.role_tags, s.status, au.email, m.id AS membership_id
       FROM soldier s
       JOIN membership m ON m.soldier_id = s.id
       LEFT JOIN app_user au ON au.id = s.user_id
       WHERE s.tenant_id = :tenant_id
         AND m.tenant_id = :tenant_id
         AND m.org_unit_id = :team_id
         AND s.status = 'active'
       ORDER BY s.display_name

    3. list_addable_soldiers — KnexRaw, payload {tenant_id: _user.tenant_id, team_id: _input.id}:
       SELECT s.id AS value, s.display_name AS label
       FROM soldier s
       WHERE s.tenant_id = :tenant_id
         AND s.status = 'active'
         AND NOT EXISTS (
           SELECT 1 FROM membership m
           WHERE m.soldier_id = s.id AND m.org_unit_id = :team_id AND m.tenant_id = :tenant_id
         )
       ORDER BY s.display_name

    4. create_membership_request — type: CreateMembership (plugin). payload: soldier_id from _state.add_member_form.soldier_id, team_id from _input.id, role 'member', tenant_id/caller_team_ids/is_admin from _user.

    5. remove_membership — KnexRaw, payload:
        tenant_id: { _user: tenant_id }
        membership_id: { _state: remove_target_membership_id }
        team_id: { _input: id }
        **caller_team_ids: { _user: team_ids }   # W7 fix — bind LITERALLY from session, NEVER from _state/_input/_payload sub-paths**
        is_admin: { _user: is_admin }            # or _array.includes on _user.roles — mirror Plan 06's CreateSoldier
       query:
       DELETE FROM membership
       WHERE id = :membership_id
         AND org_unit_id = :team_id
         AND tenant_id = :tenant_id
         AND (:is_admin OR :team_id = ANY(:caller_team_ids))
       RETURNING soldier_id;
       Then a chained audit_write call (see Phase 1 AuditWrite plugin) writing to_state='membership_removed'. NOTE: this is a DELETE — for Phase 2 we accept removing a membership (not archiving) because the membership table has no archive concept. The soldier's assignment history (in `assignment` table from Phase 3+) keeps soldier_id+org_unit_id at assignment time, so historical correctness is preserved by the snapshot pattern, not by retaining membership rows.

    blocks:

    Card 1 — "פרטי צוות" (Title level 2):
      - Title (level 1): { _nunjucks: { template: '{{ name }} | צוות', on: { name: { _request: get_team[0].name } } } }
      - Read-only display of: name, parent_name (rendered as breadcrumb via Nunjucks), level.

    Card 2 — "חברי צוות" (Title level 2):
      - Toolbar Box with Button "+ הוסף חבר לצוות" type: primary, icon AiOutlinePlus, onClick SetState { show_add_member_modal: true }.
      - AgGridAlpine block (id: team_members_grid):
        - enableRtl: true, pagination: false (teams are small), rowSelection: single
        - rowData: { _request: list_team_members }
        - columnDefs: same color/name/email/seniority/role_tags/status shape as plan 04 manage_soldiers, plus a final "פעולות" column with a single-affordance icon cellRenderer per RESEARCH P9 Pattern A:
            cellRenderer: nunjucks template emitting a single `<span title="הסר" style="cursor:pointer;color:#FF4D4F">🗑</span>` (no data-action attribute)
        - events.onCellClicked: dispatch on `_event.column.field == 'actions'`; SetState remove_target_membership_id = _event.data.membership_id; open Confirm modal (title "הסרת חבר מצוות", body "...יוסר מצוות..." per UI-SPEC), on confirm → Request remove_membership → Request list_team_members refresh → Message "החייל הוסר מהצוות".
        - events.onRowClicked: dispatch only when `_event.column.field != 'actions'` (otherwise the remove cell would also trigger row-click); on the body columns, Link to soldier_detail?id={_event.data.id}.

    Card 3 — Phase 3 placeholder per D-03:
      Result block: status: info, icon: AiOutlineClockCircle, title: 'פתח תבנית משמרות בשלב הבא'.

    Modal: add_member_modal:
      properties: { visible: { _state: show_add_member_modal }, title: 'הוספת חבר לצוות', okText: 'הוסף', cancelText: 'ביטול' }
      blocks: Form (add_member_form):
        - MultipleSelector add_member_form.soldier_id (label: 'חייל', options from list_addable_soldiers, mode: single — UI-SPEC allows multi-add later; Phase 2 ships single-pick for simplicity)
      events.onOk: [Validate, Request create_membership_request, Request list_team_members, Request list_addable_soldiers, SetState close, Reset form, Message success "החייל נוסף לצוות"]
      events.onCancel: SetState close

    Empty-state Result block when list_team_members.length == 0:
      Title: 'הצוות ריק', body: 'הוסף חברים כדי לראות אותם כאן', icon: AiOutlineInbox, extra: a button mirroring "+ הוסף חבר לצוות".
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('app/pages/admin/team_detail.yaml','utf8'); const must=['id: team_detail','roles:','team_manager','get_team','list_team_members','list_addable_soldiers','create_membership_request','type: CreateMembership','remove_membership','DELETE FROM membership','WHERE id = :membership_id','org_unit_id = :team_id','tenant_id = :tenant_id','(:is_admin OR :team_id = ANY(:caller_team_ids))',\"s.status = 'active'\",'m.org_unit_id = :team_id','AiOutlineClockCircle','פתח תבנית משמרות בשלב הבא','show_add_member_modal','_user: team_ids']; for (const m of must) if (!y.includes(m)) { console.error('MISSING:',m); process.exit(1); } if (!y.match(/caller_team_ids\\s*:\\s*\\{\\s*_user:\\s*team_ids\\s*\\}/)) { console.error('W7 fix MISSING: caller_team_ids must bind literally to `_user: team_ids` — found in some other form. Future edits could rebind it to a forgeable source.'); process.exit(2); } console.log('OK team_detail');" &amp;&amp; node tools/check-queries.mjs</automated>
  </verify>
  <done>team_detail.yaml exists; all four cards present; CreateMembership wired; remove_membership KnexRaw has Layer-4 SQL gate; **`caller_team_ids` payload binding is literally `{ _user: team_ids }` — locked by W7 grep gate; `s.status = 'active'` written as one canonical SQL token (W4 fix)**; check-queries gate green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/pages/my_profile.yaml — every-role color override per D-16 with the shared color_swatches block</name>
  <files>app/pages/my_profile.yaml</files>
  <read_first>
    - app/pages/dashboards/my_dashboard.yaml (PATTERNS analog "exact" — auth shape for all-roles page)
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Page 7: my_profile" (minimal wireframe with explicit Phase 6/7 placeholder)
    - .planning/phases/02-org-people/02-PATTERNS.md section "my_profile.yaml"
    - app/blocks/color_swatches.yaml (plan 06 Task 2 output)
  </read_first>
  <action>
    Create app/pages/my_profile.yaml.

    Top-level:
      id: my_profile
      type: PageHeaderMenu
      auth: { roles: [unit_admin, team_manager, member, viewer] }
      properties: { title: 'הפרופיל שלי | shifty' }

    requests:

    1. load_my_soldier — KnexRaw, payload {user_id: _user.user_id, tenant_id: _user.tenant_id}:
       SELECT id, display_name, color FROM soldier
       WHERE user_id = :user_id AND tenant_id = :tenant_id;
       (Soldier-row lookup keyed by app_user.id; this works because CreateSoldier in plan 06 wires soldier.user_id = app_user.id at create time, and Phase 1 D-08's signup_with_invite redemption flow also writes both. If the user signed up via Phase 1 founding-admin self-signup but the soldier row was never created, this returns 0 rows — handled by the empty-state below.)

    2. update_my_color — KnexRaw, payload {user_id: _user.user_id, tenant_id: _user.tenant_id, color: _state.selected_color_hex}:
       UPDATE soldier
       SET color = :color, updated_at = now()
       WHERE user_id = :user_id AND tenant_id = :tenant_id
       RETURNING id, color;
       Both user_id AND tenant_id flow from _user (session) — NEVER from _state or _input (Pitfall P1). The WHERE clause hard-binds to the caller's own row.

    blocks:

    1. Title (level 1): הפרופיל שלי.

    2. Card "צבע אישי" (Title level 2):
       - Paragraph: 'הצבע שלך מופיע בלוח המשמרות. בחר צבע מהפלטה שלמטה.'
       - _ref: ../blocks/color_swatches.yaml (path is `../blocks/color_swatches.yaml` since my_profile.yaml lives at app/pages/, not app/pages/admin/ — confirm path depth at write time)
       - Button "שמור צבע" type: primary
         events.onClick: [Request update_my_color, Message success "הצבע עודכן"]

    3. Empty-state Result (visible when load_my_soldier.length == 0):
       Status: info, title: 'בחר את הצבע שלך', subTitle: 'הצבע יופיע בלוח המשמרות שלך'.

    4. Phase 6/7 placeholder Card (per UI-SPEC §"Page 7" last block):
       Result: status: info, icon: AiOutlineClockCircle, title: 'עוד הגדרות פרופיל יתווספו בשלבים הבאים'.

    On mount, SetState selected_color_index = the index of the current color in PALETTE. The color_swatches block from plan 06 already handles selected-swatch highlighting via _state.selected_color_index, so the picker auto-shows the current color highlighted on load.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('app/pages/my_profile.yaml','utf8'); const must=['id: my_profile','roles:','unit_admin','team_manager','member','viewer','load_my_soldier','update_my_color','UPDATE soldier','SET color = :color','WHERE user_id = :user_id AND tenant_id = :tenant_id','_user: user_id','_user: tenant_id','_ref: ../blocks/color_swatches.yaml','שמור צבע','AiOutlineClockCircle']; for (const m of must) if (!y.includes(m)) { console.error('MISSING:',m); process.exit(1); } console.log('OK my_profile');" &amp;&amp; node tools/check-queries.mjs</automated>
  </verify>
  <done>my_profile.yaml exists with all four roles; tenant_id AND user_id both from _user; _ref path to color_swatches resolves correctly; check-queries gate green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CreateMembership | Layer 1 (session) + Layer 4 (caller_team_ids gate) + SELECT-driven INSERT with both tenant cross-checks (denormalized membership.tenant_id is set BY the SELECT, never trusted from payload). |
| Browser → remove_membership | Layer 1 + Layer 4 SQL: `(:is_admin OR :team_id = ANY(:caller_team_ids))`. **caller_team_ids is bound LITERALLY to `_user: team_ids` (session) — verified by W7 grep gate at every plan-checker run.** |
| Browser → my_profile update_my_color | Hard-bound: both user_id and tenant_id from _user; WHERE clause ensures caller can only update their own row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing (tenant_id forgery) | All three pages + CreateMembership | mitigate | Every payload sources tenant_id from `_user: tenant_id` only. CreateMembership's SELECT-driven INSERT uses `s.tenant_id` and `ou.tenant_id` from the database directly — payload tenant_id is only used in the WHERE filter, never written. |
| T-02-02 | Information Disclosure | list_team_members, list_addable_soldiers | mitigate | Both filter on `s.tenant_id = :tenant_id AND m.tenant_id = :tenant_id` explicitly; RLS catches forgery. |
| T-02-06 | Elevation of Privilege (team_manager adding members to other teams) | CreateMembership + remove_membership | mitigate | CreateMembership.canWrite gate (`is_admin || caller_team_ids.includes(team_id)`) fires before INSERT. remove_membership SQL includes `(:is_admin OR :team_id = ANY(:caller_team_ids))`. **Both rely on `caller_team_ids` flowing from session — W7 fix locks this binding so future edits cannot accidentally weaken it.** Plan 10 has an explicit RBAC E2E test against this. |
| T-02-06 | Privilege escalation via my_profile (user updating another user's color) | update_my_color | mitigate | WHERE clause is `user_id = :user_id AND tenant_id = :tenant_id` with both from session. A caller cannot update any row but their own. |

</threat_model>

<verification>
- CreateMembership.js body uses SELECT-driven INSERT with both tenant cross-checks.
- ON CONFLICT (soldier_id, org_unit_id) DO NOTHING makes the operation idempotent.
- team_detail.yaml has the 4-card layout including Phase 3 placeholder.
- my_profile.yaml is reachable by all 4 roles; both user_id and tenant_id from _user.
- check-queries gate green.
- All plan-02 unit tests still pass.
- **Status filter `s.status = 'active'` written as one canonical SQL token in all three relevant YAML SELECTs (W4 fix).**
- **remove_membership.payload.caller_team_ids binds literally to `_user: team_ids` — verify-command grep regex `caller_team_ids\s*:\s*\{\s*_user:\s*team_ids\s*\}` matches (W7 fix).**
</verification>

<success_criteria>
- [ ] CreateMembership.js no longer has the `todo: 'plan-07'` placeholder.
- [ ] team_detail.yaml has every Task 2 verify token; remove_membership has Layer-4 SQL.
- [ ] **W7: caller_team_ids in remove_membership.payload is literally `{ _user: team_ids }` (grep-locked).**
- [ ] my_profile.yaml has every Task 3 verify token; auth includes all four roles.
- [ ] check-queries gate green; unit tests still green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-org-people/02-07-SUMMARY.md` describing: the CreateMembership SQL body (verbatim INSERT-FROM-SELECT); the remove_membership Layer-4 gate; **the W7-locked caller_team_ids binding (verbatim YAML snippet showing `caller_team_ids: { _user: team_ids }`)**; the my_profile UPDATE WHERE clause; output of check-queries.
</output>

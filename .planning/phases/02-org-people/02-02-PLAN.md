---
phase: 02-org-people
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/plugins/shifty-roster/package.json
  - app/plugins/shifty-roster/src/types.js
  - app/plugins/shifty-roster/src/connections.js
  - app/plugins/shifty-roster/src/helpers/palette.js
  - app/plugins/shifty-roster/src/helpers/canonicalize.js
  - app/plugins/shifty-roster/src/helpers/role-tag.js
  - app/plugins/shifty-roster/src/dispatch/resend.js
  - app/plugins/shifty-roster/src/connections/requests/ParseCsvAndValidate.js
  - app/plugins/shifty-roster/src/connections/requests/CommitRosterImport.js
  - app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js
  - app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js
  - app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js
  - app/plugins/shifty-roster/src/connections/requests/CreateMembership.js
  - app/plugins/shifty-roster/src/connections/requests/InviteLater.js
  - app/plugins/shifty-roster/tests/canonicalize.test.mjs
  - app/plugins/shifty-roster/tests/palette.test.mjs
  - app/plugins/shifty-roster/tests/role-tag.test.mjs
  - app/package.json
  - app/lowdefy.yaml
autonomous: true
requirements:
  - ROST-02
  - ROST-06
  - ROST-07
  - ROST-11

user_setup: []

must_haves:
  truths:
    - "A new Lowdefy plugin `shifty-roster` exists with 7 request handlers registered (D-14)"
    - "canonicalizeText strips U+2019, U+200E, U+200F, U+202A–U+202E and PRESERVES Hebrew gershayim U+05F4 + ASCII apostrophe (D-12)"
    - "pickNextColor(lastIndex) returns (lastIndex + 2) mod 24, with -1 sentinel mapping to 0 (D-15)"
    - "canonicalizeRoleTag produces output matching the DB regex `^[a-z][a-z0-9-]*$` (D-13, ROST-07)"
    - "The plugin is declared in BOTH app/package.json and app/lowdefy.yaml plugins list (Pitfall P3 / Phase 1 D-08 dual-declaration rule)"
    - "Unit tests cover the kibbutz canary (`נועם ג'לאל` → `נועם גלאל`) and all 7 stripped codepoints"
    - "PALETTE is byte-equal to the UI-SPEC §'Color B' FROZEN 24-hex array (no perceptual-distance re-tuning at write time) — verified by enumerated hex match, not just length+uniqueness (W3 fix)"
  artifacts:
    - path: app/plugins/shifty-roster/package.json
      provides: "ESM module manifest with exports map and papaparse dep"
      contains: "shifty-roster"
    - path: app/plugins/shifty-roster/src/helpers/palette.js
      provides: "Frozen 24-color PALETTE export + pickNextColor"
      contains: "export const PALETTE"
    - path: app/plugins/shifty-roster/src/helpers/canonicalize.js
      provides: "canonicalizeText smart-quote+bidi stripper"
      contains: "canonicalizeText"
    - path: app/plugins/shifty-roster/src/helpers/role-tag.js
      provides: "canonicalizeRoleTag kebab-case normalizer"
      contains: "canonicalizeRoleTag"
    - path: app/plugins/shifty-roster/src/dispatch/resend.js
      provides: "Shared Resend dispatcher with NOTF-07 backoff (1s/4s/16s)"
      contains: "sendInvite"
    - path: app/plugins/shifty-roster/tests/canonicalize.test.mjs
      provides: "Smart-quote + bidi + gershayim preservation unit tests"
      contains: "נועם גלאל"
  key_links:
    - from: app/lowdefy.yaml
      to: app/plugins/shifty-roster/
      via: "plugins: list with version 'file:../../plugins/shifty-roster'"
      pattern: "shifty-roster"
    - from: app/package.json
      to: app/plugins/shifty-roster/
      via: "dependencies entry 'shifty-roster': 'file:./plugins/shifty-roster'"
      pattern: "shifty-roster"
    - from: app/plugins/shifty-roster/src/connections.js
      to: ./connections/requests/*.js
      via: "ESM import + default-export aggregator"
      pattern: "import .* from './connections/requests"
---

<objective>
Stand up the `shifty-roster` Lowdefy custom plugin — directory layout, manifest, type registry, aggregator, 7 request-handler stubs (with full guard-clauses + tenant-derivation but DB writes deliberately minimal — the deep implementations land in plans 06 and 08), and three pure-JS helper modules (`palette.js`, `canonicalize.js`, `role-tag.js`) with corresponding `node:test` unit tests. The plugin must be wired into BOTH `app/package.json` `dependencies` AND `app/lowdefy.yaml` plugins list — missing either side causes "Block type X not defined" at runtime (Pitfall P3, verified in skill `reference/09-plugins.md` line 7).

The three helpers are the write-time defenses for ROST-02/06/07/11. Their byte-for-byte correctness is critical:
- `canonicalizeText` (D-12): strip set is exactly `[U+2019, U+200E, U+200F, U+202A, U+202B, U+202C, U+202D, U+202E]`; ALL OTHER characters preserved (especially Hebrew gershayim U+05F4 and ASCII `'`/`"`).
- `pickNextColor` (D-15): pure function, `(lastIndex + 2) % 24`, sentinel `-1` → returns 0.
- `canonicalizeRoleTag` (D-13): output regex MUST be byte-equal to DB CHECK `^[a-z][a-z0-9-]*$` from migration 0011 (plan 01).
- `PALETTE` (D-14, D-15, UI-SPEC §"Color B"): MUST be the FROZEN 24-element array exactly: `["#1F77B4", "#FF7F0E", "#2CA02C", "#D62728", "#9467BD", "#8C564B", "#E377C2", "#7F7F7F", "#BCBD22", "#17BECF", "#AEC7E8", "#FFBB78", "#98DF8A", "#FF9896", "#C5B0D5", "#C49C94", "#F7B6D2", "#C7C7C7", "#DBDB8D", "#9EDAE5", "#393B79", "#637939", "#8C6D31", "#843C39"]`. Verification asserts the literal byte-equal array (not just length+uniqueness — W3 fix). The order IS the adjacency identifier; do not reorder under any cosmetic pretext.

The 7 request handlers stub their permanent shapes (guard clauses, dynamic-knex import, schema export, connectionType='Knex') so downstream plans 06/07/08 only need to fill in the SQL body — the API surface is frozen here.

Purpose: every Phase-2 mutating page in waves 1–3 routes through one of these 7 requests; the helpers are imported from inside the request bodies. Establishing the contracts now means the rest of the phase is filling implementations into a fixed shape, not negotiating shape.

Output: a complete `app/plugins/shifty-roster/` tree, two updated config files (`app/package.json` + `app/lowdefy.yaml`), three green `node:test` files asserting the helper contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-org-people/02-CONTEXT.md
@.planning/phases/02-org-people/02-RESEARCH.md
@.planning/phases/02-org-people/02-PATTERNS.md
@.planning/phases/02-org-people/02-UI-SPEC.md
@app/plugins/shifty-audit-writer/package.json
@app/plugins/shifty-audit-writer/src/types.js
@app/plugins/shifty-audit-writer/src/connections.js
@app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js
@app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs
@app/plugins/shifty-auth/src/auth/providers.js
@app/lowdefy.yaml
@app/package.json
@.claude/skills/lowdefy/reference/09-plugins.md
@.claude/skills/lowdefy/reference/10-deployment.md

<interfaces>
<!-- The 24-color FROZEN palette from UI-SPEC §"Color B" — index order IS the adjacency identifier. -->
<!-- Do NOT reorder; downstream plans persist these as numeric indices. -->

UI-SPEC §"Color B" frozen palette (index 0..23):
  0:#1F77B4  1:#FF7F0E  2:#2CA02C  3:#D62728  4:#9467BD  5:#8C564B
  6:#E377C2  7:#7F7F7F  8:#BCBD22  9:#17BECF  10:#AEC7E8 11:#FFBB78
  12:#98DF8A 13:#FF9896 14:#C5B0D5 15:#C49C94 16:#F7B6D2 17:#C7C7C7
  18:#DBDB8D 19:#9EDAE5 20:#393B79 21:#637939 22:#8C6D31 23:#843C39

From app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js (the canonical request-handler shape):
- Module starts with comment "Tenant ID from request.user (session) — NEVER from request.properties. Layer-4 defense."
- Dynamic `const { default: knex } = await import('knex');` inside the function body (not top-level).
- Guard clauses throw on missing `request.user?.tenant_id`, missing `request.user?.user_id`.
- `try { ... } finally { await db.destroy(); }` lifecycle.
- Trailing `Handler.schema = {...}; Handler.connectionType = 'Knex'; export default Handler;`.

From app/lowdefy.yaml (current plugins list ~lines 14–17):
  - name: 'shifty-auth'
    version: 'file:../../plugins/shifty-auth'
  - name: 'shifty-audit-writer'
    version: 'file:../../plugins/shifty-audit-writer'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create plugin manifest, types registry, connections aggregator, and dispatch helper</name>
  <files>
    app/plugins/shifty-roster/package.json,
    app/plugins/shifty-roster/src/types.js,
    app/plugins/shifty-roster/src/connections.js,
    app/plugins/shifty-roster/src/dispatch/resend.js
  </files>
  <read_first>
    - app/plugins/shifty-audit-writer/package.json (full file — PATTERNS analog "exact")
    - app/plugins/shifty-audit-writer/src/types.js (full file — PATTERNS analog "exact")
    - app/plugins/shifty-audit-writer/src/connections.js (full file — PATTERNS analog "exact")
    - app/plugins/shifty-auth/src/auth/providers.js lines 18–45 (createRequire(process.cwd()) idiom — PATTERNS analog for dispatch/resend.js)
    - .planning/phases/02-org-people/02-PATTERNS.md "Pattern Assignments — package.json, types.js, connections.js, dispatch/resend.js"
    - .planning/phases/02-org-people/02-RESEARCH.md §"Plugin scaffold" + §"Magic-Link Invites (Resend + Auth.js)" + §"Resend rate limits"
  </read_first>
  <action>
    Create the four files exactly as the PATTERNS.md analog dictates.

    `app/plugins/shifty-roster/package.json`:
    - `"name": "shifty-roster"`, `"version": "1.0.0"`, `"type": "module"`.
    - `"exports"`: map `./connections`, `./types`, `./helpers/palette`, `./helpers/canonicalize`, `./helpers/role-tag` (omit `./helpers/text` — split into two helper files per PATTERNS).
    - `"dependencies"`: `"knex": "*"`, `"papaparse": "^5.5.3"`, `"resend": "*"`. Use `"*"` for knex and resend so resolution flows through the Lowdefy server's outer node_modules (the same pattern shifty-audit-writer uses for knex). papaparse pins `^5.5.3` because plugin owns this dep.

    `app/plugins/shifty-roster/src/types.js`:
    - Top comment "Plugin type registry for shifty-roster — registers Phase 2 request types".
    - `export default { requests: ['ParseCsvAndValidate', 'CommitRosterImport', 'CreateSoldier', 'UpdateSoldier', 'ArchiveSoldier', 'CreateMembership', 'InviteLater'] };`

    `app/plugins/shifty-roster/src/connections.js`:
    - Import each of the 7 handler files from `./connections/requests/{Name}.js`.
    - `export default { ParseCsvAndValidate, CommitRosterImport, CreateSoldier, UpdateSoldier, ArchiveSoldier, CreateMembership, InviteLater };`

    `app/plugins/shifty-roster/src/dispatch/resend.js`:
    - Implement the `createRequire(process.cwd() + '/package.json')` idiom from `providers.js` lines 28–45 so the SDK resolves from the Lowdefy server's working directory (`/build/.lowdefy/server/`) — pnpm strict isolation breaks `import.meta.url`-based resolution.
    - Export two named functions:
      1. `sendInvite({ email, callbackUrl, locale = 'he', knexTx })` — generates `rawToken = randomBytes(32).toString('hex')` from `node:crypto`, computes `hashedToken = createHash('sha256').update(rawToken + process.env.NEXTAUTH_SECRET).digest('hex')`, inserts into `verification_tokens` (identifier=email.toLowerCase(), token=hashedToken, expires=now()+30min) via the passed-in `knexTx`, then calls `resend.emails.send({ from: process.env.RESEND_FROM_EMAIL, to: [email], subject: 'הזמנה לשיפטי', html, text })` with the RTL-formatted Hebrew template from RESEARCH §"Magic-Link Invites" lines 1219–1230 (`<html dir="rtl" lang="${locale}">` body wrapper, plaintext starts with `‏` (U+200F RLM) prefix). The link URL is `${process.env.NEXTAUTH_URL}/api/auth/callback/email?callbackUrl=${enc(callbackUrl||'/admin_dashboard')}&token=${rawToken}&email=${enc(email.toLowerCase())}`.
      2. `bulkDispatchWithBackoff(rows, onProgress)` — iterates rows[] sequentially with `await sleep(500)` between calls (Resend free-tier 2 req/s budget — RESEARCH §"Resend rate limits"), catches 429s, retries with backoff `[1000, 4000, 16000]` ms, accumulates `{ row_index, status: 'sent'|'failed', error? }` and emits `onProgress({ sent, total })` after each row. After 3 retries, the row is recorded as `failed` and the loop continues — never throws out of the loop.

    **IMPORTANT** comment block at the top of `resend.js`: cite Assumption A1 from 02-RESEARCH (sha256 hash algorithm needs a 2-hour spike at plan 08 time; this implementation is the best current understanding; document the risk so plan 08 can confirm before its commit).
  </action>
  <verify>
    <automated>node -e "const pkg=require('./app/plugins/shifty-roster/package.json'); if (pkg.name!=='shifty-roster') process.exit(1); if (pkg.type!=='module') process.exit(2); if (!pkg.exports['./helpers/palette']) process.exit(3); if (!pkg.dependencies.papaparse) process.exit(4); const fs=require('fs'); const types=fs.readFileSync('app/plugins/shifty-roster/src/types.js','utf8'); ['ParseCsvAndValidate','CommitRosterImport','CreateSoldier','UpdateSoldier','ArchiveSoldier','CreateMembership','InviteLater'].forEach(r=>{if(!types.includes(r)) process.exit(5);}); const conn=fs.readFileSync('app/plugins/shifty-roster/src/connections.js','utf8'); ['ParseCsvAndValidate','CommitRosterImport','CreateSoldier','UpdateSoldier','ArchiveSoldier','CreateMembership','InviteLater'].forEach(r=>{if(!conn.includes(r)) process.exit(6);}); const r=fs.readFileSync('app/plugins/shifty-roster/src/dispatch/resend.js','utf8'); ['sendInvite','bulkDispatchWithBackoff','createRequire','randomBytes','sha256','U+200F'].some(()=>true); ['sendInvite','bulkDispatchWithBackoff','createRequire','randomBytes'].forEach(t=>{if(!r.includes(t)){console.error('MISSING:',t);process.exit(7);}}); console.log('OK');"</automated>
  </verify>
  <done>All 4 files exist; package.json validates as JSON with type=module and papaparse pinned; types.js and connections.js name all 7 handlers; resend.js exports sendInvite + bulkDispatchWithBackoff and uses createRequire(process.cwd()) idiom.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the three pure helper modules with unit tests (canonicalize, palette, role-tag) — palette test asserts the literal 24-element FROZEN array byte-equal to UI-SPEC §"Color B" (W3 fix)</name>
  <files>
    app/plugins/shifty-roster/src/helpers/palette.js,
    app/plugins/shifty-roster/src/helpers/canonicalize.js,
    app/plugins/shifty-roster/src/helpers/role-tag.js,
    app/plugins/shifty-roster/tests/canonicalize.test.mjs,
    app/plugins/shifty-roster/tests/palette.test.mjs,
    app/plugins/shifty-roster/tests/role-tag.test.mjs
  </files>
  <behavior>
    - canonicalize.test.mjs MUST PASS the kibbutz canary: `canonicalizeText("נועם ג’לאל") === "נועם גלאל"` (U+2019 stripped to empty string).
    - canonicalize.test.mjs MUST PASS bidi-mark stripping for all of {U+200E, U+200F, U+202A, U+202B, U+202C, U+202D, U+202E}; each test passes a string with one of these codepoints embedded mid-word and asserts the codepoint is gone in the output.
    - canonicalize.test.mjs MUST PASS preservation of Hebrew gershayim U+05F4: `canonicalizeText("סמ״ר דני") === "סמ״ר דני"` (gershayim untouched).
    - canonicalize.test.mjs MUST PASS preservation of ASCII apostrophe U+0027: `canonicalizeText("D'Angelo") === "D'Angelo"`.
    - canonicalize.test.mjs MUST PASS null/undefined → empty string.
    - canonicalize.test.mjs MUST PASS NFC normalization + whitespace collapse: `canonicalizeText("  ABC   DEF  ") === "ABC DEF"`.
    - palette.test.mjs MUST PASS `PALETTE.length === 24` AND every hex matches `/^#[0-9A-F]{6}$/i` AND no duplicate hex (existing baseline check, retained).
    - **palette.test.mjs MUST PASS byte-equal assertion that PALETTE equals the literal 24-element FROZEN array `["#1F77B4", "#FF7F0E", "#2CA02C", "#D62728", "#9467BD", "#8C564B", "#E377C2", "#7F7F7F", "#BCBD22", "#17BECF", "#AEC7E8", "#FFBB78", "#98DF8A", "#FF9896", "#C5B0D5", "#C49C94", "#F7B6D2", "#C7C7C7", "#DBDB8D", "#9EDAE5", "#393B79", "#637939", "#8C6D31", "#843C39"]` — use `assert.deepStrictEqual(PALETTE, EXPECTED_PALETTE)` so a single re-ordered or substituted hex fails the test loudly. This is the W3 fix — uniqueness+length is not enough; the FROZEN order from UI-SPEC §"Color B" must match byte-for-byte. Adjacency identifiers persisted as numeric indices to org_unit.last_color_index depend on this order.**
    - palette.test.mjs MUST PASS `pickNextColor(-1) === 0`, `pickNextColor(0) === 2`, `pickNextColor(22) === 0` (wraparound), `pickNextColor(23) === 1`, `pickNextColor(null) === 0`, `pickNextColor(undefined) === 0`.
    - role-tag.test.mjs MUST PASS `canonicalizeRoleTag("Driving") === "driving"`, `canonicalizeRoleTag("long range comms") === "long-range-comms"`, `canonicalizeRoleTag("--medic--") === "medic"`, `canonicalizeRoleTag("medic’s") === "medics"` (smart-quote stripped through the same rule), `canonicalizeRoleTag(null) === ""`, AND the test verifies every passing output matches the DB CHECK regex `/^[a-z][a-z0-9-]*$/` (or is empty for null/empty input).
  </behavior>
  <read_first>
    - .planning/phases/02-org-people/02-UI-SPEC.md §"Color B. 24-color soldier-calendar palette" lines 119–155 (the FROZEN hex list — copy byte-equal)
    - .planning/phases/02-org-people/02-RESEARCH.md §"Smart-Quote / Bidi-Mark Stripping" (codepoint table + canonicalizer)
    - .planning/phases/02-org-people/02-RESEARCH.md §"Color Palette + Round-Robin Assignment" (pickNextColor semantics)
    - .planning/phases/02-org-people/02-RESEARCH.md §"Role Tag Storage + Autocomplete" (kebabCase semantics)
    - .planning/phases/02-org-people/02-PATTERNS.md "helpers/palette.js", "helpers/canonicalize.js", "helpers/role-tag.js"
    - app/plugins/shifty-audit-writer/tests/audit-writer.test.mjs (PATTERNS analog "exact" — node:test + assert/strict harness; same harness for Phase 2)
    - db/migrations/0011_role_tag.up.sql (the CHECK regex byte-equal target — written by plan 01)
  </read_first>
  <action>
    Write the three helpers first (RED before GREEN — write tests first, watch them fail with `node --test app/plugins/shifty-roster/tests/`, then implement to pass).

    `app/plugins/shifty-roster/src/helpers/palette.js`:
    - Comment header notes UI-SPEC §"Color B" is the source-of-truth and the array order is the adjacency identifier per D-15.
    - `export const PALETTE = [ ... ]` — exactly 24 entries in the order specified in UI-SPEC §"Color B" (copy uppercase hex `#1F77B4`, `#FF7F0E`, ..., `#843C39`).
    - `export function pickNextColor(lastIndex) { if (lastIndex == null || lastIndex < 0) return 0; return (lastIndex + 2) % PALETTE.length; }`
    - `export function colorByIndex(idx) { if (idx == null || idx < 0 || idx >= PALETTE.length) return PALETTE[0]; return PALETTE[idx]; }`

    `app/plugins/shifty-roster/src/helpers/canonicalize.js`:
    - Comment header cites D-12 + ROST-11 + the strip set (U+2019, U+200E, U+200F, U+202A–U+202E) + the preservation rule (gershayim U+05F4, ASCII quotes, all letters).
    - `const STRIP_REGEX = /[’‎‏‪-‮]/g;` — use explicit unicode escapes (NOT the literal characters from RESEARCH which can be lossy in some editors).
    - `export function canonicalizeText(text) { if (text === null || text === undefined) return ''; return String(text).normalize('NFC').replace(STRIP_REGEX, '').replace(/\s+/g, ' ').trim(); }`

    `app/plugins/shifty-roster/src/helpers/role-tag.js`:
    - Comment header cites D-13 + ROST-07 + the DB CHECK regex it must satisfy.
    - `import { canonicalizeText } from './canonicalize.js';` — reuse the smart-quote stripper so role-tag input from a smart-quoted CSV is canonicalized identically.
    - `export function canonicalizeRoleTag(input) { if (!input) return ''; return canonicalizeText(input).toLowerCase().replace(/[\s_]+/g, '-').replace(/[^a-z0-9-]/g, '').replace(/-+/g, '-').replace(/^-+|-+$/g, ''); }`
    - Order of operations matters: canonicalizeText FIRST (NFC + bidi strip), then lowercase, then space/underscore→dash, then drop non-kebab chars, then collapse multi-dash, then trim leading/trailing dashes.

    Unit tests — three files mirroring `audit-writer.test.mjs` harness:
    ```
    import { test } from 'node:test';
    import assert from 'node:assert/strict';
    import { canonicalizeText } from '../src/helpers/canonicalize.js';
    // ... test() blocks for each behavior listed above
    ```
    Each `.test.mjs` file's relative import path must use `../src/helpers/<file>.js`. Use `assert.equal` (not deep) for primitive comparisons; use `assert.match` for regex assertions; **for the PALETTE byte-equal assertion use `assert.deepStrictEqual(PALETTE, EXPECTED_PALETTE)` with the explicit 24-element array literal at the top of palette.test.mjs (W3 fix)**.

    Run `node --test app/plugins/shifty-roster/tests/canonicalize.test.mjs app/plugins/shifty-roster/tests/palette.test.mjs app/plugins/shifty-roster/tests/role-tag.test.mjs` and confirm all tests pass.
  </action>
  <verify>
    <automated>node --test app/plugins/shifty-roster/tests/canonicalize.test.mjs app/plugins/shifty-roster/tests/palette.test.mjs app/plugins/shifty-roster/tests/role-tag.test.mjs &amp;&amp; node -e "const fs=require('fs'); const pt=fs.readFileSync('app/plugins/shifty-roster/tests/palette.test.mjs','utf8'); const EXPECTED=['#1F77B4','#FF7F0E','#2CA02C','#D62728','#9467BD','#8C564B','#E377C2','#7F7F7F','#BCBD22','#17BECF','#AEC7E8','#FFBB78','#98DF8A','#FF9896','#C5B0D5','#C49C94','#F7B6D2','#C7C7C7','#DBDB8D','#9EDAE5','#393B79','#637939','#8C6D31','#843C39']; for (const hx of EXPECTED) { if (!pt.includes(hx)) { console.error('palette.test.mjs MISSING hex from FROZEN list:',hx); process.exit(1); } } if (!pt.match(/deepStrictEqual|deepEqual/)) { console.error('palette.test.mjs MISSING byte-equal array assertion (W3 fix) — must use assert.deepStrictEqual against the literal 24-element FROZEN array'); process.exit(2); } const pf=fs.readFileSync('app/plugins/shifty-roster/src/helpers/palette.js','utf8'); for (const hx of EXPECTED) { if (!pf.includes(hx)) { console.error('palette.js MISSING hex:',hx); process.exit(3); } } console.log('OK byte-equal palette verified');"</automated>
  </verify>
  <done>All three test files exit 0 with all assertions passing; `pickNextColor(22)` returns 0 (wraparound proof); `canonicalizeText("נועם ג’לאל")` returns `"נועם גלאל"`; PALETTE has 24 unique hex entries in the UI-SPEC order; **palette.test.mjs contains a `deepStrictEqual` byte-equal assertion against the literal 24-element FROZEN array AND lists every hex from the UI-SPEC §"Color B" frozen list (W3 fix verified by grep gate)**.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author the 7 request-handler stubs with full guard-clause shells (SQL bodies are minimal — plans 06/07/08 fill them in)</name>
  <files>
    app/plugins/shifty-roster/src/connections/requests/ParseCsvAndValidate.js,
    app/plugins/shifty-roster/src/connections/requests/CommitRosterImport.js,
    app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js,
    app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js,
    app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js,
    app/plugins/shifty-roster/src/connections/requests/CreateMembership.js,
    app/plugins/shifty-roster/src/connections/requests/InviteLater.js
  </files>
  <read_first>
    - app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js (entire file — the canonical shape PATTERNS analog "role-match")
    - .planning/phases/02-org-people/02-PATTERNS.md "Imports / module header pattern", "Guard-clause + tenant-derivation pattern", "Dynamic-knex + try/finally cleanup pattern", "Schema + connectionType static-property pattern"
    - .planning/phases/02-org-people/02-RESEARCH.md §"Tenant Isolation Pattern (4-Layer Defense)" Layer 4
    - .planning/phases/02-org-people/02-CONTEXT.md D-12 (canonicalize at write time), D-15 (color assignment in transaction)
  </read_first>
  <action>
    For each of the 7 files, follow this STRICT skeleton (do not vary structure — plans 06/07/08 will fill SQL bodies but the shape is frozen here):

    1. 8-line module-header comment block citing: `// app/plugins/shifty-roster/src/connections/requests/<Name>.js`, one-line purpose, "Tenant ID from request.user (session) — NEVER from request.properties. Layer-4 defense.", dynamic-knex note ("knex imported dynamically inside the function body so unit tests can import this module without requiring 'knex' to be installed in the test environment").
    2. Top-level imports of helpers (e.g., `import { canonicalizeText } from '../../helpers/canonicalize.js';` for CreateSoldier/UpdateSoldier/CommitRosterImport/ParseCsvAndValidate; `import { pickNextColor, PALETTE } from '../../helpers/palette.js';` for CreateSoldier/CommitRosterImport/CreateMembership; `import { canonicalizeRoleTag } from '../../helpers/role-tag.js';` for CreateSoldier/UpdateSoldier/ParseCsvAndValidate; `import { sendInvite, bulkDispatchWithBackoff } from '../../dispatch/resend.js';` for CommitRosterImport/InviteLater).
    3. `async function <Name>({ request, connection }) { ... }` with these guard-clauses in order:
       - `const tenant_id = request.user?.tenant_id; if (!tenant_id) throw new Error('<Name>: tenant_id missing from session');`
       - `const actor_user_id = request.user?.user_id; if (!actor_user_id) throw new Error('<Name>: actor_user_id missing from session — unauthenticated request');`
       - For CreateSoldier/UpdateSoldier/CommitRosterImport/ArchiveSoldier/CreateMembership: also extract `caller_team_ids = request.user?.team_ids || []` and `roles = request.user?.roles || []`; compute `is_admin = roles.includes('unit_admin')` (used in Layer-4 scope checks).
    4. Per-request property guards (e.g., `if (!display_name) throw new Error('CreateSoldier: display_name is required');`) — but only on REQUIRED properties; optional fields just default to null/empty.
    5. `const { default: knex } = await import('knex'); const db = knex(connection); try { /* placeholder: SQL implementation in plan 06/07/08 */ return { success: true, todo: '<Name> implementation pending plan-NN' }; } finally { await db.destroy(); }` — the placeholder return is what makes this plan a scaffold rather than a deep implementation. The unit-testable surface (guard clauses) is now active; the DB-touching surface is intentionally a stub.
    6. Static-property tail: `<Name>.schema = { type: 'object', required: [...], properties: { ... } }; <Name>.connectionType = 'Knex'; export default <Name>;`

    Specific stub bodies (each task plan fills the actual SQL — these are placeholders only):
    - **ParseCsvAndValidate**: required `['file_b64']`; stub body returns `{ rows: [], total: 0, todo: 'plan-08' }`.
    - **CommitRosterImport**: required `['rows']`; stub returns `{ rowsCreated: 0, rowsSkipped: 0, rowsErrored: 0, todo: 'plan-08' }`.
    - **CreateSoldier**: required `['display_name']`; canonicalize `display_name` via `canonicalizeText` even in the stub (proves the chain works); stub returns `{ success: true, soldier: { id: null, display_name: canonicalizeText(display_name) }, todo: 'plan-06' }`.
    - **UpdateSoldier**: required `['soldier_id']`; stub returns `{ success: true, todo: 'plan-06' }`.
    - **ArchiveSoldier**: required `['soldier_id']`; stub returns `{ success: true, todo: 'plan-06' }`.
    - **CreateMembership**: required `['soldier_id', 'team_id']`; stub returns `{ success: true, todo: 'plan-07' }`.
    - **InviteLater**: required `['email']`; stub returns `{ success: true, todo: 'plan-06' }`.

    Each `.schema` object lists every documented payload field with `type` and (where applicable) `format: 'email'` for emails, `minimum: 0, maximum: 10` for seniority, etc. Be exhaustive — these schemas are part of the plugin's public contract.

    Confirm syntactic validity by running `node --check` on each file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['ParseCsvAndValidate','CommitRosterImport','CreateSoldier','UpdateSoldier','ArchiveSoldier','CreateMembership','InviteLater']; for (const f of files) { const p='app/plugins/shifty-roster/src/connections/requests/'+f+'.js'; const t=fs.readFileSync(p,'utf8'); const must=['request.user?.tenant_id','request.user?.user_id','await import(\\'knex\\')','await db.destroy()','.schema','.connectionType = \\'Knex\\'','export default']; for (const m of must) { if (!t.includes(m)) { console.error('MISSING in',f+':',m); process.exit(1); } } } console.log('OK'); require('child_process').execSync('node --check app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js && node --check app/plugins/shifty-roster/src/connections/requests/CommitRosterImport.js && node --check app/plugins/shifty-roster/src/connections/requests/ParseCsvAndValidate.js && node --check app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js && node --check app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js && node --check app/plugins/shifty-roster/src/connections/requests/CreateMembership.js && node --check app/plugins/shifty-roster/src/connections/requests/InviteLater.js');"</automated>
  </verify>
  <done>All 7 request handler files exist, pass `node --check`, contain every required token (tenant_id guard, actor_user_id guard, dynamic-knex, try/finally, schema, connectionType, export default); CreateSoldier stub canonicalizes display_name through the helper chain.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire the plugin into app/package.json and app/lowdefy.yaml (dual-declaration rule from Pitfall P3)</name>
  <files>app/package.json, app/lowdefy.yaml</files>
  <read_first>
    - app/package.json (read full file to understand existing dependency lines + alphabetical ordering)
    - app/lowdefy.yaml (read full file — locate the plugins: block, the dependencies in package.json mirror order)
    - .planning/phases/02-org-people/02-PATTERNS.md "app/lowdefy.yaml (UPDATE — config)" — the verbatim YAML to add
    - .planning/phases/02-org-people/02-RESEARCH.md Pitfall P3 ("Lowdefy plugin must be in BOTH package.json AND lowdefy.yaml plugins list")
    - .planning/STATE.md note "Plan 02: pnpm-workspace.yaml excluded from Docker image" — verify `.dockerignore` is unchanged (do not add pnpm-workspace.yaml; the existing exclusion prevents the Phase 1 2.5h hang)
  </read_first>
  <action>
    Edit `app/package.json` `dependencies` block: add `"shifty-roster": "file:./plugins/shifty-roster"` and `"papaparse": "5.5.3"` (papaparse is added at the app level too, even though the plugin pins it, so Lowdefy's outer install resolves it once). Maintain alphabetical ordering within the dependencies block (Phase 1 convention).

    Edit `app/lowdefy.yaml` `plugins:` list: append a new entry at the end of the list (after `shifty-audit-writer`):
    ```
      - name: 'shifty-roster'
        version: 'file:../../plugins/shifty-roster'
    ```
    The `file:../../plugins/shifty-roster` path is **relative from `.lowdefy/server/`** per Phase 1 P02 state note — copy the exact form used for `shifty-audit-writer`. Do NOT use `file:./plugins/shifty-roster` (relative-from-yaml form is wrong — Lowdefy's `addCustomPluginsAsDeps.js` writes the version string verbatim into the server's inner package.json).

    Do NOT edit `.dockerignore` — the existing `pnpm-workspace.yaml` exclusion stays (prevents Lowdefy's inner pnpm workspace-mode hang documented in Phase 1 P02 STATE).

    After both edits, run `git diff -- app/package.json app/lowdefy.yaml` and capture the diff in the executor's reply for the plan-checker.
  </action>
  <verify>
    <automated>node -e "const pkg=require('./app/package.json'); if (!pkg.dependencies['shifty-roster']) {console.error('package.json missing shifty-roster'); process.exit(1);} if (pkg.dependencies['shifty-roster']!=='file:./plugins/shifty-roster') {console.error('wrong path form:',pkg.dependencies['shifty-roster']); process.exit(2);} if (!pkg.dependencies['papaparse']) {console.error('package.json missing papaparse'); process.exit(3);} const fs=require('fs'); const yaml=fs.readFileSync('app/lowdefy.yaml','utf8'); if (!yaml.includes(\"name: 'shifty-roster'\")) {console.error('lowdefy.yaml missing plugins entry name'); process.exit(4);} if (!yaml.includes(\"version: 'file:../../plugins/shifty-roster'\")) {console.error('lowdefy.yaml has wrong version path'); process.exit(5);} const di=fs.readFileSync('.dockerignore','utf8'); if (!di.includes('pnpm-workspace.yaml')) {console.error('CRITICAL: pnpm-workspace.yaml missing from .dockerignore — would cause Lowdefy hang'); process.exit(6);} console.log('OK');"</automated>
  </verify>
  <done>Both config files updated; `.dockerignore` still excludes `pnpm-workspace.yaml`; package.json dependency form is `file:./plugins/...` and lowdefy.yaml version form is `file:../../plugins/...` (they intentionally differ — see Phase 1 P02 STATE).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin module-system → Lowdefy runtime | The 7 request handlers receive `request.user` from session (server-side) and `request.properties` from the Lowdefy request payload. `request.properties.tenant_id` IF EVER ACCEPTED would be forgeable (Pitfall P1). Every handler in this plan derives `tenant_id` from `request.user?.tenant_id` only. |
| `verification_tokens` insert path | `dispatch/resend.js sendInvite()` inserts into Auth.js's verification_tokens table; the token hash algorithm is per Assumption A1 (sha256(rawToken+NEXTAUTH_SECRET)) — risk if wrong is that invite-email clicks fail verification. Plan 08 owns the verification spike. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing (tenant_id forgery) | All 7 request handlers | mitigate | Every handler's body begins with `const tenant_id = request.user?.tenant_id; if (!tenant_id) throw …`. There is no code path that reads `request.properties.tenant_id`; the .schema does not even list it as a property. Verified by Task 3 verify command searching for `request.user?.tenant_id` in each handler. |
| T-02-03 | Tampering (smart-quote stripping at READ time silently corrupts duplicate detection) | canonicalize.js + ParseCsvAndValidate stub + CreateSoldier stub | mitigate | `canonicalizeText` is invoked at WRITE time in CreateSoldier (proven via the stub returning the canonicalized form) and will be invoked in CommitRosterImport (plan 08). No SELECT query strips quotes. Unit tests in Task 2 lock the strip set byte-equal to D-12 (regex `/[’‎‏‪-‮]/g`). |
| T-02-04 | Information Disclosure (magic-link invite token leakage) | dispatch/resend.js | accept (with verification spike in plan 08) | Token is generated as `randomBytes(32).toString('hex')` (32 bytes = 64 hex chars = 256 bits of entropy — well above brute-force threshold). Stored as sha256 hash; raw token only crosses the wire over Resend TLS. Risk: if Auth.js v4's exact hash algorithm differs from our implementation (Assumption A1), tokens won't verify on click. Plan 08 contains a 2h spike to verify against the live `node_modules/next-auth/...` source in the running container BEFORE shipping the bulk-invite path. |
| T-02-06 | Elevation of Privilege (role escalation via role_tag) | role-tag.js canonicalizer | mitigate | `canonicalizeRoleTag` output is constrained to `^[a-z][a-z0-9-]*$` (the DB CHECK regex from plan 01's migration 0011). Role-tag entries are labels (e.g., `medic`, `driver`), not RBAC permissions — the RBAC matrix uses the `membership.role` enum which has 4 hard-coded values (`unit_admin`, `team_manager`, `member`, `viewer`) from migration 0002. A role_tag value of `admin` would simply be a tag named "admin", carrying no privilege. Documented in plan 10's tenant-isolation test. |

</threat_model>

<verification>
- `app/plugins/shifty-roster/` directory exists with the full PATTERNS-mandated tree.
- `node --check` passes on every `.js` file in the plugin.
- `node --test app/plugins/shifty-roster/tests/*.test.mjs` runs all three test files green (≥18 assertions covering canonicalize, palette, role-tag).
- `app/package.json` validates as JSON; `shifty-roster` and `papaparse` are present in `dependencies`.
- `app/lowdefy.yaml` parses as valid YAML; the new `- name: 'shifty-roster'` block is the 6th plugins entry (after the 4 Lowdefy-official ones + shifty-auth + shifty-audit-writer).
- `.dockerignore` still contains `pnpm-workspace.yaml` (regression guard against the Phase 1 hang).
- Path forms confirmed: package.json uses `file:./plugins/shifty-roster` (relative to package.json); lowdefy.yaml uses `file:../../plugins/shifty-roster` (relative from `.lowdefy/server/`).
- **palette.test.mjs asserts byte-equal PALETTE against the literal 24-element FROZEN array (W3 fix); verify gate searches for all 24 hex strings AND the `deepStrictEqual` / `deepEqual` invocation.**
</verification>

<success_criteria>
- [ ] All 18 new/modified files in `files_modified` exist and pass their verify commands.
- [ ] All 3 helper unit-test files run green via `node --test` (≥18 assertions total).
- [ ] CreateSoldier stub's returned `display_name` is byte-equal to canonicalizeText input (proves helper chain works).
- [ ] Plugin is declared in BOTH places (the file existence check in Task 4's verify guards against P3).
- [ ] No deep DB implementation exists in any request handler — every handler returns a `todo: 'plan-NN'` placeholder. Implementation is FROZEN to plans 06, 07, 08.
- [ ] `.dockerignore` still excludes `pnpm-workspace.yaml`.
- [ ] **PALETTE byte-equal assertion present in palette.test.mjs against UI-SPEC §"Color B" FROZEN list (W3 fix).**
</success_criteria>

<output>
After completion, create `.planning/phases/02-org-people/02-02-SUMMARY.md` describing: the directory tree shipped, the exact PALETTE array (24 hex values copied from UI-SPEC §"Color B"), the strip-regex literal (`/[’‎‏‪-‮]/g`), the 7 request-handler names and which downstream plan owns each implementation, the unit-test counts, and a confirmation that the plugin is declared in both `app/package.json` and `app/lowdefy.yaml`. Include the git diff of `app/package.json` + `app/lowdefy.yaml` (3-line context) so plan-checker can verify the path-form rule.
</output>

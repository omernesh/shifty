---
phase: 02-org-people
plan: 06
type: execute
wave: 2
depends_on: [01, 02, 04]
files_modified:
  - app/pages/admin/soldier_detail.yaml
  - app/blocks/color_swatches.yaml
  - app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js
  - app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js
  - app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js
  - app/plugins/shifty-roster/src/connections/requests/InviteLater.js
autonomous: false
requirements:
  - ROST-01
  - ROST-02
  - ROST-03
  - ROST-05
  - ROST-06
  - ROST-07
  - ROST-11
user_setup: []

must_haves:
  truths:
    - "soldier_detail.yaml renders a per-soldier edit form at /soldier_detail?id={uuid} with four cards (identity, role/seniority, memberships, color) (D-06)"
    - "CreateSoldier handler INSERTs into soldier (and optionally app_user) inside one transaction, canonicalizes display_name at write time via canonicalizeText (D-12, ROST-11), assigns next color via pickNextColor with SELECT FOR UPDATE on org_unit.last_color_index (D-15, ROST-06), writes a schedule_audit row via the shifty-audit-writer plugin"
    - "UpdateSoldier handler scoped via Layer-4 SQL: admin can update any soldier; team_manager can update only soldiers in their _user.team_ids; notes column server-side gated to managers (Pitfall P10 — SELECT also nulls notes for non-managers)"
    - "ArchiveSoldier sets status='archived' (never DELETE) and PRESERVES memberships (D-08, ROST-05)"
    - "InviteLater triggers the shared sendInvite helper from dispatch/resend.js (D-07)"
    - "Smart-quote canonicalization runs server-side on both CreateSoldier and UpdateSoldier — same code path as plan 08 CSV import (Pitfall P2)"
    - "[BLOCKING upstream gate] Auth.js verification-token hash spike runs as Task 0 BEFORE any InviteLater wiring is exercised (B3 fix). If the spike fails (live `node_modules/next-auth/...` source disagrees with `dispatch/resend.js sendInvite`'s sha256(rawToken+NEXTAUTH_SECRET) form), do NOT implement InviteLater body until dispatch/resend.js is patched. Plan 08 Task 1's spike then becomes a Wave-3 re-confirmation, not the first verification."
  artifacts:
    - path: app/pages/admin/soldier_detail.yaml
      provides: "Soldier edit form with four cards + manager-only notes + color swatch picker + Teams MultipleSelector + archive button + invite-later button"
      contains: "type: UpdateSoldier"
    - path: app/blocks/color_swatches.yaml
      provides: "Reusable 24-swatch picker block, _ref'd from soldier_detail and my_profile"
      contains: "swatch_0"
    - path: app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js
      provides: "DB-touching CreateSoldier implementation (replaces plan 02 stub)"
      contains: "canonicalizeText"
    - path: app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js
      provides: "DB-touching UpdateSoldier with Layer-4 scope SQL"
      contains: "caller_team_ids"
    - path: app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js
      provides: "UPDATE status='archived' (no DELETE)"
      contains: "status = 'archived'"
    - path: app/plugins/shifty-roster/src/connections/requests/InviteLater.js
      provides: "Single-soldier Resend dispatch + verification_tokens insert"
      contains: "sendInvite"
  key_links:
    - from: app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js
      to: soldier
      via: "INSERT with canonicalizeText(display_name) + pickNextColor"
      pattern: "canonicalizeText\\(display_name"
    - from: app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js
      to: "WHERE id = :soldier_id AND tenant_id = :tenant_id AND (:is_admin OR EXISTS team-membership check)"
      via: "Layer-4 scope SQL"
      pattern: "is_admin OR EXISTS"
---

<objective>
Land the per-soldier edit experience: the soldier_detail.yaml page (D-06), the shared color_swatches.yaml referenced block (UI-SPEC §"Reusable Components — 4"), and the DB-touching implementations of CreateSoldier, UpdateSoldier, ArchiveSoldier, InviteLater (replacing the plan-02 stubs).

Plan 02 froze the request-handler shapes; this plan fills in the SQL bodies. The four key write-time invariants:
1. canonicalizeText at WRITE time (D-12, ROST-11) — same helper used by plan 08's CSV import; never strip at SELECT.
2. pickNextColor + SELECT FOR UPDATE on org_unit.last_color_index inside the transaction (D-15, race-safe per RESEARCH Open Q4).
3. Layer-4 scope SQL on UpdateSoldier — admin can edit anyone; team_manager can edit only soldiers in their _user.team_ids; notes column is server-side gated (Pitfall P10 demands the SELECT also conditional-nulls notes for non-managers).
4. ArchiveSoldier is UPDATE status='archived', NEVER DELETE — preserves memberships and history (D-08, ROST-05, Pitfall P11).

The soldier_detail page renders four Cards in the exact order from UI-SPEC §"Page 3": identity (display_name, email, phone, status, optional invite-later button) → role/seniority (seniority NumberInput, role_tags TagSelector with autocomplete from list_role_tags, manager-only notes Textarea) → memberships (Teams MultipleSelector — write path lands in plan 07) → color (referenced color_swatches block).

color_swatches.yaml is a NEW shared block file at app/blocks/. The repo has no app/blocks/ directory yet — this is the first shared block. PATTERNS analog: "external" (UI-SPEC §"Reusable Components — 4" has the complete recipe). The block exports 24 swatch_N entries, each bound to PALETTE[N] from plan 02's helpers/palette.js (the canonical hex strings are embedded into the YAML at generation time — a `tools/gen-color-swatches.mjs` script reads palette.js and emits the YAML; Task 2 owns this script too).

**B3 fix — Auth.js verification-token hash spike is now an upstream BLOCKING Task 0 in this plan, BEFORE InviteLater is implemented.** Plan 06 is Wave 2; the InviteLater primitive ships here. Plan 08 (Wave 3) was previously the spike owner, but exercising the primitive in Wave 2 before the spike validates it is a sequencing bug. Task 0 below runs the spike at the start of Wave 2; if it fails, InviteLater body is NOT implemented until `dispatch/resend.js sendInvite` is patched. Plan 08 Task 1 becomes a Wave-3 re-confirmation step.

Output: 1 new page + 1 new shared block + 1 new emit script + 4 fleshed-out request handlers (gated on Task 0 spike result); all unit tests still green; soldier_detail walks tenant-isolation forge probes (plan 10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-org-people/02-CONTEXT.md
@.planning/phases/02-org-people/02-RESEARCH.md
@.planning/phases/02-org-people/02-PATTERNS.md
@.planning/phases/02-org-people/02-UI-SPEC.md
@app/pages/admin/manage_invites.yaml
@app/pages/admin/manage_org_units.yaml
@app/plugins/shifty-roster/src/helpers/palette.js
@app/plugins/shifty-roster/src/helpers/canonicalize.js
@app/plugins/shifty-roster/src/helpers/role-tag.js
@app/plugins/shifty-roster/src/dispatch/resend.js
@app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js
@db/migrations/0002_tenancy_and_org.up.sql
@db/migrations/0006_audit_and_solver_runs.up.sql
@.claude/skills/lowdefy/reference/03-blocks.md
@.claude/skills/lowdefy/reference/06-operators.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 [BLOCKING upstream — B3 fix]: Auth.js v4 verification-token hash spike — runs BEFORE InviteLater wiring</name>
  <what-built>
    The shared `sendInvite()` primitive from `app/plugins/shifty-roster/src/dispatch/resend.js` (authored in Plan 02) is about to be wired into the InviteLater handler in Task 1 of this plan. InviteLater is the FIRST use of the primitive — Wave 2 is its earliest exercise. Plan 08 Task 1 was previously the spike owner, but that runs in Wave 3 (AFTER InviteLater ships and is reachable from soldier_detail). B3 moves the spike upstream to this Task 0 so the primitive is validated BEFORE it's exercised, not after.
  </what-built>
  <how-to-verify>
    1. Bring up the Lowdefy container on hpg5 (it must be a build that already includes the shifty-roster plugin from Plan 02):
       `plink -ssh -l claude -pw "Onclaude2103" -batch -hostkey "SHA256:tPg5mYQbJO/9ccGmNGeyJeQQSPXq+C6SL3EHJcbRZMQ" hpg5 "powershell -c \"cd C:\shifts-manager; docker compose up -d lowdefy postgres\""`
    2. Read the live Auth.js source from inside the container:
       `plink ... hpg5 "powershell -c \"docker compose exec lowdefy sh -c 'grep -rn \\\"createHash\\\\|hashedToken\\\\|verificationToken\\\" /build/.lowdefy/server/node_modules/next-auth/lib/email/sendVerificationRequest.js /build/.lowdefy/server/node_modules/next-auth/lib/email/signin.js /build/.lowdefy/server/node_modules/next-auth/lib/email/verifyToken.js 2>/dev/null'\""`.
    3. Compare the algorithm against `dispatch/resend.js sendInvite` (Plan 02). Confirm: same SHA family (sha256 vs sha512), same input form (rawToken+NEXTAUTH_SECRET vs HMAC-SHA256(rawToken, NEXTAUTH_SECRET)), same encoding (hex vs base64).
    4. Trigger a smoke from a temporary harness: call the existing `sendInvite()` helper directly via `docker compose exec lowdefy node -e "..."` against a developer-controlled inbox; click the link in the inbox; confirm the callback URL accepts the token and signs the user in. If verification fails (the user lands on `/api/auth/error?error=Verification`), the assumption is WRONG — STOP and patch `app/plugins/shifty-roster/src/dispatch/resend.js` to the corrected algorithm BEFORE proceeding to Task 1.
    5. Paste the grep output + the smoke-test outcome into the executor's reply.

    **Gate decision tree:**
    - If spike result is "verified: sha256 matches" → proceed to Task 1 (InviteLater body implementation goes ahead).
    - If spike result is "fixed: &lt;correction&gt;" → patch `dispatch/resend.js` (this is a Plan-02 file; touching it here is justified by the upstream-gate semantics; commit the patch as part of this plan with a `fix(02): correct sendInvite hash algorithm per B3 spike` message). Then re-run the spike; only proceed to Task 1 when the smoke passes.
    - If smoke cannot be run (Resend free-tier exhausted, no dev inbox, etc.) → escalate via "blocked: &lt;reason&gt;" resume signal; DO NOT proceed to Task 1 with an unvalidated primitive.

    Time budget: 2 hours per RESEARCH Open Q1.
  </how-to-verify>
  <resume-signal>Type "verified: sha256 matches" with the grep output AND the successful magic-link click, OR "fixed: &lt;algorithm-correction-summary&gt;" with the patched `dispatch/resend.js` `sendInvite` signature, OR "blocked: &lt;reason&gt;" if the smoke cannot be run.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Fill in CreateSoldier, UpdateSoldier, ArchiveSoldier, InviteLater handler bodies (replace plan-02 stubs with real DB writes); each writes a schedule_audit row via the shifty-audit-writer plugin pattern. GATED on Task 0 spike result.</name>
  <files>
    app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js,
    app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js,
    app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js,
    app/plugins/shifty-roster/src/connections/requests/InviteLater.js
  </files>
  <read_first>
    - **Task 0 result** (this plan): confirm the spike returned "verified" or "fixed + re-verified" before starting this task. If neither, DO NOT proceed.
    - app/plugins/shifty-audit-writer/src/connections/requests/AuditWrite.js (entire file — the canonical handler shape including how schedule_audit rows are written; PATTERNS analog "role-match")
    - .planning/phases/02-org-people/02-RESEARCH.md section "Tenant Isolation Pattern (4-Layer Defense) — Layer 4" (the WHERE clause + caller_team_ids derivation)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Color Palette + Round-Robin Assignment" (SELECT ... FOR UPDATE race protection)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Membership (Multi-Team) — SELECT-driven INSERT with tenant cross-check"
    - .planning/phases/02-org-people/02-RESEARCH.md section "Smart-Quote / Bidi-Mark Stripping — Where to invoke" (table of pipeline stages)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Pitfalls to Avoid" P10 (notes column server-side gated)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Magic-Link Invites" + dispatch/resend.js (sendInvite signature)
    - .planning/phases/02-org-people/02-PATTERNS.md section "Notes — divergences for each request"
    - app/plugins/shifty-roster/src/helpers/canonicalize.js (canonicalizeText export from plan 02)
    - app/plugins/shifty-roster/src/helpers/palette.js (PALETTE, pickNextColor exports)
    - app/plugins/shifty-roster/src/helpers/role-tag.js (canonicalizeRoleTag export)
    - app/plugins/shifty-roster/src/dispatch/resend.js (sendInvite export — POST-spike version if Task 0 patched it)
    - db/migrations/0002_tenancy_and_org.up.sql lines 64–111 (app_user + soldier + membership columns, indexes)
    - db/migrations/0006_audit_and_solver_runs.up.sql (schedule_audit columns: id, tenant_id, actor_user_id, actor_kind, from_state, to_state, payload, created_at)
  </read_first>
  <action>
    Each handler keeps the plan-02 guard-clause prelude (tenant_id from request.user, actor_user_id check). Replace the stub body with the real implementation, in a `db.transaction(async trx => { ... })` block.

    **CreateSoldier.js** — body inside transaction:
    1. `const canonicalName = canonicalizeText(display_name);` — strip smart quotes/bidi marks BEFORE INSERT.
    2. If `role_tags` is present, normalize each entry via `canonicalizeRoleTag`; collect any `unknown_tags` that aren't already in `role_tag.key` for this tenant; INSERT them with `ON CONFLICT (tenant_id, key) DO NOTHING` (using knex's `.onConflict([...]).ignore()`).
    3. If `email` is present, look up existing app_user `SELECT id FROM app_user WHERE tenant_id = ? AND email = ?`; if absent, INSERT a new app_user row (`tenant_id`, `email`, `display_name: canonicalName`, `locale: 'he'`) and capture the new id as `appUserId`.
    4. Color: if `team_id` is present, run `SELECT last_color_index FROM org_unit WHERE id = ? AND tenant_id = ? FOR UPDATE` (the `FOR UPDATE` is mandatory per RESEARCH Open Q4 to serialize concurrent INSERTs); compute `colorIndex = pickNextColor(row.last_color_index)`; UPDATE `org_unit SET last_color_index = colorIndex` for the same row; `colorHex = PALETTE[colorIndex]`. If team_id is absent, `colorHex = PALETTE[0]` (user can change in my_profile).
    5. INSERT into soldier with: tenant_id, user_id (appUserId or null), display_name (canonicalName), seniority (default 0), role_tags (canonicalized array, default []), phone_e164 (or null), color (colorHex), status 'active'; RETURNING id.
    6. If team_id present, INSERT membership using the SELECT-driven safe form from RESEARCH §"Membership — Constraints":
       INSERT INTO membership (tenant_id, soldier_id, org_unit_id, role)
       SELECT s.tenant_id, s.id, ou.id, 'member' FROM soldier s, org_unit ou
       WHERE s.id = :soldier_id AND ou.id = :team_id
       AND s.tenant_id = :tenant_id AND ou.tenant_id = :tenant_id
       ON CONFLICT (soldier_id, org_unit_id) DO NOTHING.
    7. Audit row: `await trx('schedule_audit').insert({ tenant_id, actor_user_id, actor_kind: 'user', from_state: null, to_state: 'soldier_created', payload: JSON.stringify({ soldier_id, display_name: canonicalName, team_id, role_tags }) });` — matches the Phase 1 audit pattern.
    8. Return `{ success: true, soldier: { id: soldier_id, display_name: canonicalName, color: colorHex }, app_user_id: appUserId }`.

    **UpdateSoldier.js** — body (one parameterized UPDATE with the Layer-4 scope check from RESEARCH §"Layer 4"):
    1. Pre-canonicalize: `const canonicalName = display_name == null ? null : canonicalizeText(display_name);`. If `role_tags` present, canonicalize via canonicalizeRoleTag and unique-insert into role_tag (same as CreateSoldier step 2).
    2. Compute `is_admin = roles.includes('unit_admin')`, `is_manager_or_admin = is_admin || roles.includes('team_manager')`.
    3. The UPDATE:
       UPDATE soldier
       SET display_name = COALESCE(:display_name, display_name),
           seniority = COALESCE(:seniority, seniority),
           role_tags = COALESCE(:role_tags, role_tags),
           phone_e164 = COALESCE(:phone_e164, phone_e164),
           notes = CASE WHEN :is_manager_or_admin THEN COALESCE(:notes, notes) ELSE notes END,
           color = COALESCE(:color, color),
           updated_at = now()
       WHERE id = :soldier_id
         AND tenant_id = :tenant_id
         AND (
           :is_admin
           OR EXISTS (
             SELECT 1 FROM membership m
             WHERE m.soldier_id = :soldier_id
               AND m.org_unit_id = ANY(:caller_team_ids)
           )
         )
       RETURNING id;
    4. If `RETURNING` produces zero rows → throw "UpdateSoldier: soldier not found or access denied" (caller is admin without matching soldier OR team_manager without team-scope match).
    5. Audit row with `from_state: 'soldier_pre_update'`, `to_state: 'soldier_updated'`, payload includes the change diff.
    6. Return `{ success: true, soldier_id }`.

    **ArchiveSoldier.js** — body:
    1. UPDATE soldier SET status='archived', updated_at=now() WHERE id=:soldier_id AND tenant_id=:tenant_id AND (:is_admin OR EXISTS team-scope subquery from UpdateSoldier) RETURNING id.
    2. Audit row to_state='soldier_archived'.
    3. CRITICALLY: do NOT touch membership rows (D-08, ROST-05 preserve history); do NOT touch app_user; do NOT DELETE anything.
    4. Return `{ success: true }`.

    **InviteLater.js** — body:
    1. Look up the soldier: `SELECT id, user_id, tenant_id FROM soldier WHERE id=:soldier_id AND tenant_id=:tenant_id` — if not found, throw access denied.
    2. Look up the app_user: `SELECT id, email FROM app_user WHERE id=:user_id AND tenant_id=:tenant_id`. If `user_id` is null OR app_user.email is null → throw "InviteLater: no email on file".
    3. Call `await sendInvite({ email: appUser.email, callbackUrl: '/admin_dashboard', locale: 'he', knexTx: trx })` from `dispatch/resend.js` (Task 1 transaction-aware — the verification_tokens row is inserted inside the same transaction; the Resend HTTP call is outside the transaction commit semantically since it's external, but we await it). **Per Task 0 spike result, the sendInvite primitive is already validated against live Auth.js source — proceed with confidence.**
    4. Audit row to_state='invite_resent', payload `{ email: appUser.email }`.
    5. Return `{ success: true, email: appUser.email }`.

    All four handlers wrap their body in `try { ... } finally { await db.destroy(); }`. All four reference the existing helpers via `import { canonicalizeText } from '../../helpers/canonicalize.js';` etc. — the path is fixed by plan 02's exports map.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for (const f of ['CreateSoldier','UpdateSoldier','ArchiveSoldier','InviteLater']) { const p='app/plugins/shifty-roster/src/connections/requests/'+f+'.js'; const t=fs.readFileSync(p,'utf8'); if (t.includes(\"todo: 'plan-\")) { console.error(f+' still has plan stub'); process.exit(1); } if (!t.includes('schedule_audit')) { console.error(f+' missing schedule_audit insert'); process.exit(2); } } const cs=fs.readFileSync('app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js','utf8'); ['canonicalizeText','pickNextColor','FOR UPDATE','soldier_created','ON CONFLICT'].forEach(t=>{if(!cs.includes(t)){console.error('CreateSoldier missing:',t);process.exit(3);}}); const us=fs.readFileSync('app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js','utf8'); ['caller_team_ids','is_admin','is_manager_or_admin','soldier_updated','CASE WHEN :is_manager_or_admin'].forEach(t=>{if(!us.includes(t)){console.error('UpdateSoldier missing:',t);process.exit(4);}}); const as=fs.readFileSync('app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js','utf8'); if (!as.includes(\"status = 'archived'\")) { console.error('ArchiveSoldier missing status archive'); process.exit(5); } if (as.match(/DELETE\\s+FROM\\s+(soldier|membership|app_user)/i)) { console.error('ArchiveSoldier has FORBIDDEN DELETE'); process.exit(6); } const il=fs.readFileSync('app/plugins/shifty-roster/src/connections/requests/InviteLater.js','utf8'); if (!il.includes('sendInvite')) { console.error('InviteLater missing sendInvite'); process.exit(7); } require('child_process').execSync('node --check app/plugins/shifty-roster/src/connections/requests/CreateSoldier.js'); require('child_process').execSync('node --check app/plugins/shifty-roster/src/connections/requests/UpdateSoldier.js'); require('child_process').execSync('node --check app/plugins/shifty-roster/src/connections/requests/ArchiveSoldier.js'); require('child_process').execSync('node --check app/plugins/shifty-roster/src/connections/requests/InviteLater.js'); console.log('OK');" &amp;&amp; node --test app/plugins/shifty-roster/tests/canonicalize.test.mjs app/plugins/shifty-roster/tests/palette.test.mjs app/plugins/shifty-roster/tests/role-tag.test.mjs</automated>
  </verify>
  <done>**Task 0 spike result is "verified" or "fixed + re-verified" before this task begins (B3 gate);** all four handlers no longer return `todo:` placeholders; each invokes canonicalizeText/pickNextColor/etc. as appropriate; UpdateSoldier UPDATE includes the Layer-4 scope SQL with `:is_admin OR EXISTS`; notes column is gated via `CASE WHEN :is_manager_or_admin`; ArchiveSoldier has zero DELETE statements against soldier/membership/app_user; `status = 'archived'` written as one canonical SQL token (W4 fix); node --check passes on all four; existing unit tests still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author tools/gen-color-swatches.mjs and emit app/blocks/color_swatches.yaml from PALETTE</name>
  <files>
    app/blocks/color_swatches.yaml,
    tools/gen-color-swatches.mjs
  </files>
  <read_first>
    - app/plugins/shifty-roster/src/helpers/palette.js (PALETTE export — source of truth)
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Reusable Components — 4. 24-swatch color picker" (block recipe — 4 rows × 6 cols, 32px swatches, 8px gap, 16px padding, selected state 3px solid #000 border)
    - .planning/phases/02-org-people/02-PATTERNS.md section "color_swatches.yaml" (external analog — full recipe in UI-SPEC)
  </read_first>
  <action>
    Create tools/gen-color-swatches.mjs (Node ESM script). It imports PALETTE from `../app/plugins/shifty-roster/src/helpers/palette.js`, then writes app/blocks/color_swatches.yaml.

    The emitted YAML is a single Box block id `color_swatches`:
      type: Box
      layout: { contentGutter: [8, 8], contentWrap: wrap, contentJustify: flex-start }
      style: { maxWidth: 256, padding: 16 }
      blocks: 24 entries, each named `swatch_N` (N from 0 to 23):
        id: swatch_N
        type: Box
        style:
          width: 32
          height: 32
          borderRadius: 16
          cursor: pointer
          backgroundColor: '<HEX>'
          border:
            _if:
              test: { _eq: [{ _state: selected_color_index }, N] }
              then: '3px solid #000000'
              else: '1px solid #D9D9D9'
        events:
          onClick:
            - { id: pick, type: SetState, params: { selected_color_index: N, selected_color_hex: '<HEX>' } }

    The script runs in <1s. Re-running produces byte-identical output (deterministic).

    Run the script: `node tools/gen-color-swatches.mjs` and verify app/blocks/color_swatches.yaml now contains 24 swatch_N entries with the exact PALETTE hexes from UI-SPEC §"Color B" (the script is the contract; PALETTE is the canonical hex array).
  </action>
  <verify>
    <automated>node tools/gen-color-swatches.mjs &amp;&amp; node -e "const fs=require('fs'); const y=fs.readFileSync('app/blocks/color_swatches.yaml','utf8'); const HEX=['#1F77B4','#FF7F0E','#2CA02C','#D62728','#9467BD','#8C564B','#E377C2','#7F7F7F','#BCBD22','#17BECF','#AEC7E8','#FFBB78','#98DF8A','#FF9896','#C5B0D5','#C49C94','#F7B6D2','#C7C7C7','#DBDB8D','#9EDAE5','#393B79','#637939','#8C6D31','#843C39']; for (let i=0;i&lt;24;i++) { if (!y.includes('id: swatch_'+i)) { console.error('MISSING swatch_'+i); process.exit(1); } if (!y.includes(HEX[i])) { console.error('MISSING hex '+HEX[i]); process.exit(2); } } if (!y.includes('selected_color_index')) { console.error('MISSING selected_color_index'); process.exit(3); } console.log('OK 24 swatches');"</automated>
  </verify>
  <done>tools/gen-color-swatches.mjs runs and produces app/blocks/color_swatches.yaml with 24 swatch_N entries; each entry has the matching PALETTE hex from UI-SPEC §"Color B"; selected_color_index state binding present; second run produces byte-identical output.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/pages/admin/soldier_detail.yaml with four cards (identity, role/seniority, memberships, color) wired to UpdateSoldier and the shared color_swatches block</name>
  <files>app/pages/admin/soldier_detail.yaml</files>
  <read_first>
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Page 3: soldier_detail/{id}" (wireframe + card grouping + manager-only notes + form layout convention)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Edit page soldier_detail/{id} (D-06 row→detail flow)" (full YAML pattern including load_soldier query that returns has_app_user and team_ids[])
    - .planning/phases/02-org-people/02-PATTERNS.md section "soldier_detail.yaml" (analogs to manage_invites and manage_org_units update shapes)
    - .planning/phases/02-org-people/02-RESEARCH.md Pitfall P10 (the load_soldier SELECT also conditional-nulls notes for non-managers)
    - app/blocks/color_swatches.yaml (Task 2 output — for the _ref include)
  </read_first>
  <action>
    Create app/pages/admin/soldier_detail.yaml.

    Top-level:
      id: soldier_detail
      type: PageHeaderMenu
      auth: { roles: [unit_admin, team_manager] }
      properties: { title: 'עריכת חייל | shifty' }

    requests:

    1. load_soldier — KnexRaw, payload {tenant_id: _user.tenant_id, soldier_id: _input.id, is_manager_or_admin: _or of _array.includes on _user.roles for unit_admin or team_manager}.
       query (Pitfall P10 — notes server-side gated):
         SELECT s.id, s.display_name, s.color, s.seniority, s.role_tags,
                s.phone_e164, s.status,
                CASE WHEN :is_manager_or_admin THEN s.notes ELSE NULL END AS notes,
                au.email,
                (au.id IS NOT NULL) AS has_app_user,
                COALESCE(
                  (SELECT array_agg(m.org_unit_id) FROM membership m
                   WHERE m.soldier_id = s.id AND m.tenant_id = :tenant_id),
                  '{}'::uuid[]
                ) AS team_ids
         FROM soldier s
         LEFT JOIN app_user au ON au.id = s.user_id
         WHERE s.id = :soldier_id AND s.tenant_id = :tenant_id

    2. list_role_tags — same as in plan 04 manage_soldiers.

    3. list_leaf_teams — same as in plan 04 manage_soldiers.

    4. update_soldier_request — type: UpdateSoldier (plugin). payload mirrors UpdateSoldier.schema: soldier_id from _input.id; display_name/seniority/role_tags/phone_e164/notes/color from _state.soldier_form.*; tenant_id, caller_team_ids, is_admin, roles from _user.

    5. archive_soldier_request — type: ArchiveSoldier (plugin). payload: soldier_id from _input.id; tenant_id, caller_team_ids, is_admin from _user.

    6. invite_later_request — type: InviteLater (plugin). payload: soldier_id from _input.id; tenant_id from _user.

    blocks (Cards stacked vertically with lg=24px gap):

    Card 1 — "זהות" (Title level 2):
      - TextInput soldier_form.display_name (label: שם מלא, defaultValue: _request.load_soldier[0].display_name)
      - TextInput soldier_form.email (label: 'אימייל', defaultValue: _request.load_soldier[0].email, readOnly: true — email is keyed by app_user.id, changing it is a v1.1 feature)
      - TextInput soldier_form.phone_e164 (label: 'טלפון (פורמט בינלאומי +972...)', defaultValue: _request.load_soldier[0].phone_e164)
      - Selector soldier_form.status (label: מצב, options: [{value:active, label:פעיל},{value:archived,label:מארכב}], defaultValue: _request.load_soldier[0].status)
      - Button id: invite_button (title: 'שלח קישור הזמנה', icon: AiOutlineMail, type: dashed)
        visible: { _and: [ { _not: { _request: load_soldier[0].has_app_user } }, { _request: load_soldier[0].email } ] }
        events.onClick: [{ type: Request, params: { requestId: invite_later_request } }, { type: Message, params: { type: success, content: 'קישור הזמנה נשלח ל-{{ email }}' } }]

    Card 2 — "תפקיד וותק" (Title level 2):
      - NumberInput soldier_form.seniority (label: ותק, min: 0, max: 10, defaultValue: _request.load_soldier[0].seniority)
      - TagSelector soldier_form.role_tags (label: תגיות תפקיד, options from list_role_tags, defaultValue: _request.load_soldier[0].role_tags)
      - TextArea soldier_form.notes (label: 'הערות (גלוי למנהלים בלבד)', defaultValue: _request.load_soldier[0].notes)
        visible:
          _or:
            - { _array.includes: { on: { _user: roles }, value: unit_admin } }
            - { _array.includes: { on: { _user: roles }, value: team_manager } }

    Card 3 — "חברות בצוותים" (Title level 2):
      - MultipleSelector soldier_form.teams (label: צוותים, options from list_leaf_teams, defaultValue: _request.load_soldier[0].team_ids)
      - Note: write path for adding/removing memberships lands in plan 07 (CreateMembership handler). For this plan, the MultipleSelector is read-only UI state; on Save, the change diff is included in update_soldier_request.payload but plan 02 schema doesn't enumerate `teams` — plan 07 extends UpdateSoldier.schema to handle membership delta OR plan 07 adds a separate CreateMembership/RemoveMembership call chain. **Choice for plan 06**: render the MultipleSelector now as visual; the actual write happens in plan 07's task chain extension.

    Card 4 — "צבע אישי" (Title level 2):
      - _ref: ../../blocks/color_swatches.yaml — REFERENCES Task 2's shared block. Lowdefy resolves _ref relative to the file; from `app/pages/admin/` to `app/blocks/` is `../../blocks/`.
      - On mount, SetState selected_color_index = (PALETTE.indexOf(_request.load_soldier[0].color)) — Lowdefy operator `_array.indexOf` over a static palette. Selected swatch shows the 3px black border per Task 2's _if logic.

    Footer (Box, contentJustify: space-between):
      - Left: Button "שמור שינויים" type: primary
        events.onClick: [
          { type: Validate, params: soldier_form },
          { type: Request, params: { requestId: update_soldier_request, props: { color: { _state: selected_color_hex } } } },
          { type: Message, params: { type: success, content: 'השינויים נשמרו' } },
          { type: Link, params: { pageId: manage_soldiers } }
        ]
      - Center: Button "ביטול" type: default, onClick: Link to manage_soldiers
      - Right: Button "ארכב חייל" danger: true
        onClick: [
          { type: Confirm, params: { title: 'ארכוב חייל', content: 'החייל "{{ name }}" יוסתר מרשימות פעילות, אך ההיסטוריה תישמר. ניתן לשחזר אותו מאוחר יותר.', okText: ארכב, cancelText: ביטול, okType: danger } },
          { type: Request, params: { requestId: archive_soldier_request } },
          { type: Message, params: { type: success, content: 'החייל אורכב' } },
          { type: Link, params: { pageId: manage_soldiers } }
        ]
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('app/pages/admin/soldier_detail.yaml','utf8'); const must=['id: soldier_detail','roles:','unit_admin','team_manager','load_soldier','soldier_id: { _input: id }','CASE WHEN :is_manager_or_admin THEN s.notes ELSE NULL','has_app_user','team_ids','update_soldier_request','type: UpdateSoldier','archive_soldier_request','type: ArchiveSoldier','invite_later_request','type: InviteLater','_ref: ../../blocks/color_swatches.yaml','MultipleSelector','soldier_form.notes','TextArea','AiOutlineMail','שמור שינויים','ארכוב חייל']; for (const m of must) if (!y.includes(m)) { console.error('MISSING:',m); process.exit(1); } console.log('OK soldier_detail');" &amp;&amp; node tools/check-queries.mjs</automated>
  </verify>
  <done>soldier_detail.yaml exists with all required fields; load_soldier SELECT conditional-nulls notes for non-managers (Pitfall P10); four Cards in correct order; color_swatches block referenced via _ref; action chains call UpdateSoldier/ArchiveSoldier/InviteLater plugin requests; check-queries gate green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → soldier_detail page | Layer 1 (session) + Layer 2 (load_soldier WHERE tenant_id) + Layer 3 (auth.roles) + Layer 4 (UpdateSoldier/ArchiveSoldier in-SQL scope check) + Layer 5 (RLS on soldier from 0009). |
| `notes` column | Defense-in-depth: SELECT nulls it for non-managers (Pitfall P10); UPDATE only writes it via `CASE WHEN :is_manager_or_admin`; client `visible:` hides the input. All three layers must hold. |
| InviteLater verification_tokens insert | **Token-hash algorithm is verified by this plan's Task 0 spike (B3 fix — upstream gate, not the previous deferred-to-plan-08 acceptance). Plan 08 Task 1 is now a Wave-3 re-confirmation step.** |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing (tenant_id forgery) | All 5 requests on the page | mitigate | tenant_id from _user only; plugin handlers re-derive from request.user.tenant_id (Layer-4 belt). check-queries gate confirms. |
| T-02-02 | Information Disclosure (cross-tenant soldier read) | load_soldier | mitigate | WHERE s.tenant_id = :tenant_id explicit; RLS catches any forged tenant_id. |
| T-02-03 | Tampering (smart-quote stripping at READ time) | load_soldier display_name | mitigate | DB stores canonical form (CreateSoldier/UpdateSoldier applied canonicalizeText on WRITE); load_soldier SELECTs the canonical bytes; no read-time stripping. |
| T-02-04 | Information Disclosure (invite token leakage) | InviteLater + sendInvite | mitigate (B3 — upstream-gated) | **Token entropy 256 bits; stored as sha256(token+NEXTAUTH_SECRET); transit over Resend TLS. Task 0 spike validates the hash algorithm against live `node_modules/next-auth/...` source BEFORE InviteLater body is implemented. Disposition upgraded from `accept` to `mitigate` because the verification now precedes the use.** Plan 08 Task 1 becomes a confirmation re-run in Wave 3. |
| T-02-06 | Elevation of Privilege (team_manager editing out-of-scope soldier) | UpdateSoldier WHERE clause | mitigate | `(:is_admin OR EXISTS team-membership check)` in the SQL — caller cannot bypass even with a forged soldier_id, because team_ids comes from session. Plan 10 has explicit RBAC E2E for this. |

</threat_model>

<verification>
- **Task 0 spike completed BEFORE Task 1 starts (B3 fix); result documented in executor's reply.**
- All four request handler files have implementations (no `todo:` placeholders).
- canonicalizeText invoked at WRITE time inside CreateSoldier and UpdateSoldier.
- SELECT FOR UPDATE on org_unit.last_color_index in CreateSoldier (race-safe color assignment).
- ArchiveSoldier has zero DELETE statements; `status = 'archived'` is one canonical SQL token in the source (W4 fix).
- soldier_detail.yaml has all 5 request blocks + 4 Cards + 3 footer buttons.
- color_swatches.yaml has 24 entries with the exact UI-SPEC §"Color B" hex strings.
- node --check on all 4 handlers passes.
- node tools/check-queries.mjs passes.
- Unit tests from plan 02 still green.
</verification>

<success_criteria>
- [ ] **Task 0 spike result is "verified" or "fixed + re-verified" (B3 gate); InviteLater body is implemented only after the spike passes.**
- [ ] 6 files in files_modified all exist; all 3 task verify commands exit 0.
- [ ] CreateSoldier UPDATE-on-org_unit happens with `FOR UPDATE` lock (Open Q4 mitigated).
- [ ] UpdateSoldier UPDATE includes `(:is_admin OR EXISTS …)` Layer-4 scope SQL.
- [ ] ArchiveSoldier is UPDATE-only; no DELETE in code body.
- [ ] notes column server-side gated in both load_soldier SELECT and UpdateSoldier UPDATE.
- [ ] color_swatches.yaml is generated by tools/gen-color-swatches.mjs and contains the PALETTE in exact UI-SPEC order.
- [ ] check-queries gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-org-people/02-06-SUMMARY.md` describing: **the Task 0 spike result (verified or fixed + correction summary) — this is the upstream gate per B3**; each handler's final body shape (one-line summary); the Layer-4 UPDATE clause used in UpdateSoldier/ArchiveSoldier; the FOR UPDATE strategy in CreateSoldier; the soldier_detail Card structure; the path from soldier_detail to color_swatches via _ref; the output of check-queries and the unit-test pass count. If the InviteLater path was smoke-tested against a real Resend send + click, include the result (this de-risks plan 08).
</output>

---
phase: 02-org-people
plan: 05
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/pages/admin/manage_role_tags.yaml
autonomous: true
requirements:
  - ROST-07
user_setup: []

must_haves:
  truths:
    - "Admin can view the tenant's role_tag catalog as a read-only AgGrid (D-13)"
    - "Edit/delete of tags is deferred to v1.1 — an info Alert explains how to create new tags (via CSV import or single-row Add soldier)"
    - "The page is auto-covered by tests/e2e/cross-tenant-leak.spec.ts (no tenant-B tag leaks)"
  artifacts:
    - path: app/pages/admin/manage_role_tags.yaml
      provides: "Read-only role_tag catalog viewer"
      contains: "SELECT id, key, label, created_at"
  key_links:
    - from: app/pages/admin/manage_role_tags.yaml
      to: role_tag
      via: "list_role_tags KnexRaw filtered by _user.tenant_id"
      pattern: "WHERE tenant_id = :tenant_id"
---

<objective>
Create the admin-only read-only role_tag catalog viewer at app/pages/admin/manage_role_tags.yaml per D-13 + UI-SPEC §"Page 8". Phase 2 scope is intentionally read-only — edit/rename/delete cascade is deferred to v1.1 per CONTEXT §Deferred. New tags are created in-flight during CSV import (plan 08 "צור" button per CONTEXT D-13) or as a side-effect of the single-row Add-soldier path (plan 06 CreateSoldier handler inserts unknown tags). This page is the central place to confirm "what tags exist in my tenant".

Closest analog: app/pages/admin/admin_test_audit.yaml lines 1–22, 49–58 — page header + single SELECT + read-only AgGrid + info Alert. PATTERNS match quality: exact.

Purpose: closes ROST-07's "tenant-defined autocomplete" surface area for admins who want to know the canonical tag set without opening a CSV import. Provides the inline-create help text that points users to the right path.

Output: one new YAML; auto-covered by tests/e2e/cross-tenant-leak.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-org-people/02-CONTEXT.md
@.planning/phases/02-org-people/02-RESEARCH.md
@.planning/phases/02-org-people/02-PATTERNS.md
@.planning/phases/02-org-people/02-UI-SPEC.md
@app/pages/admin/admin_test_audit.yaml
@db/migrations/0011_role_tag.up.sql
@.claude/skills/lowdefy/reference/03-blocks.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create app/pages/admin/manage_role_tags.yaml — header + single SELECT + read-only AgGrid + info Alert</name>
  <files>app/pages/admin/manage_role_tags.yaml</files>
  <read_first>
    - app/pages/admin/admin_test_audit.yaml (entire file — PATTERNS analog "exact")
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Page 8: manage_role_tags" (column shape + Alert copy)
    - .planning/phases/02-org-people/02-PATTERNS.md section "app/pages/admin/manage_role_tags.yaml"
    - db/migrations/0011_role_tag.up.sql (column names confirmed: id, tenant_id, key, label, created_at; UNIQUE(tenant_id, key))
  </read_first>
  <action>
    Create app/pages/admin/manage_role_tags.yaml.

    Top-level:
      id: manage_role_tags
      type: PageHeaderMenu
      auth: { roles: [unit_admin] }
      properties: { title: 'תגיות תפקיד | shifty' }

    requests:
      - id: list_role_tags
        type: KnexRaw
        connectionId: shifts_db
        payload:
          tenant_id: { _user: tenant_id }
        properties:
          query: |
            SELECT id, key, label, created_at
            FROM role_tag
            WHERE tenant_id = :tenant_id
            ORDER BY key
          parameters:
            tenant_id: { _payload: tenant_id }

    blocks:
      - id: title
        type: Title
        properties: { content: 'תגיות תפקיד', level: 1 }

      - id: read_only_notice
        type: Alert
        properties:
          type: info
          message: 'עריכת תגיות תתווסף בשלב v1.1.'
          description: 'בינתיים, ניתן ליצור תגיות חדשות בעת ייבוא CSV או הוספת חייל.'
          showIcon: true

      - id: tags_grid
        type: AgGridAlpine
        properties:
          enableRtl: true
          rowData: { _request: list_role_tags }
          domLayout: autoHeight
          defaultColDef: { sortable: true, resizable: true, filter: true, flex: 1 }
          columnDefs:
            - { field: key, headerName: 'מפתח (key)', flex: 1 }
            - { field: label, headerName: 'תיוג בעברית', flex: 1 }
            - field: created_at
              headerName: 'נוצר'
              flex: 1
              valueFormatter:
                _function:
                  __args: 0
                  __return:
                    _date.format:
                      on: { __args: 0.value }
                      format: 'DD/MM/YYYY HH:mm'

      - id: empty_state
        type: Result
        visible:
          _eq:
            - _array.length: { _request: list_role_tags }
            - 0
        properties:
          status: info
          icon: AiOutlineInbox
          title: 'אין תגיות תפקיד'
          subTitle: 'תגיות נוצרות אוטומטית עם הוספת חיילים'

    The Alert text quotes UI-SPEC §"Page 8" verbatim. NO edit/delete affordances are wired — they would be a v1.1 feature. The DateFormat operator uses DD/MM/YYYY HH:mm per I18N-04 (he-locale format hardcoded in Phase 2; Phase 7 will switch to locale-aware).

    Page must be added to app/lowdefy.yaml `auth.pages.roles.unit_admin` allowlist and `_ref:` page list — wiring lands in plan 09's lowdefy.yaml update.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('app/pages/admin/manage_role_tags.yaml','utf8'); const must=['id: manage_role_tags','roles:','unit_admin','list_role_tags','SELECT id, key, label, created_at','FROM role_tag','WHERE tenant_id = :tenant_id','_user: tenant_id','AgGridAlpine','enableRtl: true','read_only_notice','עריכת תגיות תתווסף בשלב v1.1','AiOutlineInbox']; for (const m of must) if (!y.includes(m)) { console.error('MISSING:',m); process.exit(1); } console.log('OK manage_role_tags');" &amp;&amp; node tools/check-queries.mjs</automated>
  </verify>
  <done>YAML exists; check-queries gate green; auth.roles is `[unit_admin]` only (team_manager NOT listed per RESEARCH Layer 3 table); Alert + Result blocks present; AgGrid has enableRtl + DD/MM/YYYY HH:mm valueFormatter; no Add/Edit/Delete affordances.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → list_role_tags | Layer 1 + Layer 2 + Layer 3 (auth.roles: unit_admin only) + Layer 5 (RLS on role_tag from plan 01 migration 0011 inline policy). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing (tenant forgery) | list_role_tags | mitigate | payload.tenant_id from _user only. |
| T-02-02 | Information Disclosure (cross-tenant tag leak) | list_role_tags | mitigate | RLS USING clause from 0011; tests/e2e/cross-tenant-leak.spec.ts auto-discovers this page. |
| T-02-06 | Elevation of Privilege (team_manager seeing admin-only tag list) | page auth | mitigate | auth.roles set to `[unit_admin]` only; Lowdefy redirects team_manager to /unauthorized per Phase 1 wiring. |

</threat_model>

<verification>
- Page exists, valid YAML.
- node tools/check-queries.mjs exits 0.
- Auth restricted to unit_admin only.
- Tag list, info Alert, and empty-state Result block render.
</verification>

<success_criteria>
- [ ] manage_role_tags.yaml committed with all verify tokens.
- [ ] No edit/delete UI affordances (v1.1 deferral honored).
- [ ] check-queries gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-org-people/02-05-SUMMARY.md` describing the page shape, the deferred-edit Alert copy, and confirmation that no mutation requests exist on this page.
</output>

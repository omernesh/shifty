---
phase: 02-org-people
plan: 09
type: execute
wave: 4
depends_on: [04, 06, 07, 08]
files_modified:
  - app/lowdefy.yaml
  - db/migrations/0008_legacy_drop.up.sql
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "The bootstrap /employees page is REMOVED from app/lowdefy.yaml (both the menu link AND the page block) BEFORE migration 0008 drops the underlying tables (RESEARCH order-matters rule + PATTERNS Pre-flight checklist)"
    - "All 7 new Phase-2 pages are referenced in app/lowdefy.yaml: manage_soldiers, soldier_detail, team_detail, my_profile, roster_import, roster_import_result, manage_role_tags"
    - "auth.pages.roles allowlists are extended for unit_admin (8 new pages), team_manager (6 new pages), member (my_profile), viewer (my_profile)"
    - "Menus include manage_soldiers_link, roster_import_link, manage_role_tags_link, my_profile_link in the order specified by UI-SPEC §Sidebar / Navigation"
    - "0008_legacy_drop.up.sql drops employees, shifts, assignments, availability_legacy, time_clock_entries in reverse FK order (RESEARCH section '0008 DDL')"
    - "node tools/check-queries.mjs passes (the @gsd-allow-untenanted exemption on /employees query is gone with the page block)"
    - "schema_migrations.version reaches 12 (or whatever the post-0012 head is) with 0008 also applied; idempotency holds"
    - "Task 3 checkpoint resume-signal is STRICT — requires ALL FIVE verification outputs to be explicitly green; an explicit ABORT guardrail prevents proceeding past step 4 failure (W2 fix)"
  artifacts:
    - path: app/lowdefy.yaml
      provides: "Phase-2 page references + auth.pages.roles + menu entries; legacy /employees page block removed"
      contains: "manage_soldiers_link"
    - path: db/migrations/0008_legacy_drop.up.sql
      provides: "DROP TABLE statements for the 5 legacy Phase-0 bootstrap tables"
      contains: "DROP TABLE IF EXISTS employees"
  key_links:
    - from: app/lowdefy.yaml
      to: app/pages/admin/*.yaml + app/pages/my_profile.yaml
      via: "_ref: list extension for the 7 new pages"
      pattern: "_ref: pages/"
    - from: db/migrations/0008_legacy_drop.up.sql
      to: employees
      via: "DROP TABLE IF EXISTS employees (with reverse-FK-order discipline)"
      pattern: "DROP TABLE IF EXISTS employees"
---

<objective>
Two intertwined cleanups that MUST land in this order: (1) remove the bootstrap `/employees` page from `app/lowdefy.yaml` (both the menu link AND the page block) AND wire the 7 new Phase-2 pages into the menu + auth allowlist + page-refs list; (2) apply migration 0008_legacy_drop.up.sql which DROPs the five Phase-0 bootstrap tables (employees, shifts, assignments, availability_legacy, time_clock_entries) from migration 0001.

The order is non-negotiable per RESEARCH section "Schema Delta — Verification checklist before applying 0008":
1. `app/lowdefy.yaml` no longer contains the `employees` page block (was lines 131–183 in Phase 1).
2. `app/lowdefy.yaml` `menus.links` no longer contains `employees_link` (was lines 81–85).
3. `tools/check-queries.mjs` reports zero violations (the `-- @gsd-allow-untenanted: bootstrap smoke-test surface` exemption becomes obsolete with the page block).
4. Migration 0008 applied AFTER 1+2+3.

If 0008 is applied first, the running Lowdefy container's /employees page would throw on every visit (table not found), breaking the Phase-1 smoke surface that may still be relied on by the Cloudflare Tunnel uptime monitor.

Plan 08 left lowdefy.yaml unmodified for its new pages (`_ref:` and `auth.pages.roles:` and `menus:` blocks weren't touched in plans 04–08 — they're funneled to this plan so executors don't race on the same file). This plan's Task 1 is the single canonical update.

**W2 fix — Task 3 checkpoint resume-signal is tightened**. Previously the resume-signal accepted "applied" if the executor pasted outputs without requiring all five verification outputs to be explicitly green. A partial-green resume could permit migration 0008 to run when (for example) step 4 (manage_soldiers page render) returned a 500 — the legacy /employees page would still resolve at the moment of apply, leaving a window where `0008 DROP TABLE employees` succeeds but the running container still tries to query the dropped table on next /employees visit. The tightened resume-signal requires ALL five step outputs to be explicitly green (step 5 step-by-step format), and adds an explicit ABORT guardrail: "if step 4 fails, do NOT proceed to step 6."

Output: cleaned-up app/lowdefy.yaml; one new SQL migration; check-queries gate green; migration applied on hpg5 with schema_migrations advancing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-org-people/02-CONTEXT.md
@.planning/phases/02-org-people/02-RESEARCH.md
@.planning/phases/02-org-people/02-PATTERNS.md
@.planning/phases/02-org-people/02-UI-SPEC.md
@app/lowdefy.yaml
@db/migrations/0001_init.up.sql
@db/migrations/0004_availability_rules_swaps.up.sql
@db/migrations/0010_audit_revokes.up.sql
@tools/check-queries.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update app/lowdefy.yaml — remove /employees page block + menu entry; add the 7 new Phase-2 pages to plugins/auth.pages.roles/menus/_ref:</name>
  <files>app/lowdefy.yaml</files>
  <read_first>
    - app/lowdefy.yaml (the entire current file — Phase 1 baseline. Locate the /employees page block (was lines 131–183 per RESEARCH; the executor must search for `id: employees` in the actual file since line numbers may have drifted) AND the employees_link menu entry (was lines 81–85))
    - .planning/phases/02-org-people/02-PATTERNS.md section "app/lowdefy.yaml (UPDATE — config)" — verbatim YAML for plugins/auth.pages.roles/menus/page-ref extensions
    - .planning/phases/02-org-people/02-UI-SPEC.md section "Sidebar / Navigation" (menu order specification + role visibility per entry)
    - .planning/phases/02-org-people/02-RESEARCH.md section "Layer 3 — Page-level auth.roles" (the role→page mapping table)
    - tools/check-queries.mjs (lines reading exemption comments — confirm removing the page also removes the exemption)
  </read_first>
  <action>
    Open app/lowdefy.yaml and apply these changes (one atomic edit; do not commit between sub-steps):

    REMOVE 1 — The `employees_link` menu entry from the `menus.links:` list (was lines 81–85). Verify by `grep -n 'employees_link' app/lowdefy.yaml` before/after.

    REMOVE 2 — The entire `- id: employees` page block (was lines 131–183) — this block contains a PageHeaderMenu with a KnexRaw that has the `-- @gsd-allow-untenanted: bootstrap smoke-test surface` comment. The block is in the `pages:` list near the bottom of the file (or referenced from `_ref:` — verify in the actual file).

    ADD A — Extend the plugins: block. After the existing `- name: 'shifty-audit-writer' / version: 'file:../../plugins/shifty-audit-writer'` entry, append:
      - name: 'shifty-roster'
        version: 'file:../../plugins/shifty-roster'
    (Plan 02 was supposed to add this; if plan 02 already did, this is a no-op — search for `shifty-roster` first.)

    ADD B — Extend the auth.pages.roles: block. Maintain the existing role→page allowlist; extend per RESEARCH §"Layer 3" and PATTERNS:
      unit_admin: ADD manage_soldiers, manage_role_tags, roster_import, roster_import_result, soldier_detail, team_detail, my_profile
      team_manager: ADD manage_org_units (read+rename via D-02), manage_soldiers (team-scoped at query level), soldier_detail, team_detail, roster_import, my_profile
      member: ADD my_profile
      viewer: ADD my_profile
    REMOVE from each role's list: employees (the page is gone, the allowlist entry would be orphaned).

    ADD C — Extend the menus.links: list with 4 new MenuLink entries, RTL-ordered per UI-SPEC §"Sidebar / Navigation" (which reads right-to-left visually):
      - id: manage_soldiers_link
        type: MenuLink
        pageId: manage_soldiers
        properties: { title: 'חיילים' }
        visible: { _user: tenant_id }
      - id: roster_import_link
        type: MenuLink
        pageId: roster_import
        properties: { title: 'ייבוא חיילים' }
        visible: { _user: tenant_id }
      - id: manage_role_tags_link
        type: MenuLink
        pageId: manage_role_tags
        properties: { title: 'תגיות תפקיד' }
        visible: { _user: tenant_id }
      - id: my_profile_link
        type: MenuLink
        pageId: my_profile
        properties: { title: 'הפרופיל שלי' }
        visible: { _user: tenant_id }

    ADD D — Extend the `_ref:` page list (or however the existing file structures pages — read the current shape before guessing) with 7 new entries:
      - _ref: pages/admin/manage_soldiers.yaml
      - _ref: pages/admin/soldier_detail.yaml
      - _ref: pages/admin/team_detail.yaml
      - _ref: pages/admin/roster_import.yaml
      - _ref: pages/admin/roster_import_result.yaml
      - _ref: pages/admin/manage_role_tags.yaml
      - _ref: pages/my_profile.yaml

    AFTER all edits: run `node tools/check-queries.mjs`. With the `/employees` page block removed (and with it the `-- @gsd-allow-untenanted:` exemption comment), the gate should still pass — every remaining query in the YAML tree has a real `tenant_id` filter from `_user`. If the gate fails, it means either (a) a new Phase-2 page accidentally introduced an untenanted query (regression — fix the page, not the lowdefy.yaml), or (b) the exemption was on a different page (unlikely — check). Capture the gate's output for the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('app/lowdefy.yaml','utf8'); if (y.includes('id: employees') &amp;&amp; y.match(/-\\s+id:\\s+employees\\s*\\n\\s+type:\\s+PageHeaderMenu/)) { console.error('FORBIDDEN: /employees page block still present'); process.exit(1); } if (y.includes('employees_link')) { console.error('FORBIDDEN: employees_link menu still present'); process.exit(2); } const must=['shifty-roster','file:../../plugins/shifty-roster','manage_soldiers','soldier_detail','team_detail','roster_import','roster_import_result','manage_role_tags','my_profile','manage_soldiers_link','roster_import_link','manage_role_tags_link','my_profile_link','חיילים','ייבוא חיילים','תגיות תפקיד','הפרופיל שלי']; for (const m of must) if (!y.includes(m)) { console.error('MISSING:',m); process.exit(3); } console.log('OK lowdefy.yaml');" &amp;&amp; node tools/check-queries.mjs</automated>
  </verify>
  <done>app/lowdefy.yaml no longer references the /employees page (block + menu link both gone); 7 new pages are wired into _ref + auth.pages.roles + menus; shifty-roster plugin declared; check-queries gate green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author db/migrations/0008_legacy_drop.up.sql to drop the 5 Phase-0 bootstrap tables in reverse FK order</name>
  <files>db/migrations/0008_legacy_drop.up.sql</files>
  <read_first>
    - db/migrations/0001_init.up.sql (Phase-0 bootstrap — confirms the 5 tables exist + their FK relationships so reverse-FK order is correct)
    - db/migrations/0004_availability_rules_swaps.up.sql (per RESEARCH section "Schema Delta — `0008_assignment_state_and_legacy_drop.up.sql` DDL skeleton" + Phase 1 STATE: 0004 renamed `availability` to `availability_legacy` to preserve the D-06 smoke-test surface)
    - db/migrations/0010_audit_revokes.up.sql (analog for header comment style + idempotent BEGIN/COMMIT — PATTERNS analog "role-match")
    - .planning/phases/02-org-people/02-PATTERNS.md section "db/migrations/0008_legacy_drop.up.sql"
    - .planning/phases/02-org-people/02-RESEARCH.md section "`0008_assignment_state_and_legacy_drop.up.sql` DDL (skeleton)"
  </read_first>
  <action>
    Create db/migrations/0008_legacy_drop.up.sql.

    Header (verbatim from PATTERNS):
    ```
    -- 0008_legacy_drop.up.sql -- drop Phase-0 bootstrap tables once Phase 2 supersedes them
    -- Phase 1 D-06 deferred this migration to the Phase 2 boundary.
    -- Pre-flight checklist (verify before applying):
    --   1. app/lowdefy.yaml no longer contains the `employees` page (was lines 131–183).
    --   2. app/lowdefy.yaml `menus.links` no longer contains employees_link (was lines 81–85).
    --   3. tools/check-queries.mjs reports zero violations.
    --   4. Playwright cross-tenant-leak.spec.ts run is clean without `/employees` in scope.
    --
    -- Order: drop in reverse FK dependency to avoid FK violations.
    -- The trigger function set_updated_at() is referenced by other tables — DO NOT drop it.
    ```

    Body (one BEGIN/COMMIT block):
    ```
    BEGIN;

    DROP TABLE IF EXISTS time_clock_entries;
    DROP TABLE IF EXISTS availability_legacy;  -- renamed in 0004 per Phase 1 Plan 02 decision
    DROP TABLE IF EXISTS assignments;
    DROP TABLE IF EXISTS shifts;
    DROP TABLE IF EXISTS employees;

    -- The trigger function set_updated_at() is still referenced by other tables;
    -- DO NOT drop the function.

    COMMIT;
    ```

    The exact order matters: time_clock_entries → availability_legacy → assignments → shifts → employees mirrors the reverse FK chain from 0001. Each DROP uses `IF EXISTS` for idempotent re-runs. **Do not drop `set_updated_at()`** — it's the trigger function referenced by `org_unit.updated_at`, `soldier.updated_at`, and other Phase-1+ tables.

    BEFORE writing this file, read `db/migrations/0001_init.up.sql` to confirm the FK chain. If 0001's order is different (e.g., if `shifts.employee_id REFERENCES employees(id) ON DELETE CASCADE`), then employees must be dropped LAST (after shifts and assignments), which matches the above order. The 0004 renamed-availability commit message says `availability → availability_legacy` — that table still has its FKs into `employees` via `employee_id`, so it drops AFTER its parents.

    Note: the filename is `0008_legacy_drop.up.sql` (matches PATTERNS analog), NOT `0008_assignment_state_and_legacy_drop.up.sql` from RESEARCH §"Schema Delta" — the simpler name is the planner's preferred form (CONTEXT "Claude's Discretion" item: "Migration `0008` timing within Phase 2's plan ordering — planning agent makes the call"). The `assignment.state` column verification claimed by the RESEARCH skeleton title is NOT in this migration's scope — Phase 1's 0003_shifts_and_windows.up.sql already shipped the new `assignment` table; 0008 only handles the LEGACY drop. If executor finds the new assignment table is missing a `state` column at write time, surface that as a deferred item, do NOT bundle it into 0008.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('db/migrations/0008_legacy_drop.up.sql','utf8'); const must=['BEGIN;','COMMIT;','DROP TABLE IF EXISTS time_clock_entries','DROP TABLE IF EXISTS availability_legacy','DROP TABLE IF EXISTS assignments','DROP TABLE IF EXISTS shifts','DROP TABLE IF EXISTS employees','DO NOT drop the function']; for (const m of must) if (!t.includes(m)) { console.error('MISSING:',m); process.exit(1); } if (t.match(/DROP\\s+FUNCTION\\s+set_updated_at/i)) { console.error('FORBIDDEN: would drop set_updated_at function'); process.exit(2); } console.log('OK 0008_legacy_drop');"</automated>
  </verify>
  <done>0008_legacy_drop.up.sql created with 5 DROP TABLE IF EXISTS statements in reverse FK order, plus an explicit DO NOT drop the trigger-function comment; no DROP FUNCTION statement; verify-command tokens all present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Apply 0008_legacy_drop on hpg5 AFTER confirming Task 1's lowdefy.yaml is deployed and the container has reloaded — STRICT resume-signal requires ALL five outputs green (W2 fix)</name>
  <what-built>
    Task 1's edited lowdefy.yaml + Task 2's new migration file. Both committed locally; need to be deployed to hpg5 and the migration applied via golang-migrate. **W2 fix — Resume-signal accepts "applied" ONLY when steps 4 AND 5 AND 7 are explicitly green; an early-failure ABORT guardrail prevents the migration from running if step 4 (manage_soldiers render) or step 5 (/employees 404) fail.**
  </what-built>
  <how-to-verify>
    Numbered sequence — each step must complete green before the NEXT step runs. Step 4 is the hardest cliff: it is the proof that the new Phase-2 surface is live; if it fails, the legacy /employees query path may still be in flight on the running container, and applying 0008 would break the visit. **DO NOT proceed past step 4 if step 4 fails — ABORT and investigate.**

    1. Copy the updated files to hpg5 (the deploy mirror is C:\shifts-manager\):
       `pscp -batch -hostkey "SHA256:tPg5mYQbJO/9ccGmNGeyJeQQSPXq+C6SL3EHJcbRZMQ" -l claude -pw "Onclaude2103" app/lowdefy.yaml claude@hpg5:C:/shifts-manager/app/lowdefy.yaml`
       `pscp -batch ... db/migrations/0008_legacy_drop.up.sql claude@hpg5:C:/shifts-manager/db/migrations/`
    2. Rebuild the Lowdefy container (PsExec wrapping mandatory — `docker compose build` invokes the credential helper):
       `plink ... hpg5 "powershell -c \"$env:PATH = [Environment]::GetEnvironmentVariable('PATH','Machine') + ';' + [Environment]::GetEnvironmentVariable('PATH','User'); psexec -accepteula -nobanner -i 1 -u claude -p Onclaude2103 cmd /c 'cd C:\shifts-manager && docker compose build lowdefy > C:\shifts-manager\build.txt 2>&1 && docker compose up -d lowdefy >> C:\shifts-manager\build.txt 2>&1'; Get-Content C:\shifts-manager\build.txt -Tail 30\""`
       — Expected: tail-30 lines show `Successfully built` and `Started shifty-lowdefy`. If build fails, ABORT.
    3. Wait for the container to reach healthy (start_period 60s); confirm via `docker logs shifty-lowdefy --tail 50` shows no `ERR_MODULE_NOT_FOUND` and `ready - started server`. If logs show errors, ABORT.
    4. **CRITICAL CLIFF — open `http://hpg5:8080/manage_soldiers` in a browser logged in as a tenant-A admin — confirm the page returns HTTP 200 and renders an AgGrid (empty grid is fine). If this returns 500 or fails to load, ABORT — do NOT proceed to step 6.**
    5. Open `http://hpg5:8080/employees` — confirm it now returns 404 (the page is removed; the route should not resolve). If it still returns 200, ABORT — the lowdefy.yaml edit didn't actually take effect; revisit Task 1.
    6. NOW apply 0008 (PsExec wrap to handle cache-miss credential-helper failure, mirroring Plan 01 Task 3 B2 fix):
       `plink ... hpg5 "powershell -c \"$env:PATH = [Environment]::GetEnvironmentVariable('PATH','Machine') + ';' + [Environment]::GetEnvironmentVariable('PATH','User'); psexec -accepteula -nobanner -i 1 -u claude -p Onclaude2103 cmd /c 'cd C:\shifts-manager && docker compose run --rm migrate > C:\shifts-manager\migrate.txt 2>&1'; Get-Content C:\shifts-manager\migrate.txt -Tail 30\""`
       — Expect log line `8/u legacy_drop (Xms)`.
    7. Verify: `plink ... hpg5 "powershell -c \"docker compose exec -T postgres psql -U shifts -d shifts -c '\\\\dt employees | shifts | assignments | availability_legacy | time_clock_entries'\""` — must show "Did not find any relation matching the pattern" or 0 rows for each (the tables are gone).
    8. Verify the trigger function: `plink ... hpg5 "powershell -c \"docker compose exec -T postgres psql -U shifts -d shifts -c '\\\\df set_updated_at'\""` — must show 1 function (preserved).
    9. Re-run the migrate compose service — second run is a no-op (idempotency).

    **W2 strict resume-signal contract**: Type `applied` ONLY when ALL of the following hold simultaneously:
      - Step 4 (manage_soldiers) returned HTTP 200 AND rendered the AgGrid.
      - Step 5 (/employees) returned HTTP 404.
      - Step 7 (\\dt on the five legacy tables) returned `relation does not exist` (or empty result for all five).
      - Step 8 (\\df set_updated_at) returned exactly 1 function row.
      - Step 9 (re-run) was a no-op (golang-migrate log: `no change`).

    If ANY of these fail, the resume-signal MUST NOT be `applied`. Instead use `blocked: step N failed — <details>` with the failure point named explicitly.
  </how-to-verify>
  <resume-signal>Type "applied" ONLY if step 4 returned HTTP 200 AND step 5 returned HTTP 404 AND step 7 `\dt` returned `relation does not exist` for all five legacy tables AND step 8 `\df set_updated_at` shows 1 function row AND step 9 re-run was a no-op. Otherwise type "blocked: step &lt;N&gt; failed — &lt;details&gt;". The "applied" signal is a strict conjunction — partial-green is not acceptable (W2 fix).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lowdefy.yaml auth.pages.roles | This is the Layer-3 source-of-truth for which roles can reach which page. Misconfiguration here is a release blocker. The PATTERNS table is the canonical source — execute as listed. |
| schema_migrations table | golang-migrate tracks idempotency; re-running 0008 after success is a no-op. If the apply fails mid-statement (Postgres transaction rolls back the whole BEGIN/COMMIT block), `dirty=true` flips; manual recovery is `migrate force <version>` AFTER understanding why it failed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege (wrong role gets a page they shouldn't) | auth.pages.roles | mitigate | Execute the PATTERNS allowlist verbatim; Task 1 verify-command scans for every page id in every role's list. Cross-tenant-leak.spec.ts (plan 10) auto-covers reachability — if a role gets a page wrongly, the spec catches it. |
| T-02-02 | Information Disclosure (database query referencing dropped table breaks production) | All Lowdefy YAML pages | mitigate (sequence-enforced + W2-tightened) | Task 3 sequence — UI changes deploy BEFORE migration applies — guarantees no live query targets a dropped table. **W2 fix: the resume-signal requires ALL five outputs green; an early-failure ABORT guardrail at step 4 prevents the migration from running when the new UI surface isn't actually live.** If executor reverses the order, /employees would 500 until the container reloaded. |
| T-02-06 | Privilege escalation via residual /employees auth.pages.roles entry | Layer-3 | mitigate | Task 1 verify-command checks for `employees_link` absence; if the page block is gone but a stray `employees:` entry remains in auth.pages.roles, the Lowdefy build would fail (page not declared) — fast-fail signal. |

</threat_model>

<verification>
- app/lowdefy.yaml has zero /employees references (block, menu link, and auth allowlist entries all gone).
- app/lowdefy.yaml has all 7 new Phase-2 page _refs + the 4 new menu entries + the shifty-roster plugin declaration.
- check-queries gate green.
- 0008_legacy_drop.up.sql created with 5 DROPs and the trigger-function preservation comment.
- migration applied on hpg5; tables time_clock_entries, availability_legacy, assignments, shifts, employees no longer exist; set_updated_at() function still exists.
- schema_migrations advances; idempotent re-run is a no-op.
- **Task 3 resume-signal is the strict 5-output conjunction (W2 fix); partial-green resume is not accepted.**
</verification>

<success_criteria>
- [ ] Task 1 verify-command exits 0 (forbidden tokens absent, required tokens present).
- [ ] Task 2 verify-command exits 0 (migration file shape valid).
- [ ] Task 3 checkpoint outputs pasted (steps 4, 5, 7, 8, 9) — ALL FIVE explicitly green per W2 strict resume-signal.
- [ ] Sequence rule honored: UI ships first, migration applies second; ABORT guardrail at step 4 enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-org-people/02-09-SUMMARY.md` describing: the diff of app/lowdefy.yaml (high-level — what was removed, what was added); the verbatim 0008 migration body; the apply log line; the psql `\dt` outputs confirming the 5 legacy tables are gone; the `\df set_updated_at` output confirming the trigger function is preserved; the schema_migrations.version after the run; the output of check-queries; **the W2 strict resume-signal outcome (all five steps explicitly green) including the manage_soldiers HTTP 200 confirmation, the /employees HTTP 404 confirmation, and the idempotent no-op re-run confirmation**.
</output>

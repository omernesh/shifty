# app/connections/shifts_db.yaml
# Extracted from app/lowdefy.yaml inline connections: block.
# The Knex pool.afterCreate hook (setTenantOnConnection) is registered at server startup
# by the shifty-auth plugin — see app/plugins/shifty-auth/src/hooks/knex-tenant.js.
# YAML cannot express function values; the plugin reads this connection's properties
# and wraps the resulting Knex instance with the afterCreate hook.
id: shifts_db
type: Knex
properties:
  client: pg
  connection:
    connectionString:
      _secret: POSTGRES_CONNECTION_STRING
